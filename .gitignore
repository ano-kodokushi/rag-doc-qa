# ── 环境与密钥（真实 key 绝不入库）──
.env

# ── Python 运行产物 ──
.venv/
__pycache__/
*.pyc

# ── 本地数据与索引 ──
data/chroma/
data/chunks.jsonl

# ── 原始语料：默认忽略，只放行说明文件与自有文档 ──
# 第三方官方文档（有版权）不入库；00_ 开头的自有项目文档入库，保证 clone 下来就能跑通链路
data/raw/*
!data/raw/README.md
!data/raw/00_*.md

# ── 第二语料域（T-026）：软件工程实践 ──
# 全部忽略：内容是本人课程作业（含学号、同学与导师姓名），且部分是他人的课程/案例材料
# ⇒ 既不进公开仓库，也不需要 `!` 放行任何文件（`data/raw_se/README.md` 故意不建）
data/raw_se/
data/chroma_se/
data/chunks_se.jsonl

# ── 评测报告 ──
eval/report*.json
