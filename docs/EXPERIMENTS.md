# 对照实验记录（EXPERIMENTS）

本文件存放**受控对照实验**的设计、原始数据与结论。
每条实验都必须写明：受控条件、自变量、**方法学限制**、以及结论是「方向性证据」还是「统计显著」。

**为什么单独建这个文件**：项目的评测数字（`eval/report.json`）只说"现在是多少"，
而对照实验回答的是"**改动到底有没有用**"。这两件事必须分开存放，否则很容易把波动当成进步。

---

## E-01 · multi-hop 的瓶颈在检索还是生成？（T-016，2026-09-18）

### 0. 为什么要做这个实验

`multi_hop` 是五类指标里最弱的一类（accuracy **0.20**，5 题里只有 1 题完全正确），
而项目当时**缺一个"前后对比"的数字**。

### 1. 方法學前提（**先写清楚，否则实验必然自欺**）

1. **样本量极小**：只有 **5 道**多跳题 → 1 题翻转就是 `0.20 → 0.40`。
   **这个样本量做不了统计推断**，任何"提升"都只能算**方向性证据**。
2. **所以主指标不用准确率**，改用检索层的可诊断指标：
   > **`gold_sources` 的全部依据是否都进了候选池 / top-k？**
3. 为什么需要这个新指标 —— **现有 `hit_rate` 有一个盲区**：
   `eval/metrics.py` 的 `hit_at_k` 是「**任一** `gold_source` 命中即算命中」，
   所以多跳题报的 `hit 5/5` **只说明两篇依据里找到了至少一篇**，并不代表两篇都进了 top-k。
   而且 `hit_at_k` **对 k 单调**（k 越大越容易命中），因此 **`hit_rate` 不能用来跨 k 比较质量** —— 这一条是本实验的副产品发现。

### 2. 受控条件

| 项 | 状态 |
|---|---|
| 题库 | `eval/questions.jsonl` **冻结，一字未改**（20 题：单跳 12 / 多跳 5 / 不可答 3） |
| 语料 | `data/raw/` 12 份 / 400 块，**冻结** |
| 生成模型 | DeepSeek `deepseek-chat`，`temperature=0` |
| 向量模型 | 百炼 `text-embedding-v3`（1024 维） |
| **唯一自变量** | **`TOP_K_FINAL`**（环境变量，改它不需要动代码） |

### 3. Step 0 · 诊断（只调 query embedding，不调生成）

对 5 道多跳题，统计两篇依据各进了几篇（原始输出，`fused` = 融合池，`final` = 最终 top-4）：

| 题 | 判分 | fused 覆盖 | final(top-4) 覆盖 | final 里出现的来源 |
|---|---|---|---|---|
| `q13` | **wrong** | 2/2 | **1/2** | `02_Vue3_响应式基础.md`, `05_Vue3_组合式函数.md` |
| `q14` | partial | 2/2 | 2/2 | `02_…`, `04_Vue3_计算属性.md`, `05_…` |
| `q15` | correct | 3/3 | **2/3** | `08_FastAPI_查询参数.md`, `09_FastAPI_请求体.md` |
| `q16` | partial | 2/2 | 2/2 | `01_Vue3_快速上手.md`, `03_Vue3_组件基础.md` |
| `q17` | partial | 2/2 | 2/2 | `02_…`, `03_…`, `04_…`, `05_…` |

**汇总**：
- 融合池里两篇（及以上）依据**全部覆盖 = 5/5**
- 最终 top-4 里全部覆盖 = **3/5**（`q13`、`q15` 各被截断掉一篇）

**对照**：`single_hop` 的唯一依据进 top-4 = **11/12**（未命中只有 `q07`）。

**诊断结论**：召回没问题（候选池里**永远**能找到全部依据）；`top-4` 截断会丢依据，但只影响 5 题里的 2 题。

### 4. Step 1 · 变体对照（`TOP_K_FINAL` 4 → 8）

同一套题、同一份语料、同一模型、同一天，只改这一个环境变量：

| 指标 | V1 基线（`top_k=4`） | V2（`top_k=8`） | 差异 |
|---|---|---|---|
| `accuracy` | 0.50 | 0.55 | +0.05（= **1 题**） |
| `partial_rate` | 0.25 | 0.30 | +0.05 |
| `wrong_rate` | 0.25 | 0.15 | −0.10（= 2 题） |
| `refusal_accuracy` | 1.00 | 1.00 | 0 |
| `hit_rate` | 0.80 | 0.85 | +0.05（**注意：对 k 单调，不可比**） |
| `avg_latency_ms` | 1316.3 | 1300.8 | −15.5（基本不变） |
| **`single_hop` 对/部分/错** | 6 / 2 / 4 | 7 / 3 / 2 | +1 题 |
| **`multi_hop` 对/部分/错** | **1 / 3 / 1** | **1 / 3 / 1** | **0（一分未动）** |
| `unanswerable` 对/部分/错 | 3 / 0 / 0 | 3 / 0 / 0 | 0 |
| 主指标：多跳「依据全进 top-k」 | 3/5 | 4/5 | +1 题（截断确实缓解了） |

原始产出文件：`eval/report.json`（V1，官方基线）、`eval/report_v2_topk8.json`（V2）；
逐题明细：`eval/report_detail.json`、`eval/report_detail_v2_topk8.json`（均在本地，被 `.gitignore` 挡住）。

### 5. 结论

> ⚠️ **本节第 1、2、6 条已被 `E-02` 推翻**（连续覆盖率显示：扩大召回**真实有效**，只是没跨过判分阈值；
> 瓶颈在**小节级检索精度**，而不是生成侧）。**保留原文以便对照，一律以 `E-02` 为准。**

1. **多跳题的瓶颈不是检索召回。** 决定性证据：把候选从 4 扩到 8，**`multi_hop` 一分未动（仍是 1/5）**；
   而融合池里两篇依据**本来就 5/5 全覆盖**。所以"扩大召回范围"这条路对多跳题无效。
2. **瓶颈在生成侧的跨文档综合。** `q14`/`q16`/`q17` 两篇依据**都在 top-4 里**，却只拿到"部分对" —— 说明资料给到了，模型没综合好。
3. **V2 的总体提升不构成"更好"的证据。** `accuracy 0.50 → 0.55` 与 `single_hop 6 → 7` 都只是 **1 题**的差别；
   项目已记录的运行间波动区间是 **0.45–0.50（±1 题量级）**（`PLAN.md §5.5`），因此这个差异**无法与噪声区分**。
4. **`hit_rate 0.80 → 0.85` 不能当作质量提升**：`hit_at_k` 对 k 单调，k 变大命中率机械上升。
5. **因此不建议改 `TOP_K_FINAL` 的默认值**（`SPEC.md §5` 定为 4）。没有证据表明 8 更好，而改默认值需要先改契约、另开卡。
6. **V3（父子分块 / 按来源限流）本次不做**：它们的动机是"召回不够"，而诊断显示召回**已经足够**。
   若将来要做父子分块，应当把它设计成**生成侧改进**（给综合提供更完整的上下文），而不是检索侧改进 —— 这个动机上的区分比改动本身更重要。

### 6. 方法学限制（**必须与结论一起引用**）

- **5 道多跳题做不了统计推断**：本实验只能给出方向性判断，不能给出"提升 X 个百分点"的结论。
- **准确率有运行间波动**：同输入连跑三次实测 0.45 / 0.45 / 0.50（`PLAN.md §5.5`）。
- **`hit_rate` 跨 k 不可比**（单调性，见上）。
- **向量检索层本身不可跨进程复现**（`PLAN.md §5.12`）：逐次召回的 chunk 排名并非完全一致，
  本实验的"两篇依据是否都进 top-k"是**集合级**判断，因此比逐条排名稳定，但仍带这一层不确定性。
- 本实验**没有**做多轮重复（每个变体只跑一次）—— 因为每次都是一次真实 API 评测（有成本），
  且单轮就足以否证"扩大召回能救多跳题"这一假设。

### 7. 复现方式

```powershell
$P = "C:\Users\a2695\AppData\Local\Programs\Python\Python312\python.exe"
$R = "C:\Users\a2695\Desktop\作业\Agent\rag-doc-qa"
Set-Location $R

# V1 基线（默认 TOP_K_FINAL=4）
& $P -m src.ingest                                   # MOCK=0 时是真实入库
& $P -m eval.run_eval --mode full --questions eval\questions.jsonl --out eval\report.json

# V2 变体（只改环境变量）
$env:TOP_K_FINAL = "8"
& $P -m eval.run_eval --mode full --questions eval\questions.jsonl --out eval\report_v2_topk8.json
Remove-Item Env:TOP_K_FINAL
```

> 注：真实模式需要 `LLM_API_KEY` / `EMBED_API_KEY`（本项目实测时经环境变量注入、未落盘）。

---

## E-02 · 重读 E-01：连续覆盖率**推翻**了 E-01 的结论（T-018，2026-09-18）

> ⚠️ **E-01 的第 5 节结论「检索侧无效、瓶颈在生成侧」是错的。本节是更正，以本节为准。**

### 1. 为什么要重读

E-01 用**判分桶**（对 / 部分对 / 错，阈值 = keypoint 覆盖率 ≥ 0.8）比较 V1/V2，于是 5 道多跳题只要没有整题翻转，看起来就是"一分未动"。
但 `keypoints_match` 本身返回 **0~1 的连续值**，桶把中间信息全丢了。
用连续值重读**同一批已有数据**（零成本，只读 `eval/report_detail*.json`）：

| 指标 | V1（`top_k=4`） | V2（`top_k=8`） | 变化 |
|---|---|---|---|
| `single_hop` 平均 keypoint 覆盖率 | 0.569 | **0.667** | +0.098 |
| `multi_hop` 平均 keypoint 覆盖率 | 0.450 | **0.550** | +0.100 |
| 逐题覆盖率升降 | — | — | **上升 5 题 / 下降 0 题 / 持平 12 题** |

上升的 5 题：`q03` 0.00→0.33、`q07` 0.00→0.33、`q11` 0.50→1.00、`q14` 0.25→0.50、`q16` 0.50→0.75。

**单调向上、零下降** —— 在"抛硬币"零假设下 **p ≈ 0.031（单侧符号检验，5 升 0 降）**。
这是本项目目前**最强的统计证据**。

**所以正确的表述是**：不是"扩大召回无效"，而是「**扩大召回有真实效果，但不足以跨过 0.8 的桶阈值，于是桶看不见**」。

### 2. 真正的瓶颈在哪一层（三层都试过）

| 曾怀疑的瓶颈 | 证据 | 判决 |
|---|---|---|
| 文件级召回 | 融合池 **5/5** 覆盖全部 `gold` 文件 | ❌ 不是 |
| 生成侧跨文档综合 | 模型答"资料里没有" —— 而内容**确实不在它看到的小节里** | ❌ **不是（E-01 说错了）** |
| **小节级检索精度** | 上升的 5 题全是"多给几块后**对的小节**进来了"；下面 6 题缺的内容**全在语料里**，只是检索到同一文件的**错误小节** | ✅ **是这里** |

逐题证据（取自 `eval/report_detail.json` 里模型的真实回答）：

| 题 | 模型的回答 | 语料里的真相 |
|---|---|---|
| `q03` | 「根据现有资料**无法回答**」 | `02_Vue3_响应式基础.md` 有 `### reactive() 的局限性`，**三个局限一字不差** |
| `q12` | 「资料**没有说明**哪两种情况必须写 `.value`」 | 同文件有 `### 数组和集合的注意事项` + `### 在模板中解包的注意事项` |
| `q13` | 「资料中**没有直接给出**官方推荐倾向」 | 原文：`由于这些限制，我们建议使用 ref() 作为声明响应式状态的主要 API` |
| `q14` | 「资料**没有对**计算属性与普通函数调用**进行对比**」 | `04_Vue3_计算属性.md` 有整节 `## 计算属性缓存 vs 方法` |
| `q16` | 「不使用构建步骤时**资料中未展开说明**」 | `03_Vue3_组件基础.md` 原文：`以一个包含 Vue 特定选项的 JavaScript 对象来定义`、`Vue 将会在运行时编译它` |
| `q17` | 「资料**没有包含**组件间传递数据这部分」 | 同文件有 `## 传递 props` + `## 监听事件` |

> **模型的回答是诚实的**：它每次说"资料里没有"，而检索给它的上下文里**确实没有**。
> 所以这既不是幻觉、也不是生成能力问题，而是**给错了小节**。

### 3. 方法学教训（**本项目最重要的一条**）

**粒度决定了你能看见什么。** 同一批数据，三层指标给出三个互相矛盾的结论：

| 指标粒度 | 它说的话 | 真相 |
|---|---|---|
| 文件级（`hit_at_k`） | "召回没问题"（且它**对 k 单调**、还是"**任一**命中即算"） | 粗了两层 |
| 桶级（阈值 0.8） | "检索侧无效" | 粗了一层 |
| **连续覆盖率** | **"有效，只是不够跨阈值"** | ✅ |

推论：
1. 比较任何检索/生成改动时，**主指标用连续 keypoint 覆盖率**，桶指标只作参考。
2. `hit_rate` **不能跨 k 比较**（单调性，见 E-01）。
3. **`keypoints_match` 是字面子串匹配** —— 改写措辞即判错（例：`q02` 模型说"原始值 / 对象"，
   而 keypoints 是"原始类型 / 对象类型"）。**所有准确率数字都必须带这条限制引用**；
   若将来要改（语义判分 / 同义词表），属单独一张卡，且会**破坏与历史数字的可比性**。

### 4. 还有 4 题连 `top_k=8` 都没救

`q02` / `q12` / `q13` / `q17` 的覆盖率在 V2 下**仍未上升**。
→ 光加 k 不够，需要**把命中块所在的小节一起给模型**（父子分块的最小形态），而不是继续加大 k。

### 5. 对后续实验的约束（写进规范）

- 主指标：**逐题连续 keypoint 覆盖率** + 升降计数；桶指标（对/部分对/错）仅作汇报用。
- 每次对照必须报：受控条件、逐题覆盖率、升降计数、**样本量限制**（多跳题只有 5 道）。
- **负结果与正结果同等对待**：E-01 的结论被 E-02 推翻，本身说明"记录 + 可重读的原始数据"比结论更重要 ——
  这也是为什么每个实验都必须把**原始产出文件**（`eval/report*.json`）留在本地。

---

## E-03 · 小节级整理三模式对照：`diverse` 更差、`expand` 持平但大面积丢资料（T-020，2026-09-21）

### 0. 假设与来源

E-02 定位到「检索到同一文件的**错误小节**」。据此设计三个模式（**唯一自变量 `SECTION_MODE`**）：

| 值 | 行为 |
|---|---|
| `off` | 现状：top-k 的块原样返回 |
| `diverse` | 按 `(source, heading)` 去重，每节保留排名最高的一块，**文本不扩展** |
| `expand` | 去重 + 文本换成**该小节全文** |

**预期**：既然需要的内容常在同一文档的**另一个小节**，`diverse`（用同样的 k 覆盖更多小节）应当最好。

### 1. 受控条件

题库 / 语料 / 模型 / `top_k_final=4` 全部冻结，**唯一自变量 `SECTION_MODE`**；每个模式一次真实评测（合计约 0.3 元）。
原始产出：`eval/report_t020_{off,diverse,expand}.json` 与对应 `report_detail_t020_*.json`（本地，已 gitignore）。
prompt 长度那两行不在这些文件里，来自另一次**只跑检索**的复算，口径见 §2 表格下方的说明。

### 2. 结果

| 指标 | `off`（基线） | `diverse` | `expand` |
|---|---|---|---|
| **`avg_keypoint_coverage`** | **0.5343** | 0.5147（↓） | **0.5441**（↑ 但在噪声内） |
| `accuracy` | 0.50 | 0.45 | 0.50 |
| `partial_rate` / `wrong_rate` | 0.25 / 0.25 | 0.35 / 0.20 | 0.25 / 0.25 |
| `refusal_accuracy` / `hit_rate` | 1.00 / 0.80 | 1.00 / 0.80 | 1.00 / 0.80 |
| `avg_latency_ms` | 1235.5 | 1147.4 | 1244.0 |
| prompt 中位 / 最大 | 1624 / 1936 | 1465 / 1936 | **2388 / 4742** |
| **有资料被丢掉的题数** | **0** | **0** | **14**（`q01`/`q03`/`q04`/`q05`/`q06`/`q07`/`q08`/`q09`/`q11`/`q12`/`q13`/`q14`/`q16`/`q17`） |
| 其中"最终 prompt 仍超 3000"的题 | 0 | 0 | **4**（`q04`/`q07`/`q11`/`q14`） |
| 逐题覆盖率升降 | — | 升 1 / 降 1 | 升 2 / 降 1 |

逐题明细：
- `diverse`：`q02` 0.00→0.33；**`q04` 1.00→0.33**（净负）
- `expand`：`q02` 0.00→0.67、`q16` 0.50→1.00；**`q04` 1.00→0.00**（被截断所致）

> **prompt 长度与"丢资料"的行是怎么来的（口径必须写清楚）**：这几行**不是**那三次评测的产物 ——
> 逐题明细文件里没有 prompt 长度字段。它们来自一次**只跑检索、不调用 LLM**的本地复算
> （`Retriever.search()` + `build_prompt(max_chars=3000)`，成本只有 60 次 query embedding，约 0 元）。
> 「有资料被丢掉」的判据是 `len(build_prompt(..., 3000)) < len(build_prompt(..., 0))`，即**有限预算下被 break 掉的资料数 > 0**；
> 而「最终 prompt 仍超 3000」是更窄的判据（连第 1 条资料都塞不进预算）。
> **两个数都是真的，但用哪个口径会决定你看见什么** —— 这正是 E-02 的教训在这里重演一次。

### 3. 结论（**假设被证伪：两个模式都没有净收益**）

1. **`diverse` 比基线更差**（覆盖率 −0.020、accuracy −0.05）：去重**减少了进入上下文的块数**（材料从 4 条降到 2~4 条），
   换来的"小节多样性"补不回丢掉的内容。
   → **"用同样的 k 覆盖更多小节"这个想法不成立**：**内容总量**比小节多样性更重要。
2. **`expand` 覆盖率 +0.010，但落在噪声内**（逐题 升 2 / 降 1），代价却是**大面积丢资料**：
   **14 / 20 题**有资料被 3000 字符预算 break 掉，其中 **4 题**（`q04`/`q07`/`q11`/`q14`）连第 1 条都塞不进、最终 prompt 仍超 3000，
   而 `q04` 直接从 1.00 掉到 0.00。
   **还有一类更隐蔽的破坏**：`build_prompt` 是"遇到第一条塞不下就 `break`"，
   所以当**第 2 条小节特别长**时，模型最后只拿到第 1 条**很小**的材料 —— `q05` 只剩 **346 字符**、`q09` 只剩 **200 字符**、`q08` 只剩 **560 字符**
   （它们的完整材料分别是 6128 / 4558 / 4585 字符）。**prompt 变短不一定是好事，这里是把上下文掏空了。**
   → **收益不显著、风险明确且量级很大**，不能作为默认值。
3. **因此 `SECTION_MODE` 的默认保持 `off`**；代码与测试保留，作为**可复现的实验装置**。
4. **与 E-02 合起来看，故事是一致的**：

   | 改动 | 方向 | 覆盖率结果 |
   |---|---|---|
   | `top_k` 4→8（E-02） | **内容更多** | **5 升 0 降**（有效） |
   | `diverse`（E-03） | **内容更少** | 更差 |
   | `expand`（E-03） | **内容更长但被截断** | 持平偏险 |

   → **真正起作用的是「上下文里有没有那段内容」，而不是它怎么排列、怎么拼长。**
5. **矛头指向「排序」**：需要的内容**明明在融合池里**（E-01 已证：文件级 5/5），却没进 top-k 的窗口 ——
   这正是 **rerank** 要解决的问题。而 `src/retrieve.py` 里的 rerank **已实现、`RERANK_ENABLED=0` 从未启用、也从未用真实 API 验证过**。

### 4. 方法学限制

- 每题只跑一次；n=20，且只有 2~3 题发生移动 → **不能做统计推断**。
  （E-02 的"5 升 0 降"是特例的强信号，本实验没有这种信号 —— **不要把 ±0.01 的覆盖率差异当成改善**。）
- 三个模式的 `hit_rate` 都是 0.80 → 检索层稳定，差异来自生成侧看到的上下文。
- **`expand` 的大面积丢资料是本实验最大的混杂因素**：它的覆盖率数字**严重低估**了"无截断时的扩展效果"。
  要公平评估就得提高 `max_chars`（`SPEC.md §5` 的默认 3000）—— 那属于另一张卡。
  也可以说：**`expand` 的失败至少有一半是预算问题，不全是"整理方式"的问题** —— 这条要说在结论里，不能含糊过去。
- 同一份代码**跨进程复算** prompt 长度时，`vector` 层会漂移（§5.12），所以上表的 prompt 数字应读作**量级**，
  不要当成逐字常量；但"`expand` 会让十几道题丢资料"这个结论对漂移不敏感（它由小节字数决定）。

---

## E-04 · rerank 真实验证：总覆盖率 0.549 → 0.637，**但多跳题零效果**，代价是每次 +2.0 s（T-021，2026-09-21）

> **这是本项目第一个正面结果**（E-01 被推翻、E-02 是更正、E-03 是负结果，都只到"排除了什么"）。
> 但它**不改变默认值** —— 理由见 §5。

### 0. 假设与来源

E-01/E-02/E-03 三个实验把矛头收敛到**排序**：
内容**已经在融合池里**（文件级 5/5）、扩大窗口**有效**（`top_k` 4→8 覆盖率 5 升 0 降）、
而"整理材料的方式"**无效**（`diverse` 更差、`expand` 持平偏险）。
⇒ 缺的不是内容、也不是排列，而是**排序精度**：该进 top-k 的块没进来。

`src/retrieve.py:203-288` 的 rerank 正是为此写的，但 `RERANK_ENABLED=0`、**从未启用、从未联网验证**。

### 1. Step 0 · 官方契约核对（0 元，先于任何调用）

对象：阿里云百炼《文本排序》，[官方文档](https://www.alibabacloud.com/help/zh/model-studio/text-rerank-api)，页面更新时间 **2026-09-04**。

| # | 结论 | 判定 |
|---|---|---|
| 1 | 请求/响应字段与我们实现**一致**：`{model, input:{query,documents}, parameters:{top_n}}` → `output.results[].{index, relevance_score}` | ✅ 实现无需猜改 |
| 2 | 官方已公告 **`gte-rerank` 下线、推荐 `qwen3-rerank`**；后者是**另一套形状**（请求扁平、**响应 `results` 在顶层、无 `output`**、端点 `/compatible-api/v1/reranks`） | ⚠️ 生命周期风险 |
| 3 | 文档端点已改为**业务空间域名** `{WorkspaceId}.cn-beijing.maas.aliyuncs.com/api/v1/...`；我们配的是全局 `dashscope.aliyuncs.com/api/v1/...` | ⚠️ 是否兼容未知 |

> **为什么这一步不能省**：若跳过它直接跑 A/B，最可能的结果是**每次调用抛异常 → 被 `except` 静默降级 → 报告显示"rerank 无提升"**，
> 而真实原因是**契约变了**。那会得出一个**归因错误**的结论 —— 比没有结论更糟。

### 2. Step 1 · 最小连通性探测（3 条路径，各 1 次调用，合计几厘钱）

query = `什么是混合检索`，3 条短文档，`top_n=2`。原始响应（脱敏后照抄）：

| 路径 | 端点 | 模型 / body | HTTP | 响应要点 |
|---|---|---|---|---|
| **P1（我们的现配置）** | `dashscope.aliyuncs.com/api/v1/services/rerank/text-rerank/text-rerank` | `gte-rerank-v2` / 嵌套 | **200** | `{"output":{"results":[{"index":0,"relevance_score":0.8512...},{"index":2,"relevance_score":0.1399...}],"usage":{"total_tokens":85}}` |
| **P2** | 同上 | `qwen3-rerank` / 嵌套 | **200** | `{"output":{"results":[{"index":0,"relevance_score":0.9672...},{"index":2,"relevance_score":0.4601...}],"total_tokens":106}` |
| **P3** | `dashscope.aliyuncs.com/compatible-mode/v1/reranks` | `qwen3-rerank` / 扁平 | **404** | 空响应体 |

**三条结论**
1. ✅ **现配置仍然可用**：老服务端点 + `gte-rerank-v2` + 嵌套 body 返回 200，响应形状与 `_rerank` 的解析**完全一致** → **走 A 路径，`src/retrieve.py` 一行都不用改**
2. ✅ **`qwen3-rerank` 在同一端点上用同一套嵌套 body 也能 200** → 官方推荐的替代模型可以**纯靠 `RERANK_MODEL` 环境变量切换**，无需改代码（**但官方文档说它的形状是扁平的** —— 说明老服务端点做了兼容转换，这条属于**实测行为**，不是文档承诺，将来可能变）
3. ❌ **文档里的 `/compatible-api/v1/reranks` 在老域名上是 404**（`compatible-mode` 也不行）→ **不能**按文档把 `_rerank` 改成扁平形状；那会把一个能用的实现改坏

附带确认：请求**没有**传 `return_documents`（我们靠 `index` 回填原文，官方默认 `false`），**省掉了回传文档正文的流量**，这是对的。

### 3. 受控条件（这次为什么不是"跑两个进程"）

`PLAN.md §5.12` 已证 **chroma 的向量层不可跨进程复现**。若用两条 `python -m eval.run_eval` 做 A/B，
「rerank 的效果」会和「向量召回漂移」**混在一起**，无法归因。所以本实验：

- **同一个 `Retriever` 实例**（同一份 BM25 缓存、同一份 `chunk_map`、同一个 chroma 句柄）
- 只用 `search(..., use_rerank=...)` 这一个参数切换，包一个只注入该参数的薄代理交给 `run_once`
- ⇒ 除 rerank 本身外，两条路径**共享同一份检索状态**

题库 / 语料 / 生成模型 / `top_k_final=4` / `SECTION_MODE=off` 全部冻结。**唯一自变量 `use_rerank`**。
原始产出：`eval/report_t021_rerank_{off,on}.json` 与 `report_detail_t021_rerank_{off,on}.json`（本地，已 gitignore）。

### 4. 结果

| 指标 | `rerank=0` | `rerank=1` | 变化 |
|---|---|---|---|
| **`avg_keypoint_coverage`**（主指标） | **0.5490** | **0.6373** | **+0.088** |
| `accuracy` | 0.45 | **0.50** | +0.05 |
| `partial_rate` | 0.35 | 0.40 | — |
| `wrong_rate` | 0.20 | **0.10** | **−0.10（腰斩）** |
| `refusal_accuracy` | 1.00 | 1.00 | 不变 |
| `hit_rate` | 0.80 | **0.85** | +0.05 |
| `avg_latency_ms` | 1136.2 | **3187.9** | **+2051.7（+1.8 倍）** |

逐题覆盖率：**升 4 / 降 1**

| 题 | 题型 | `rerank=0` | `rerank=1` | 备注 |
|---|---|---|---|---|
| `q03` | single_hop | 0.000 | 0.333 | 升 |
| `q11` | single_hop | 0.500 | 1.000 | 升 |
| `q12` | single_hop | 0.000 | 0.667 | 升 |
| `q16` | **multi_hop** | 0.500 | 0.750 | 升 |
| `q14` | **multi_hop** | 0.500 | 0.250 | **降** |

**⚠️ 按题型拆开之后，结论要改一半（本实验最重要的补充）**

| 题型 | 题数 | `rerank=0` 覆盖率均值 | `rerank=1` 覆盖率均值 | 桶内（对 / 部分 / 错） |
|---|---|---|---|---|
| `single_hop` | 12 | 0.5694 | **0.6944（+0.125）** | 5/4/3 → **6/5/1** |
| `multi_hop` | 5 | **0.5000** | **0.5000（±0.000）** | **1/3/1 → 1/3/1（完全没变）** |

多跳逐题：`q13` 0.000→0.000、**`q14` 0.500→0.250（降）**、`q15` 1.000→1.000、`q16` 0.500→0.750（升）、`q17` 0.500→0.500
→ **`q16` 的 +0.25 与 `q14` 的 −0.25 精确抵消**，等于两题互换。

**⇒ 所以 `+0.088` 的收益全部来自单跳题；rerank 对多跳题是「零效果」。**

这条必须紧挨着结论的第一句，否则会得出「排序就是多跳瓶颈、rerank 已经解决它」这个**错误**结论 ——
而本实验的动机恰恰是**多跳**。真实情况是：**排序确实是全库的瓶颈之一（单跳有效），但它不是多跳的瓶颈。**
一个可解释的机制（**只是假设，本实验没有设计能证伪它的对照**）：
rerank 按「**单个 query 的相关性**」打分，而多跳题需要的是**跨两篇文档的互补覆盖** ——
它会把"第二跳"那篇挤出去，`q14` 正是这个表现。

**三条"确实生效"的证据（缺一条都不能说 rerank 起作用了）**
1. **`_rerank` 被调用 20 次、异常 0 次**（脚本内探针计数）→ 不是"静默降级后看起来没效果"
2. **20 / 20 题的 top-k 顺序被改变** → 输出确实经过重排
3. **"rerank 输出的 chunk 全部来自融合池"被违反 0 题** → 重排没有偷换候选集（它只能在池子里换顺序，符合设计）

### 5. 结论

0. ⚠️ **先说最重要的一条（否则下面的数字会被误读）：rerank 对多跳题「零效果」。**
   收益**全部**来自单跳题（`single_hop` 覆盖率 0.5694 → 0.6944，桶内 5/4/3 → 6/5/1）；
   多跳题覆盖率均值 **0.5000 → 0.5000**、桶内 **1/3/1 → 1/3/1**，两题一升一降精确抵消。
   **本实验的动机是修多跳，而多跳没被修好。** 详见 §4 的按题型拆解。
1. **但 rerank 在本语料与题库上确实有效**：主指标 **+0.088**（0.549 → 0.637），逐题 **升 4 / 降 1**、
   `wrong_rate` **0.20 → 0.10（腰斩）** —— 幅度远超 `§5.5` 记录的运行间波动（覆盖率 ±0.01~0.02）。
   即：**排序是全库的瓶颈之一，但不是多跳的瓶颈。** 这两句话必须一起说。
2. **代价是延迟**：单题 **+2.05 s**（1136 → 3188 ms，+1.8 倍），因为每次都多一次跨网调用
3. **因此本卡不改默认值**：`RERANK_ENABLED` 仍为 `0`。理由是**收益与成本要由使用方按场景权衡**
   （离线批量评测偏质量、在线问答偏延迟），而且 `SPEC.md` 把 rerank 列为**二期可选** ——
   改默认值是另一张卡 + 用户批准的事（详见 `BOARD.md` 决策日志）
4. **对前面实验的收口（但只收到一半）**：E-01/E-02/E-03 把矛头指向"排序"，本实验给出**部分确认** ——
   同一份融合池、只换顺序，主指标就涨了 0.088，**说明"排序"这条线本身是通的**；
   但**它并没有回答"多跳为什么差"**，因为多跳那一栏纹丝不动。
   证据链现在的状态：**内容在池子里（E-01）→ 扩大窗口对多跳有效（E-02）→ 排列方式无效（E-03）→ 排序对单跳有效、对多跳无效（E-04）**
   ⇒ **多跳的瓶颈仍然未知**，已知的排除项是：文件级召回、窗口大小（部分）、材料排列、以及现在的"全局重排"
5. **下一步的候选（都未开卡，按证据强度排序）**：
   - **`top_k` 与 rerank 联合调参**：E-02 说"窗口更大 → 多跳更好"，E-04 说"重排 → 多跳无效"。
     两者合起来暗示一个**可证伪**的假设：**先扩大窗口再多给几条（例如 `RERANK_ENABLED=1` 且 `top_k_final=8`）**，
     让 rerank 只做"排序"而**不替多跳题做"取舍"**。这是最便宜、最直接的下一个实验
   - **保证跨文档覆盖的排序**：例如按 `source` 限制每条来源最多占几个席位（E-01 提出的"多路召回"），
     这与 rerank 的"单 query 相关性"是正交的，恰好能补上它挤掉第二跳的倾向

### 6. Step 3 · 降级路径真实验证（**故意失败**，不是"跑的时候恰好没失败"）

> §4 的第 1 条证据（`_rerank` 20 次 0 异常）只说明 **A/B 那次没触发降级**，它**不能**证明降级路径本身是好的。
> 所以这里**故意**把 key 弄错，让 rerank 必然失败。

**设置**：`RERANK_ENABLED=1`、错误 key（`sk-invalid-...`）、`MOCK=0`（真实模式）、1 道题。
**注意**：这不是"配置错误"，而是**故意注入的故障** —— 验的是「rerank 挂了，问答还能不能用」。

**原始 stdout（脱敏后照抄；只把 key 换成了占位串）**

```
[info] mock=False rerank_enabled=True
[info] rerank_url=https://dashscope.aliyuncs.com/api/v1/services/rerank/text-rerank/text-rerank
[info] rerank_api_key 是否被替换为错误值: True
[info] 融合结果（use_rerank=False）: ['02_Vue3_响应式基础.md#0040', '02_Vue3_响应式基础.md#0031', '02_Vue3_响应式基础.md#0030', '02_Vue3_响应式基础.md#0038']
==============================================================================
[1] 是否抛异常          : 否 ✅
[2] 是否出现降级告警    : 是 ✅
    告警原文            : [warn] rerank 降级: 401 Client Error: Unauthorized for url: https://dashscope.aliyuncs.com/api/v1/services/rerank/text-rerank/text-rerank
[3] 是否与融合结果一致  : 是 ✅
    降级后结果          : ['02_Vue3_响应式基础.md#0040', '02_Vue3_响应式基础.md#0031', '02_Vue3_响应式基础.md#0030', '02_Vue3_响应式基础.md#0038']
==============================================================================
结论: 降级路径实测通过
```

**三条结论**
1. ✅ `search()` **不抛异常**（`401` 被 `src/retrieve.py:287-288` 的 `except Exception` 吞掉）
2. ✅ 标准输出出现 **`[warn] rerank 降级: 401 Client Error: Unauthorized ...`** —— 失败**看得见**，而不是静默变成"没效果"
3. ✅ 返回结果与 `use_rerank=False` 的融合结果**逐字一致** —— 是真降级（退回融合结果），不是返回空、也不是半吊子结果

**`/chat` 同一次故障注入下的补测**（`fastapi.testclient`，不起 uvicorn，真实模式 1 题）：

```
[1] /chat 是否抛异常     : 否 ✅
[2] /chat HTTP 状态码    : 200
[3] 是否返回非空答案     : 是 ✅
    latency_ms           : 7884      ← 含冷启动（首次请求要建 BM25 索引），与 T-014 记录的口径一致
    answer 前 120 字符   : 根据现有资料，可以归纳出 Vue 3 中 `ref` 和 `reactive` 的以下区别：…
[4] 是否出现降级告警     : 是 ✅
结论: /chat 在 rerank 降级下仍然可用
```

**一条环境坑（记下来省得重踩）**：该脚本**第一次运行退出码 1、且没有任何结论输出**，
原因是控制台编码 —— 打印 `✅` 抛 `UnicodeEncodeError`，而我当时把 stderr 重定向掉了，于是"看起来像代码失败"。
加 `PYTHONIOENCODING=utf-8` 后正常。**这与本项目此前多次踩到的"控制台编码假象"是同一个坑**
（PowerShell 的 `$` / 反引号 / 全角引号也曾导致假失败）。

> 本节的脚本是**一次性**的、用完即删、**未入库**（key 只经环境变量注入）。
> A/B 的原始产物（汇总 + 逐题明细）留在本地 `eval/`，已 gitignore。

### 7. 方法学限制

- **每题只跑一次，n=20，5 题发生移动** → 与 E-02 同量级的信号强度，但**仍不是统计检验**；
  引用时说"单次运行、逐题升 4 降 1"，不要说"提升 8.8 个百分点"这种精确口径
- ⚠️ **最重要的一条：总体数字会掩盖"分题型后结论完全不同"**。
  只看总覆盖率是 **+0.088（有效）**，按题型拆开是 **单跳 +0.125 / 多跳 ±0.000（零效果）**。
  而本实验的动机是**多跳** —— **如果只报总体数字，就等于用一个"看起来成功"的结论掩盖了"目标没达成"**。
  这是 E-02「粒度决定你能看见什么」的**第三次**重演（第一次是文件级/桶级/连续值，第二次是"截断"的两种口径）
- **`q14` 是唯一变差的题**（0.500 → 0.250）：说明 rerank **不是单调改进**，
  它会把某些题的正确答案排下去。**这一条必须主动说**，否则等于只报好看的一半
- **rerank 分数的绝对大小无意义**：官方文档明确"此分数为当前请求内的相对分数，**不可跨请求比较**"。
  我们的实现只在**一次请求内**排序，正好符合这个约束 —— 但**不要**把 `relevance_score` 当成可跨题比较的质量分
- **同进程设计是优点也是边界**：它排除了向量漂移这个混杂因素，但代价是
  **本实验的数字不能直接和 `report.json` 的历史数字比**（那次是另一个进程、另一次召回）。
  基线 `0.5490` 与 T-020 的 `0.5343` 的差异来自 LLM 波动与召回漂移，**不是**"改了什么东西"
- **延迟数字含网络抖动**：`+2.05 s` 是一次 20 题运行的均值，未做多次重复取中位；量级可信、小数点不可信
- **P2 的"qwen3-rerank 兼容嵌套 body"是实测行为、不是文档承诺**：官方文档说的是扁平形状。
  若哪天老端点下线或去掉兼容层，`RERANK_MODEL=qwen3-rerank` 会开始抛异常 → 被降级吞掉 →
  **表现又是"rerank 没效果"**。所以这条要连同"降级日志必须被看见"一起记（`PLAN.md §5.15`）

---

## E-05 · `top_k` × rerank 联合：H1 被证伪，**0 元的机制诊断省下了 0.9 元**（T-022，2026-09-21）

### 0. 假设与来源

E-02 与 E-04 的交叉点：
- E-02：`top_k` 4→8 → 多跳覆盖率 **0.450 → 0.550（5 升 0 降）** ⇒ **扩大窗口对多跳有效**
- E-04：rerank 开着、窗口仍 4 → 多跳覆盖率 **0.500 → 0.500（±0.000）** ⇒ **重排对多跳无效**

**H1（可证伪）**：rerank 对多跳无效，是因为窗口只有 4 个席位、它在**替多跳题做取舍**（把"第二跳"挤出去，`q14` 正是这个表现）。
⇒ 预测：**先把窗口扩到 8 再重排，被挤掉的依据应当回来**，即 `(k=8, on)` 的多跳"两篇依据都进窗口"应 ≥ `(k=8, off)`。

**T-022 的设计：先花 0 元看机制，再决定要不要花 0.9 元。** 机制诊断只看**窗口里有没有那篇依据**，不需要任何 LLM 调用。

### 1. Step 1 · 检索侧机制诊断（**无任何 LLM/生成调用**；80 次 query embedding + 40 次 rerank 调用，约 0.01 元）

> ⚠️ **每行分母不同，必须看清**（独立验证抓到过这里的一处口径错误，见 §7）：
> - 「平均命中依据篇数」**分母 = 20 题**（3 道不可答题 `gold_sources` 为空，天然贡献 0 篇）
> - 「全部依据都进窗口」**分母 = 有依据的 17 题**（不可答题的"全部依据"是空集，计入等于白送 ✅，故**不计**）
> - 多跳 / 单跳两列的分母分别是 **5 题 / 12 题**

| 配置 | 平均命中依据篇数<br>（/20） | **有依据的 17 题中「全部依据都进窗口」**<br>（/17） | **多跳两篇都进窗口**<br>（/5） | 单跳依据进窗口<br>（/12） |
|---|---|---|---|---|
| `k=4, off` | 1.000 | 14 / 17 | **3 / 5** | 11 / 12 |
| `k=8, off` | **1.100** | **16 / 17** | **4 / 5** | **12 / 12** |
| `k=4, on` | 1.000 | 14 / 17 | **2 / 5** | 12 / 12 |
| **`k=8, on`（H1 的关键格）** | 1.050 | 15 / 17 | **3 / 5** | 12 / 12 |

> 若把分母统一成 20 题（不可答题按"空集全部进窗口"计 ✅），同一行是 **17 / 19 / 17 / 18**；
> 两个数都对，只是分母不同 —— **写清分母比写哪一个数更重要**。

**H1 的预测是 `(k=8,on)` ≥ `(k=8,off)`；实测 `3/5 < 4/5`。⇒ H1 被证伪。**

**多跳题的文件级连续覆盖率**（= 进窗口的 gold 篇数 / gold 总篇数，作为 §2.12 预注册判据的代理指标）：
`k=4,off` **0.8333** · `k=8,off` **0.9000** · `k=4,on` **0.7333** · **`k=8,on` 0.8333**
→ 同样满足 `(k=8,on) < (k=8,off)`，**换口径后方向不变**（这正是"不靠挑指标得结论"的检验）。

### 2. 三条"rerank 真的在跑"的证据（照用 E-04 的方法，缺一条都不能下结论）

1. **`_rerank` 被调用 40 次 / 异常 0 次**（预期 = 20 题 × 2 个 `on` 配置）
2. **`(k,off)` 与 `(k,on)` 的窗口"内容不同"的题数** —— **同一件事在三种粒度上给出三个不同的数**，
   所以**必须把键写死**（这是「粒度决定你能看见什么」在本项目里的**第四次**现身）：

   | 比较键 | k=4 | k=8 |
   |---|---|---|
   | **`chunk_id` 列表**（`[h.chunk_id for h in hits]`） | **20 / 20** | **20 / 20** |
   | **有序 `source` 列表**（保留重复） | **13 / 20** | **19 / 20** |
   | **`source` 去重集合**（`set(...)`） | **10 / 20** | **12 / 20** |

   - 为什么三个数不同（**逐题核过，不是推测**）：
     - k=4 有 **3 题**（`q01`/`q12`/`q16`）是"**文件集合相同、只有顺序变了**" → 计入有序列表、不计入集合
     - 另有 **7 题**的 `source` 列表**完全相同** —— rerank **只在同一份文件内部换序**（一个窗口里往往有同一文件的多个块），
       **文件级（无论列表还是集合）都看不出**，只有 `chunk_id` 级看得出
   - ⇒ **`chunk_id` 级的 20/20 才是"rerank 确实改变了结果"的正确证据**；引用文件级数字时必须写明是列表还是集合
 
   > ⚠️ **本文档原来给的解释是错的**（独立验证第二轮抓到）：原文写"k=4 时有些题的 4 个窗口槽来自同一份文档，文件级必然相同" ——
   > 这只解释了那 7 题，却被我当成了"13 vs 20"整段差异的原因。真正的构成是 **3 题换序 + 7 题文件内换序**。
   > **错误的归因比错误的数字更难发现**（数字至少能被复算打脸），已按验证意见改写。
3. 逐题窗口内容与缺失来源**逐条打印**（见 §3），不是只报聚合数

### 3. 多跳题逐题：缺的是哪一篇

| 题 | gold 篇数 | `k=4 off` | `k=8 off` | `k=4 on` | `k=8 on` | 读法 |
|---|---|---|---|---|---|---|
| `q13` | 2 | 缺 1 | 缺 1 | 缺 1 | 缺 1 | **四种配置全缺同一篇**（`04_Vue3_计算属性.md`）→ 与窗口/重排**无关** |
| `q14` | 2 | 缺 0 | 缺 0 | **缺 1** | **缺 0** | **唯一符合 H1 的一题**：扩窗口把 rerank 挤掉的救回来了 |
| `q15` | 3 | 缺 1 | **缺 0** | 缺 1 | **缺 1** | **反例**：不开 rerank 时 8 席能装下三篇，一开 rerank 就掉一篇 |
| `q16` | 2 | 缺 0 | 缺 0 | 缺 0 | 缺 0 | 全配置都齐 |
| `q17` | 2 | 缺 0 | 缺 0 | 缺 0 | 缺 0 | 全配置都齐 |

**⇒ 结论兼有两种成分，都比"H1 成立"更有信息量：**
- **`q15` 直接证伪机制**：**8 个席位足够装下三篇依据，rerank 仍然把它们挤掉一篇** —— 所以"席位不够"不是 rerank 挤依据的原因，**是 rerank 本身的目标函数与"跨文档互补覆盖"不一致**
- **`q14` 是唯一的支持例**：扩窗口确实救回了它。**H1 不是"全错"，而是"只在个别题上成立"**
- **`q13` 与窗口/重排完全无关**：四种配置都缺同一篇 —— 这类题的问题在**融合池排名**，不在这两个旋钮上

### 4. 决策点：**不进入 Step 2（省下约 0.9 元）**

> 卡面的决策规则是"若 `(k=8,on)` 的多跳题仍然只有 1 篇依据进窗口 → H1 死，不花钱"。
> **严格说，实测的 3/5 并不是那条规则的字面触发条件**（它不是"只有 1 篇"）。
> 我按**假设本身的预测方向**判定：H1 预测 `(k=8,on) ≥ (k=8,off)`，实测**低于**，
> 而且 `q15` 给出了"席位足够仍被挤掉"的直接反例 —— 机制层面的预测已经失败，所以不再花这 0.9 元。
> **这条判定我写在这里，是为了让"我改过判据吗"这个问题有答案：判据的字面阈值我没用，我用的是更贴近 H1 的预测方向，方向是更严的（提前判定失败，而不是延后）。**

### 5. 一条顺带拿到的重要副产品（解释了 E-04 的分题型结果）

**rerank 从不通过"增加依据覆盖"来提升效果**：
- `k=4`：开 rerank 前后，平均命中依据数都是 **1.000**，可 `accuracy` 却从 0.45 升到 0.50、覆盖率 +0.088（E-04）
- `k=8`：开 rerank 反而让覆盖**变差**（1.100 → 1.050；多跳 4/5 → 3/5）

⇒ **rerank 的贡献在「把窗口内已有的块排到更前面」，而不是「把缺的块找回来」。**
这与 E-04 的分题型结论**完全自洽**：单跳题只需要"最相关那块在前面"（受益），
多跳题需要"两篇都在场"（rerank 不但帮不上，还会挤掉一篇）。

### 6. 方法学限制 / 没做的事（**必须一起引用**）

- **本实验没有 LLM 侧数据** ⇒ 「`(k=8,on)` 的**答案**是否好于 `(k=4,on)`」**仍未测**。
  我判定不必花那 0.9 元，理由是 H1 的机制预测已失败；
  但严格讲：**"机制失败"不等于"答案一定更差"**（E-04 已证 rerank 能靠排序提升单跳，而排序不体现在窗口覆盖上）。
  ⇒ **这条边界必须说清，不能说成"已证明 k=8 + rerank 没有价值"**
- **"依据进窗口"是文件级口径**：它**不能**说明"需要的那一小节"在窗口里 ——
  这正是 E-02 的结论（瓶颈在小节级精度）。所以本实验**没有**推翻 E-02，反而指向它：
  多跳题在 `k=8,off` 下已经 **4/5 文件级齐全**，答案却仍然差 ⇒ **下一个该量的是「小节级」覆盖**
- **每格只跑一次检索**：`vector` 层在同一进程内稳定，但**跨进程会漂移**（§5.12），
  所以这四格的数字只在本次进程内互相可比
- **`q13` 那种"四种配置全缺"的题**：本实验只证明它与这两个旋钮无关，**没有**定位它的原因

### 7. 独立验证与更正（2026-09-21）

**独立验证者自己重跑了本节的机制诊断**（同样只调 embedding、不调 LLM），结果：

| 项 | 独立复现 | 本文档原值 | 判定 |
|---|---|---|---|
| 多跳两篇都进 `(k=4,off)` / `(k=8,off)` / `(k=4,on)` / `(k=8,on)` | 3/5 · 4/5 · 2/5 · 3/5 | 同 | ✅ **逐格一致** |
| 平均命中依据篇数 | 1.000 · 1.100 · 1.000 · 1.050 | 同 | ✅ |
| 单跳依据进窗口 | 11/12 · 12/12 · 12/12 · 12/12 | 同 | ✅ |
| `_rerank` 调用 / 异常 | 40 / 0 | 40 / 0 | ✅ |
| **"全部依据都进窗口"** | **17 / 19 / 17 / 18（分母 20）**<br>或 **14 / 16 / 14 / 15（分母 17）** | 原写 **14 / 20** 等 | ❌ **本文档错** |

**被抓到的错（已在本节 §1 修正）**：原文把「分母 = 有依据的 17 题」的分子（14/16/14/15）**写在了 /20 的分母上** ——
数字本身来自真实数据，但**分母写错导致这一行无法复现**，且与同一张表里"平均命中依据篇数（/20）"的口径互相矛盾。
**已改为显式标注分母**（并同时给出 /20 口径的 17/19/17/18），**因为"写清分母"比"写哪一个数"更重要**。

**另外三处据验证意见改进的点**（都属于"口径没说清"而不是"数字错"）：
1. 「窗口内容不同」**写死了比较键**，并给出三种粒度的数（`chunk_id` 20/20、有序 `source` 列表 13/20 与 19/20、去重集合 10/20 与 12/20，见 §2）
2. 成本口径**补充 40 次 rerank 调用**（rerank 是计费接口，原文只说"仅 query embedding"不完整）
3. 补上 §2.12 预注册判据的**代理指标**（多跳文件级连续覆盖率 `k8on 0.8333 < k8off 0.9000`），证明**换口径方向不变**

**第二轮验证又抓到 1 处"归因错误"（比数字错更难发现）**：§2 原来把"文件级 13/20 vs chunk 级 20/20"的差异归因于
"有些题的 4 个槽来自同一份文档"，但那只能解释 7 题；**真正构成是 3 题换序 + 7 题文件内换序**。
已改写，并把三个粒度的数并排列出 —— **数字错能被复算打脸，归因错不能，只能靠独立复核**。

> **独立验证的价值在这一轮非常直接**：核心结论 4 个格子全部被复现，**但累计 5 处口径/归因描述有问题** ——
> 如果不做这一步，"14/20"这个复现不出来的数字、以及那条错误的归因，都会一直留在文档里，
> 而它们**恰好出现在结论旁边**（读者会先看它们）。**两轮验证一共改了 8 处**（1 处数字分母 + 1 处归因 + 6 处口径/表述）。

---

## E-06 · 小节级覆盖的量化：**H2 成立，多跳有四种病因，且「扩大窗口」这条线远没到头**（T-023，2026-09-21）

### 0. 假设与来源（**判据先于结果**，这是本项目第一次做到有版本证据）

- **E-02**（定性）：多跳的瓶颈是「检索到同一份文件的**错误小节**」—— 但当年只有"缺的内容全在语料里"这种定性说法
- **E-05**：`k=8` 且不开 rerank 时，多跳题已经 **4/5 文件级齐全**，答案却仍然差

**H2（可证伪）**：把口径从「文件」降到「小节 / 块」之后，覆盖率会**明显低于**文件级。

> ⚠️ **本卡的判据（5 条）在跑实验之前就写进 `PLAN.md §2.13` 并单独提交**，commit **`5c33943`**（"开卡「小节级覆盖的量化」——判据先于结果，单独提交"）。
> 这是 T-022 的流程教训（`EXPERIMENTS.md` E-05 §7 / `BOARD.md` 决策日志：开卡与结项在同一次改动里，git 无法证明时序）的**直接兑现** ——
> 现在任何人 `git show 5c33943` 都能看到判据写在结果之前。**结论提交是 `3cb0676` 之后的另一次提交。**
> 本卡执行期间**没有调整任何判据**（§2.13 第 5 条要求"若有调整必须写进 E-06"，此处如实声明：**无调整**）。

### 1. 证据可定位率（预注册判据第 4 条：定位不到的必须单独报数）

**53 / 53 个 keypoint 全部匹配到 chunk（100%）** —— 没有"定位不到"的 keypoint 需要排除。

**但必须说清这一步的弱点**：匹配是**归一化子串匹配**，所以**越通用的 keypoint 命中越多**：

| 极端例子 | 匹配到的小节数 / 证据块数 |
|---|---|
| `q16` 的 `.vue` | **52 个小节 / 141 个块** |
| `q16` 的 `单文件组件` | 6 / 9 |
| `q09` 全题 | 19 个小节 |
| `q02`/`q03`（最短） | 1~2 个小节 |

⇒ 对**通用 keypoint**，"证据小节"这个集合会很大，**小节级命中因此被放宽**（集合里任意一个进窗口就算命中）。
**这个偏差的方向是对 H2 不利的**（让小节级看起来更好），所以"小节级仍然明显低于文件级"这个结论**不依赖**这个偏差；
但**不能**反过来用它证明小节级其实更好。

### 2. 覆盖率四级分解（预注册判据第 1、2、3 条）

**多跳（5 题）**

| 来源 | 文件级 | **小节级** | 块级 |
|---|---|---|---|
| 融合池 `fused` | **5/5** | 4/5 | 3/5 |
| 窗口 `k=4, off` | 3/5 | 2/5 | 1/5 |
| **窗口 `k=8, off`** | **4/5** | **2/5** | **1/5** |

**单跳（12 题）**

| 来源 | 文件级 | 小节级 | 块级 |
|---|---|---|---|
| 融合池 `fused` | 12/12 | 12/12 | 11/12 |
| 窗口 `k=4, off` | 11/12 | 7/12 | 5/12 |
| 窗口 `k=8, off` | **12/12** | **9/12** | **7/12** |

**全部 20 题**：融合池 17/16/14 → `k=4,off` 14/9/6 → `k=8,off` **16/11/8**（文件级/小节级/块级）
（不可答题 `keypoints` 为空，恒不命中，见 §5 限制）

**各级定义**（写死，避免口径漂移）
- **文件级**：该题 `gold_sources` **全部**出现在窗口内
- **小节级**：对**每个**可定位 keypoint，其证据小节集合与窗口小节集合**有交集**（= 该 keypoint 的"那一节"在场）
- **块级**：对**每个**可定位 keypoint，其证据 chunk 集合与窗口 chunk 集合**有交集**（= 真正承载该要点的块在场）

### 3. **H2 判定：成立**

**多跳题在同一个配置（`k=8, off`）下：文件级 4/5 → 小节级 2/5。**
⇒ **窗口里有那份文件，但没有那一节** —— E-02 的定性结论**第一次被量化**。
**小节级未过的是 4 题（`q13`/`q14`/`q16`/`q17`）**，其中**因"整个节没进窗口"而失分的是 3 题（`q13`/`q14`/`q17`）**，
`q16` 属另一种（节在、承载要点的块不在，见 §4）。

**更狠的一级**：块级只有 **1/5** —— 真正承载要点的**那一块**几乎从不在窗口里。

**一个必须同时说的反例方向**：`k=8` 相对 `k=4`，文件级从 3/5 升到 4/5、单跳小节级从 7/12 升到 9/12，
但**多跳的小节级一步没动（2/5 → 2/5）** ⇒ **在 `k=4→8` 这一步上，扩大窗口只改善"文件在不在"、改善不了"节对不对"**。
这与 E-05 的结论（扩窗口对多跳依据进窗口仅从 3/5 到 4/5）是同一件事的两个粒度视角。

> ⚠️ **但这**不等于**"窗口这条线到头了"** —— 见 §3.5：把 `k` 继续加大，多跳的小节级与块级**继续上升**。
> 我最初在这里写下了"改善不了节对不对"并把方向判死，**是我错了**（独立验证后的补充测量抓到的，见 §7）。

### 4. 副产品（本卡最有价值的部分）：多跳的 20 个 keypoint 拆成**四种病因**

把 5 道多跳题的 20 个 keypoint 逐个按"丢在哪一级"归类（`k=8, off`）：

| 病因 | 个数 | 含义 | 该怎么治 |
|---|---|---|---|
| **① 池子里就没有** | **3 / 20** | 该 keypoint 的**全部**证据块都不在融合池（池中**至少一块**即算 ②）⇒ 问题在**召回/融合**，不在窗口、不在重排 | 提高召回（多路召回 / 换 embedding / 加大候选） |
| **② 池子里有、窗口里没有（节错）** | **4 / 20** | 典型「文件对了、节错了」⇒ 问题在**排序/截断** | **加大窗口**（§3.5 证有效）或按小节召回；⚠️ rerank 已证对多跳净负（E-04/E-05） |
| **③ 小节在、块不在** | **2 / 20** | 窗口有小节里的别的块，但没有承载该要点的那一块 | 窗口内块的选择（块级排序 / 相邻块扩展） |
| **④ 全都在场** | **11 / 20** | 要点所需的东西**都在模型眼前** ⇒ 若仍答错，只能是**生成/综合** | 提示词 / 分步生成 / 子问题拆解 |

> ⚠️ **①/② 的判据原文写错过（独立验证抓到，见 §7）**：我第一次把 ① 写成"**任一**证据块不在融合池"，
> 但表里的 3/4/2/11 **只有按"全部证据块都不在池"才算得出来**（按字面"任一"算是 6/1/2/11）。
> 现已把 ① 的**规则文本改成与算法一致**，并在此留痕 —— **数字未变，改的是文字**。
> 这正是 E-05 §7 记录过的同一类病：**规则与结果不自洽，只能靠独立复核发现**。

**分类规则（有序，先判前者）+ 逐 keypoint 判定表**（`k=8`；三个谓词都是可独立复算的）

```
P1 chunk_in_pool     = 证据块 ∩ 融合池 ≠ ∅
P2 section_in_window = 证据小节 ∩ 窗口小节 ≠ ∅
P3 chunk_in_window   = 证据块 ∩ 窗口 ≠ ∅
① = not P1       ② = P1 and not P2       ③ = P1 and P2 and not P3       ④ = P3
```

| 题 | keypoint | P1 池 | P2 节在窗 | P3 块在窗 | 类 |
|---|---|---|---|---|---|
| `q13` | 主要 API | ✅ | ❌ | ❌ | **②** |
| `q13` | 不能替换整个对象 | ❌ | ❌ | ❌ | **①** |
| `q13` | 对解构操作不友好 | ❌ | ❌ | ❌ | **①** |
| `q13` | 缓存 | ✅ | ❌ | ❌ | **②** |
| `q14` | 计算属性 | ✅ | **✅** | ❌ | **③** |
| `q14` | 缓存 | ✅ | ❌ | ❌ | **②** |
| `q14` | 组合式函数 | ✅ | ✅ | ✅ | ④ |
| `q14` | 有状态逻辑 | ✅ | ✅ | ✅ | ④ |
| `q15` | 路径中声明 / 单一类型 / Pydantic 模型 / 请求体 | ✅ | ✅ | ✅ | ④×4 |
| `q16` | `.vue` / 单文件组件 / JavaScript 对象 | ✅ | ✅ | ✅ | ④×3 |
| `q16` | 运行时编译 | ✅ | **✅** | ❌ | **③** |
| `q17` | 组合式函数 / 有状态逻辑 | ✅ | ✅ | ✅ | ④×2 |
| `q17` | props | ✅ | ❌ | ❌ | **②** |
| `q17` | 自定义事件 | ❌ | ❌ | ❌ | **①** |

**计数：① 3 · ② 4 · ③ 2 · ④ 11** —— 与上表一致。
> **这张表同时是对一条验证意见的裁决**：第三轮验证怀疑"② 的标签（节没进窗口）与算法（块不在窗口）不一致，
> 那两题（`q14/计算属性`、`q16/运行时编译`）应属 ③"。**实测三个谓词后：② 的四个成员 `P2` 全是 `False`（节确实不在窗口），
> 而该意见点名的两题 `P2=True, P3=False` —— 本来就归在 ③。** 故该意见**不成立**，判据与标签自洽；
> 这张逐条表就是证据（谁都可以拿它复算）。

**逐题落点（`k=8, off`）**

| 题 | ①池子没有 | ②进了池子但节没进窗口 | ③小节在块不在 | ④全在场 | 该题最终得分（E-04 的 `off` 栏） |
|---|---|---|---|---|---|
| `q13` | **2**（不能替换整个对象 / 对解构操作不友好） | 2（主要 API / 缓存） | 0 | 0 | 覆盖 0.000（wrong） |
| `q14` | 0 | **1**（缓存） | 1（计算属性） | 2 | 0.500（partial） |
| `q15` | 0 | 0 | 0 | **4** | **1.000（correct）** |
| `q16` | 0 | 0 | **1**（运行时编译） | 3 | 0.500（partial） |
| `q17` | **1**（自定义事件） | **1**（props） | 0 | 2 | 0.500（partial） |

**⇒ 四个结论，一个比一个重要**

1. **多跳不是"一个瓶颈"，是至少三个**：召回层 3/20、排序/截断层 4/20、窗口内块选择 2/20 ——
   过去四张卡（E-02/E-03/E-04/E-05）一直在**排序**这一格上打转，而它只占 **4/20**
2. **E-01 的第一版结论被部分平反**：`q15` **四个要点的证据块都至少有一块在窗口里，而它答对了（1.000）**；
   `q16` 也 3/4 在场却只拿 0.500 ⇒ **在场之后仍然答不好的情况确实存在**（④ 占 11/20）。
   当年 E-01 说"瓶颈在生成侧"、被 E-02 用指标粒度推翻 —— 现在看：**E-01 错在把它当成唯一原因，而不是错在指出了它**
3. ~~**"扩大窗口"这条线已经走到头**~~ → **错，见 §3.5**（我在 §3.5 已推翻自己：`k=12` 仍能把多跳小节级从 2/5 拉到 3/5、块级从 1/5 拉到 3/5）
4. **病因是"分布"不是"单一原因"**：引用时必须给四个数，**不能说"找到了多跳的原因"**

### 3.5 **补充测量（独立验证之后补做，0 元）：窗口大小扫描 —— 我原来的判断是错的**

> 触发原因：独立验证指出"② 的成员其证据小节的最小命中窗口是 `k=12`"（见 §7）。
> 我据此把 `k` 扫了一遍 —— **因为 `SECTION_MODE=off` 时窗口就是融合池 `[:k]`，所以只要一次 `debug()` 就能离线扫出所有 `k`**，零额外 API 调用。

**多跳（5 题）**

| `k` | 文件级 | **小节级** | 块级 |
|---|---|---|---|
| 4 | 3/5 | 2/5 | 1/5 |
| **8** | 4/5 | 2/5 | 1/5 |
| **12** | **5/5** | **3/5** | **3/5** |
| **15** | 5/5 | **4/5** | 3/5 |
| 20 | 5/5 | 4/5 | 3/5 |

**单跳（12 题）**：`k=4` 11/7/5 → `k=8` 12/9/7 → `k=12` 12/11/9 → `k=15` 12/12/11（文件/小节/块）

**⇒ 两条纠正**

1. **"扩大窗口这条线到头了"是错的**：`k=4→8` 这一步确实没让多跳小节级动（2/5 → 2/5，E-02/E-05 看到的就是这一步），
   但 **`k=8→12` 让多跳小节级 2/5 → 3/5、块级 1/5 → 3/5，`k=15` 小节级再到 4/5**。
   **原因很简单：需要的小节常常排在融合池的第 9~15 名**（逐 keypoint 的最小命中排名见 §7）——
   而 E-02/E-05 只比较了 4 和 8，**正好卡在拐点之前**。
2. **所以"继续调 `top_k`"应当重新列为候选**（而不是像我在 §6 原稿里那样排除掉）。
   ⚠️ 但**必须带上一个已知副作用**：窗口从 4 扩到 12/15，prompt 会变长，而 `SPEC.md §5` 的 `max_chars=3000` 是硬预算 ——
   **E-03 已经演过一次"材料变多但被截断，覆盖率反而没用"**（`expand` 模式 14/20 题丢资料）。所以下一步若要试 `k=12`，
   **必须同时记录 prompt 长度与截断题数**，否则会重蹈 E-03 的覆辙。

### 5. 方法学限制（**必须与结论一起引用**）

- **证据是靠词面子串匹配定位的**，不是人工标注：通用 keypoint（`.vue` 命中 52 个小节 / **141 个块**）会**放宽**小节级判定；
  这个偏差**对 H2 不利**，所以 H2 成立这个结论不依赖它 —— 但也**不能**据此说"小节级其实很容易满足"（§1）
  - **同一个偏差也放宽「块级」判定**（证据块集合同样可能巨大），所以**"块级 1/5"同样是保守下界**；两个方向都对 H2 不利
- **`q16` 的 `.vue` 这类 keypoint 几乎无区分度**：它的"证据小节"覆盖大半语料，对它的命中判定**信息量很低**。
  ⇒ 逐题表里的 `q16` 应当**打折看**（这也是它"③ 1 个"那条要谨慎的原因）
- **判分口径与本题无关**：本卡测的是**检索侧覆盖率**，没有跑任何生成，所以**不能用它推断任何 `accuracy`**；
  表中"该题最终得分"一列引用的是 E-04 的 `off` 栏（**另一次运行**），只作对照、不是同一次测量
- **n=5（多跳）**：keypoint 级 20 个样本稍好，但仍是**单次检索**；`vector` 层跨进程会漂移（§5.12），
  本表只在本次进程内自洽 —— **§3.5 的最小命中排名尤其如此**（排名 9~15 是"这一次召回"的排名，跨进程会变）
- **不可答题（3 题）`keypoints` 为空**：它们在"全部 20 题"一栏里恒不命中，所以那一栏的**分母含 3 道永远不可能命中的题**；
  要看有意义的数字请用**多跳 / 单跳分栏**，或把 20 题口径记作"分母含 3 道不可答题"（E-05 的口径教训在这里同样适用）
- **"小节"= `(source, heading)`**：`heading` 来自切块时的 markdown 标题，**不同文件的同名小节会被区分**（有 `source` 在键里），
  但**同一份文件里层级不同、标题相同的节**会被合并 —— 语料里未发现这种情况，但这是定义上的边界
- **§3.5 的扫描是"离线推窗口"**（窗口 = 融合池 `[:k]`），这在 `SECTION_MODE=off` 且 `rerank=off` 时严格成立；
  一旦开了 rerank 或小节整理，窗口就不再等于前缀，该推法失效

### 6. 下一步（按证据强度排序，都未开卡）

1. **先验"治 ②：加大窗口"（复活的方向，0.3 元级）**：§3.5 说 `k=12/15` 能把多跳小节级拉到 3/5~4/5、块级到 3/5。
   **必须与 prompt 长度、截断题数一起报**（E-03 的教训），否则可能又是一次"覆盖率涨了、答案没动"
2. **治 ① 召回（3/20，最被忽视的一格）**：`q13`/`q17` 各有 keypoint 的**全部**证据块都不在融合池。
   这是四张卡以来**第一次把召回层量化出来**（此前一直被"文件级 5/5"这个口径掩盖着）
3. **治 ④ 生成（11/20 全在场）**：要点都在眼前还答不好 —— 例如**先把资料按小节分组再让模型逐节回答**、
   或**子问题拆解**（把多跳问句拆成两个单跳）。这是**不需要改检索**的一条路
4. **治 ③**：窗口内的块选择（相邻块扩展那一类）——**注意**这与 E-03 的 `expand` 不同：
   那次是"整节进 prompt 撞预算"，这次是"在窗口内选对块"
5. **仍然不建议**：调 rerank（E-04/E-05 已证对多跳净负）。**~~继续调 `top_k`~~ 已从"不建议"移回候选**（§3.5）

### 7. 独立验证与更正（2026-09-21）

独立验证者**自己按 §2.13 的定义重新实现了一遍测量**（只调 embedding、不调生成）：

| 项 | 独立复现 | 本文档 | 判定 |
|---|---|---|---|
| 三级覆盖率 **18 格**（多跳 / 单跳 / 全部 × 融合池 / k=4 / k=8 × 文件/小节/块） | 与本卡 **18/18 逐格一致** | 同 | ✅ |
| 证据可定位率 | **53 / 53** | 53/53 | ✅ |
| 极端 keypoint `.vue` | **52 个小节 / 141 个块** | 同 | ✅ |
| 判据先于结果 | `5c33943` 只含判据、**无任何实验结果**；5 条原文与现在**逐字一致**（sha1 相同） | 同 | ✅ |

**抓到的 7 处问题与处理**（全部已改，逐条留痕）

| id | 严重度 | 问题 | 处理 |
|---|---|---|---|
| **V1** | **high** | §4 把 ① 的**规则**写成"**任一**证据块不在融合池"，但表里的 3/4/2/11 **只有按"全部证据块都不在池"**才算得出来（按字面算是 6/1/2/11）⇒ **规则文本与数字不自洽** | 已把 ① 的定义改成与算法一致（并注明"池中至少一块即算 ②"）；**数字未变、改的是文字**，按判据 5 在此留痕 |
| **V2** | **high** | `README.md` 有一处旧文（"瓶颈在错误小节、**不在生成侧**"）与本卡 ④=11/20 直接冲突（本轮漏改） | 已改（见 `README.md` 的已知限制第 2 条） |
| **V3** | medium | "3 道题失分（`q13`/`q14`/`q17`）"把两个集合混着说：**小节级未过是 4 题**（含 `q16`），**因"整节没进窗口"失分的是 3 题** | 四处（E-06/PLAN×2/BOARD）统一改成两句写法 |
| **V4** | medium | ① 标题写"**三个**病因"而表里是**四个**；② ② 的成员是否该算排序层 | 标题已改"四个"；② 的归属用 §7 的补充测量回答（见下） |
| **V5** | low | "`q15` 四个要点全在场"措辞过强（在场只说明**至少一块**进窗口） | 已改为"证据块都至少有一块在窗口里" |
| **V6** | low | 只说明了通用 keypoint 放宽**小节级**，没说明同样放宽**块级** | 已补（故"块级 1/5"是**保守下界**） |
| **V7** | low | "① 召回层第一次被指出来"缺数据支撑（E-02 已提过扩召回方向） | 已弱化为"**第一次被量化**成 3/20" |

**验证线索触发的补充测量，直接推翻了我自己的一个结论（本卡最重要的更正）**

验证者指出：② 的成员其**证据小节的最小命中窗口是 `k=12`**。我据此把 `k` 扫了一遍（§3.5，0 元）：

- **`k=8 → 12`：多跳小节级 2/5 → 3/5、块级 1/5 → 3/5**；`k=15` 小节级到 4/5
- ⇒ **"扩大窗口这条线到头了"是错的**。E-02/E-05 只比较了 4 与 8，**正好卡在拐点之前**；
  需要的小节常常排在融合池第 **9~15** 名（逐 keypoint 最小命中排名：`q13` 主要API **15**、`q13`/`q14`/`q17` 的另几个 **12**、`q16` 运行时编译 **9**）
- ⇒ 我把 §6 第 4 条"不再建议继续调 `top_k`"**撤回**，把它移回候选第 1 位（并附上 E-03 的截断教训作为必须同时报的指标）
- ⚠️ 关于验证者对 ② 归属的怀疑（"那两个更接近 ①"）：**`k=12` 能命中恰恰证明那一节在融合池里、只是排在窗口之外** ⇒
  它们确实属于 **②（排序/截断）**，不是 ①。**这一点我用数据反驳了验证意见，并写在这里以便复核者判断谁对。**

> **两轮验证（E-05/E-06）合起来说明一件事**：核心数字能被独立复现，**但"规则与算法是否一致""结论是否越过数据"这两类问题，只有复核能发现** ——
> 而它们恰好出现在**结论句里**。本项目已有两次因此改掉自己的结论（E-05 §7 的归因错误、本节的"窗口到头了"）。

**第三轮验证（针对上面的更正与新结论）**

| id | 严重度 | 问题 | 处理 |
|---|---|---|---|
| **V8** | **high** | 面试稿（仓库外 `RAG项目_面试问答.md`）**漏改**一处旧结论"扩大窗口救不了它"，与它自己下方的 `k=12` 表格自相矛盾 | 已改，并把"最小命中排名到 `k=12`"这句**改准**（`q16` 块=**9**、`q14`/`q17` 的块=**12**、`q13` 主要API=**15**） |
| V9 | medium | 怀疑 ② 的**标签**（节没进窗口）与算法（块不在窗口）不一致 | **判为不成立，已用 §4 的逐 keypoint 谓词表反驳**：② 的四个成员 `P2` 全 `False`；被点名的两题 `P2=True,P3=False` 本来就归 ③。**证据表已写进 §4 供任何人复算** |
| V10 | low | 摘要里的排除项仍写「窗口大小」，与"`top_k` 回到第一候选"口径相反 | 已加限定语（README / BOARD / 面试稿同步） |

**第三轮验证也确认了一件重要的事**：`SECTION_MODE=off ∧ use_rerank=False` 时 **`search(top_k=k) == debug(q)["fused"][:k]` 严格成立**
（代码依据：`src/retrieve.py:290` 返回 `fused_hits[:top_k_final]`、`:298-303` 的 `_finalize` 在 `off` 时原样返回、`:282` 的 rerank 被 `RERANK_ENABLED=0` 跳过），
验证者还实测了 18 例全部相等 ⇒ **§3.5 的"离线推窗口"不是假象**，那个翻转站得住。

