# 协调板 · BOARD

> 进度可视化 + 决策留痕。
> 铁律：**状态只在这里更新**，不要在聊天记录里。否则换个会话就断了。

**最近更新**：2026-09-21 · **当前里程碑**：**T-026 + T-027 完成（M14）—— 第二语料域「软件工程实践」跑通**：19 份 / **16.9 万字** / 830 块；新域 20 题基线 **accuracy 0.90~0.95**、`wrong_rate` **0.00**、`refusal_accuracy` 1.00。⭐ **"多跳最弱"跨域复现**（两域皆然）⇒ 多跳难度更像**题型固有**。⚠️ **两域数字不可比**（换语料 ≠ 优化）。全部卡片闭环（**27 / 27**）
>
> ✅ **T-028（M15）全部完成 —— `SPEC.md` 默认值已改为 `TOP_K_FINAL=15` + `max_chars=6000`**：
> 这是**第一张真正改动了主指标的卡**（前七张只交结论），也是**第一张改动冻结契约默认值的卡**。
> **Step 1（0 元）**：给状态加到三态后，多跳 20 个 keypoint 里 **6 个属于"进了窗口却被截掉"**（只在 `k=15`）⇒ 判据放行，`k*=15`、`X=6000`。
> ⚠️ **我开卡前的预期（"会在 Step 1 以负结果终止"）被推翻并留痕** —— 错因是那条推理建立在**二值口径**上（"不在 prompt 里"同时兼容"没进窗口"与"进了窗口被截掉"）。见 `E-10 §3`。
> **Step 2（第一域 A/B）**：覆盖率 **0.5343 → 0.8676（+0.3333）**、`accuracy` **0.50 → 0.80**、`wrong_rate` 0.25→0.05；
> 代价 = prompt token **×3.26**、延迟 **+188.5 ms**。⭐ **格子 A 精确复现 E-08 的混合基线**（三项全同 + 逐题对上）⇒ 装置可信。
> **Step 3（第二域，判据"不得下降"）**：覆盖率 **0.9345 → 0.9748，未下降 ⇒ 通过**；但**桶分布一动不动**（0.95→0.95）
> ⇒ **天花板效应表现为"不伤也没用"**：已经答对的题，给再多上下文也不会更对。
> ⚠️ **人工判分（0 元）把 `+0.30` 修正为 `+0.20`**：9 道变化题里 **2 道是度量伪影**（`q07` 在否定句里提到 `defineEmits` 被算命中；
> `q11` 语义等价、只因没写字面串"编译宏命令"失分）⇒ 新增 **`PLAN §5.23`**：**纯字面覆盖在上下文变长时系统性高估**。
> **Step 4（用户批准）**：⚠️ **执行前查出卡面写漏了参数** —— 胜出配置是**一对**，只改 `max_chars` 会是**零效果**改动
> （`k=4` 时 0/20 题被截断）⇒ **同时改两个默认值**，并把 `SPEC.md:103` 等 4 处改动**登记为 `inScope` 扩展**。
> **按出厂默认值重跑（单次）**：`accuracy` **0.75**、覆盖率 **0.8480**、`hit_rate` 0.85、延迟 1593.95 ms；
> 分题型 单跳 **10/1/1**、多跳 **2/3/0**、不可答 3/0/0 ⇒ **多跳仍是最弱的一类**。回滚：`git revert` 即回到 4/3000。
> **⇒ 对外口径：`accuracy` `0.50 → 0.70`（+0.20，人工修正后的下界），代价是 token ×3.2。**
> **等用户批准 Step 4**：**`SPEC.md:260` 的 `max_chars` 默认值变更**（冻结契约，`§0.2` 红线）。




>
> 📅 **注意日期**：本会话横跨 **2026-09-18 → 09-21**。早期条目写的是 09-18（当时确实如此），
> 后续条目写 09-21。**同一批工作里混着这两个日期是正常的**，不是笔误。

## 1. 当前进度

| ID | 任务 | 负责 | 状态 | 分支 | 备注 |
|---|---|---|---|---|---|
| T-001 | 项目骨架（app/src/eval/tests）+ 离线单测 | — | 完成 | main（历史） | 23 项测试全绿；先于治理套件建立 |
| T-002 | 装入治理套件（AGENTS / TASK_BRIEF / PLAN / BOARD） | — | 完成 | main（历史） | 第 1 次提交；占位符已于 2026-09-18 补齐 |
| T-003 | git 仓库 + 远端（GitHub）打通 | — | 完成 | main | SSH 免密推送实测 4.8 秒 |
| **T-004** | 安装依赖并冻结环境 | captain | **完成** | `chore/T-004-冻结依赖版本` | 9 库全装；`requirements.txt` 已 pin 直接依赖；另做了 mock 最小烟雾（见交接记录） |
| **T-005** | 建 20 题题库文件（占位可直接跑） | work_t2 + captain 验收 | **完成** | `feat/T-005-建20题题库` | 20 行、配比 12/5/3、无 `\uXXXX`；另收紧 multi_hop 来源数以满足 `eval/README.md §3.3` |
| **T-006** | 补齐语料至 10 份 | work_t2 抓取 + verify_t1 独立验证 | **完成** | `chore/T-006-补齐语料` | 12 份语料（1 自有 + 11 官方中文文档）；11/11 重下比对 sha256 一致；来源已登记 |
| **T-007** | **mock 模式全链路验收** | captain 执行 + verify_t1 独立验证 | **完成** | `test/T-007-mock全链路验收` | ★**M1 闭环**：files=13 chunks=405 mocked=True；n=20；独立探针证明指标非硬编码；报告全 0 属占位数据（见交接第七段） |
| **T-008** | 按真实语料出 20 题正式题库 | work_t1 起草 + verify_t1 两轮独立复核 | **完成** | `feat/T-008-正式题库` | 20 题真题；复核关掉 F1/F2/F3 + D1；mock 下 `hit_rate=0.80`；连带暴露 T-013 |
| **T-009** | 真实模式 20 题评测 | captain 执行（key 运行时注入，不落盘） | **完成** | `feat/T-009-真实评测` | **真实数字（最终语料 12 份 / 400 块）**：accuracy **0.45** / partial 0.30 / wrong 0.25 / refusal **1.00** / hit_rate **0.80** / 1654.6ms；`multi_hop` 最弱（0.20）；已按最终语料刷新一次 |
| **T-010** | 文档与交接收口 | captain | **完成** | `docs/T-010-交接收口` | README 全部 9 条命令本轮实跑（含**首次**验证 uvicorn 起服务）；新增「实测结果 + 已知限制」两节；暴露并登记 T-014 |
| **T-011** | 补齐 TASK_BRIEF 并修治理矛盾 | captain | **完成** | `docs/T-011-补齐任务书与治理缺口` | 任务书补齐 8 节；`$K` 修掉 push-task 相对路径坑；BOARD 定为 outOfScope 唯一例外 |
| **T-012** | 排除 README 被当作语料入库 | work_t1 + captain 边界探针 | **完成** | `fix/T-012-排除README入库` | files 13→12、chunks 405→400；补 2 条单测（23→25）；边界探针确认没误伤 `readme-v2.md` 等 |
| **T-013** | 让离线单测对环境变量隔离 | work_t1 + captain 5 组环境验收 | **完成** | `fix/T-013-单测环境隔离` | 干净 / `MOCK=1` / `+CHUNK_SIZE` / `+EMBED_DIM` / `+COLLECTION` 五种环境全绿；键名由 `Settings` 字段自动推导 |
| **T-014** | `/chat` 的 `latency_ms` 只覆盖生成段 | work_t1 + captain 双模式验收 | **完成** | `fix/T-014-chat延迟口径` | 计时挪到请求入口并弃用 `answer.latency_ms`；mock **0 → 452/456ms**（比值 0.97/0.99）、真实 **1251 → 1700ms**（比值 **1.00**）；冷启动计入并写明取舍 |
| **T-015** | 缓存检索索引（每次查询重建 BM25 占检索耗时 99%） | work_t1 + captain 同进程 A/B | **完成** | `perf/T-015-缓存检索索引` | 单次检索 **401.6 → 10.7 ms（-97.3%）**；真实端到端 **1654.6 → 1316.3 ms（-20.4%）**；同进程 A/B 证明结果**逐字中性（差异 0 处）**；连带发现 vector 层不可跨进程复现（§5.12） |
| **T-016** | multi-hop 检索侧改进的对照实验 | captain（受控实验） | **完成**（结论已被 **E-02** 推翻） | `exp/T-016-多跳检索对照` | ⚠️ 原判「检索侧无效、瓶颈在生成侧」**是错的**：桶指标把「覆盖率 0.450→0.550、5 升 0 降」压成了"一分未动"。真实瓶颈是**小节级检索精度**（T-018） |
| **T-017** | 修正治理脚本路径（`$K` 已失效） | captain | **完成** | `chore/T-017-修正治理脚本路径` | 工作区整理把 `agent-project-kit/` 移到根目录，`§0.2` 的 `$K` 成死路径（T-016 提交时实际撞到 `MODULE_NOT_FOUND`）；已更新路径并补「找不到时怎么定位」 |
| **T-018** | 修正 E-01 的错误结论，记录「指标粒度」教训 | captain（纯分析，0 元） | **完成** | `docs/T-018-修正E01结论` | 用**连续 keypoint 覆盖率**重读同一批数据：**上升 5 题 / 下降 0 题**（多跳均值 0.450→0.550，**p≈0.031**）；确立「主指标改用连续覆盖率」 |
| **T-019** | 把「连续 keypoint 覆盖率」接进评测报告 | work_t1（L1）+ captain 验收 | **完成** | `feat/T-019-连续覆盖率入报告` | `run_once` 新增记录键 `keypoint_coverage`、`aggregate` 新增 `avg_keypoint_coverage`（**只对非拒答记录**求平均；缺键/None/非数值既不进分子也不进分母）、`SUMMARY_FIELDS` 追加该字段；单测 **25 → 31**；captain 另做 4 条独立边界探针全 PASS；SPEC 清单的**向后兼容扩展**登记在 `PLAN.md §5.13` |
| **T-020** | 小节级整理三模式对照（`off`/`diverse`/`expand`） | work_t1（L1）+ captain 三模式实测 | **完成（假设被证伪）** | `feat/T-020-小节级上下文` | 实测：`diverse` **更差**（覆盖率 0.5343→0.5147，accuracy 0.50→0.45）；`expand` 覆盖率 +0.010 但在噪声内、且 **14/20 题有资料被 3000 字符预算丢掉**（`q05` 只剩 346 字符、`q04` 1.00→0.00）。→ **默认保持 `off`**，代码作为可复现实验装置保留。详见 `EXPERIMENTS.md` **E-03** |

| **T-021** | 启用并真实验证 rerank（契约核对 → 连通性 → 对齐 → A/B → 降级验证） | captain（Step 0/1/2/3 实测）+ work_t1（3 条离线单测） | **完成（正面结果，但多跳零效果）** | `feat/T-021-rerank真实验证` | **结论：有效，但默认值不改（仍 `0`）**。同进程 A/B：主指标 **0.5490 → 0.6373（+0.088）**、`wrong_rate` **0.20 → 0.10**；⚠️ **按题型拆开：单跳 +0.125、多跳 ±0.000（桶内 1/3/1 完全没变）** —— **收益全在单跳，多跳没解决**。延迟 **1136 → 3188 ms（+2.05 s）**。**Step 1 走 A 路径：`src/retrieve.py` 零改动**（老端点 + `gte-rerank-v2` 实测 200）。降级路径**实测通过**（错 key → 401 → 告警 → 结果与融合结果逐字一致，`/chat` 仍 200）。单测 **49 → 52**。详见 `EXPERIMENTS.md` **E-04**、`PLAN.md §5.15` |

| **T-022** | `top_k_final` × rerank 联合实验（先 0 元机制诊断，再决定是否花 0.9 元） | captain（Step 1 诊断） | **完成（H1 被证伪）** | `exp/T-022-topk-rerank联合` | **0 元机制诊断就否掉了假设，Step 2 未执行，省下 0.9 元**。多跳「两篇依据都进窗口」：`(k=4,off)` 3/5 · `(k=8,off)` **4/5** · `(k=4,on)` 2/5 · **`(k=8,on)` 3/5** ← H1 预测应 ≥4/5。**`q15` 最干净：8 席不开 rerank 时三篇全进，一开就掉一篇** ⇒ **挤依据与席位无关**。副产品：rerank **从不靠增加覆盖生效**（k=4 前后覆盖都是 1.000 却提升了答案）。`src/**` 零改动。详见 `EXPERIMENTS.md` **E-05**、`PLAN.md §5.16` |

| **T-023** | 小节级覆盖的量化（0 元；把"文件在窗口里"拆到"小节 / 块"两级） | captain | **完成（H2 成立；并自行推翻一条判断）** | `exp/T-023-小节级覆盖` | **多跳三级暴降**：文件级 **4/5** → 小节级 **2/5** → 块级 **1/5**（`k=8,off`）⇒「**有文件、没那节**」，E-02 的定性结论首次被量化。⚠️ **结项后自推翻**："扩大窗口到头了"**错** —— k 扫全：多跳小节级 `k=12` **3/5**、`k=15` **4/5**，块级 `k=12` **3/5**（需要的小节常排第 9~15 名）⇒ 当时把 `top_k` 放回第一候选 🔴**但这条"放回候选"已被 T-024/E-07 否掉：窗口里的东西到不了模型**。**副产品**：多跳 20 个 keypoint 拆成 **①池子没有 3 · ②进池子没进窗口 4 · ③小节在块不在 2 · ④全在场 11**；`q15` 全在场且答对、`q16` 3/4 在场只得 0.5 ⇒ **E-01 的第一版结论被部分平反**。**0 元**；`src/**` 零改动；**独立验证 7 条 finding 全改**。详见 `EXPERIMENTS.md` **E-06**、`PLAN.md §5.17` |

| **T-024** | `k=12/15` 的 LLM 侧验证（先 0 元测 prompt 预算，再决定是否花约 0.6 元） | captain | **完成（H3 被证伪；Step 2 未执行）** | `exp/T-024-加大窗口` | **0 元就否掉了假设，省下 0.6 元**。`k=4/8/12/15` 的 prompt 中位 = **1623.5 / 2800 / 2800 / 2800**，丢资料题数 = **0 / 15 / 20 / 20**；**prompt 里实际保留的资料中位 = 4 / 7 / 7 / 7 条**；**多跳「全部要点证据都进 prompt」= 1/5（四个 k 完全相同）**。⇒ **`k≥8` 之后送给模型的 prompt 逐字相同**，"加大窗口"对模型零影响。**同时推翻 E-06 §3.5 的结论部分**（数据仍成立）。**新突破口：`max_chars`（`SPEC.md` 冻结值 3000，改需用户批准）**。`src/**` 零改动；约 0.01 元。详见 `EXPERIMENTS.md` **E-07**、`PLAN.md §5.18` |

| **T-025** | 仅向量 / 仅 BM25 / 混合（RRF）三路同进程对照 | captain | **完成（H4 被证伪）** | `exp/T-025-三路对照` | **混合反而最差**：`avg_keypoint_coverage` 仅向量 **0.6275** · 仅 BM25 0.5294 · **混合 0.5343**；`accuracy` **0.60** / 0.45 / 0.50；逐题（混合−向量）**升 2 降 4 平 14**，主指标差 **低 0.05~0.09**（独立验证复现区间）。**分题型揭示机制**：单跳 **仅向量 0.7222** 最强、多跳 **仅 BM25 0.5000** 最强 ⇒ **两路互补是真的，等权 RRF 不是好的组合方式**。**检索侧四口径方向会翻转**（`hit_at_k` 17/16/16 同向 · "全部 gold **文件**进窗口" 13/14/14 **反向** · 证据**小节**级 11/8/9 同向 · 证据**块**级 7/6/6 同向）⇒ **正确的机制说法是「混合让"需要的证据"更难进窗口」**（初版"文件更全、块质量更差"**方向说反了**，已改）。**简历必须改写**。**不改默认值**（单次运行、n=20）。`src/**` 零改动；约 0.9 元。详见 `EXPERIMENTS.md` **E-08**、`PLAN.md §5.19` |

| **T-026** | 第二语料域的语料准备与落地（软件工程实践） | captain | **完成** | `chore/T-026-第二语料域` | **19 份 / 中文 169,083 字 / 入库 `files=19 chunks=830`**；Word COM 正规转换；**又剔除 1 份主题不符的**（`07` 是思政课报告）；第一域未被动（400 条 / 12 来源、`chunks.jsonl` 仍 09-18）；`src/**` 与 `SPEC.md` 零改动；**改了一处 outOfScope 的 `.gitignore`**（否则含学号姓名的语料会进公开仓库，已显式登记）。约 0.1 元 |
| **T-027** | 新域 20 题题库 + 真实基线评测 | captain + work_t1 起草/修订 + verify_t1 复核 | **完成** | `feat/T-027-新域题库` | **题库先于评测提交**（`4a6733f`，无任何分数）；独立复核 **9 条 finding** 修订（1 high：`se06` 同文档 RTO 矛盾会**假失败**）。**基线：accuracy 0.90 / 0.95（两次）、`wrong_rate` 0.00、`refusal_accuracy` 1.00、`hit_rate` 0.85、覆盖率 0.8913 / 0.9109**。**分题型：单跳 12/0/0（0.9667）· 多跳 4/1/0（0.7771）· 不可答 3/0/0**。⭐ **多跳最弱跨域复现**；⚠️ **两域数字不可比**。实花 **0.6 元**（估 0.3）。详见 `EXPERIMENTS.md` **E-09**、`PLAN.md §5.21` |

| **T-028** | `max_chars` × `top_k_final` 联合调参（**先 0 元判定"截断是不是真瓶颈"，再决定是否花钱**） | captain | **完成（默认值已改）** | `exp/T-028-max_chars` | **第一张真正改动主指标 + 第一张改动冻结契约默认值的卡**。**Step 1（0 元）**：三态口径 ⇒ 多跳 **a=6，只在 `k=15`** ⇒ `k*=15`、`X=6000`。⚠️ 开卡前预期被推翻并留痕。**Step 2（第一域 A/B）**：覆盖率 **0.5343 → 0.8676**、`accuracy` **0.50 → 0.80**、`wrong` 0.25→0.05、延迟 +188.5 ms、token **×3.26**；⭐ **格子 A 精确复现 E-08 基线**⇒装置可信。**Step 3（第二域，"不得下降"）**：覆盖率 **0.9345 → 0.9748 通过**，但**桶分布一动不动** ⇒ **不伤也没用**。⚠️ **人工判分把 +0.30 修正为 +0.20**：9 道变化题里 **2 道伪影** ⇒ 新增 **`PLAN §5.23`**。**Step 4（用户批准）**：⚠️ **卡面写漏参数** —— 胜出配置是**一对**，只改 `max_chars` 零效果 ⇒ **同时改 `TOP_K_FINAL` 4→15 与 `max_chars` 3000→6000**，4 处改动登记为 `inScope` 扩展；测试断言跟契约 4→15（**不是放宽校验**）。**重跑（出厂默认值，单次）**：`accuracy` **0.75**、覆盖率 **0.8480**、hit 0.85、延迟 1593.95 ms；单跳 **10/1/1**、多跳 **2/3/0**。**对外口径 0.50 → 0.70（+0.20，下界），代价 token ×3.2**。详见 `PLAN.md §2.17`、`§5.22`~`§5.24`、`EXPERIMENTS.md` **E-10** |


> 任务卡正文（含逐条验收命令与通过标准）见 `docs/PLAN.md`。
> **T-001~T-003 是治理脚手架阶段**，先于本计划建立，编号保留不回退。

## 2. 阻塞区

<!-- 卡住就写这里，禁止沉默硬扛 -->

| 日期 | 任务 | 阻塞原因 | 已尝试 | 需要什么 |
|---|---|---|---|---|
|  |  |  |  |  |

> **已解除**：2026-09-18 的 T-006 语料阻塞 —— 用户显式授权后由 Agent 抓取公开官方文档补齐（见决策日志）。

## 3. 决策日志

<!-- 记录「为什么这么定」，避免下次重来时重复讨论 -->

| 日期 | 决策 | 备选方案 | 选择理由 |
|---|---|---|---|
| 2026-09-18 | 把 `SPEC.md` 列为冲突优先级最高（高于本治理书） | 保持「AGENTS > 任务书 > 计划书」 | 本仓库已存在 `SPEC.md` 作为需求契约，且它明确写了"唯一权威、不得自行改名/改签名/改路径"。设成最高优先级才能避免两套契约打架 |
| 2026-09-18 | 提交前全绿命令固定为 `compileall` + 离线单测两条 | 只跑单测 | `SPEC.md §6` 把两条都列为必需；且沙箱 `PATH` 为空，命令必须写绝对路径 |
| 2026-09-18 | 远端用 SSH 而非 HTTPS | HTTPS + 凭据助手 | 本机 git 2.26 的凭据助手对 GitHub 无凭据，HTTPS 推送会挂住（实测超时 120 秒）；SSH 密钥一次配好、之后全部免密 |
| 2026-09-18 | 用 `includeIf "gitdir:**/作业/Agent/**"` 自动切身份 | 每个仓库单独 `git config user.email` | 要同时保留 Gitee 身份（`genshingod114`）。实测 `hasconfig:remote.*.url` 需 git 2.36+，本机 2.26 **静默忽略**，不可用 |
| 2026-09-18 | T-004「冻结环境」= 只 pin 9 个**直接依赖**到实测版本 | ① `pip freeze` 全量锁成 `requirements.lock.txt` ② 不落盘、只在 BOARD 记版本 | 全量锁会连 `onnxruntime`/`kubernetes` 等间接大件一起钉死，升级受阻且 diff 不可读；不落盘则 clone 后装到什么版本全看运气，违背 §5「lock 文件必须一并提交」 |
| 2026-09-18 | 允许 captain 本次更新 `docs/BOARD.md`，尽管 PLAN T-004 把 `docs/**` 列为 outOfScope | ① 保持 outOfScope，由用户手改 BOARD ② 先开治理修复卡改 PLAN 的 outOfScope | DoD（§8）强制要求「BOARD 状态已更新」，与 T-004 的 outOfScope 直接冲突；本次由用户显式授权破例，**矛盾本身待 T-011 一并修掉**，不作为常态 |
| 2026-09-18 | `TASK_BRIEF.md` 用「按 SPEC + PLAN 反推补齐」而非删除它 | ① 删除并把 `AGENTS.md §1` 的必读项去掉 ② 保持空模板 | 删除等于承认治理链第一环不适用；而 `SPEC.md` 只回答「做成什么样」，回答不了「为什么做、给谁做、范围外不做什么」。补一版能让后续 PR / 交接有背景可依，且与既有契约同源、不会打架 |
| 2026-09-18 | `docs/BOARD.md` 定为所有卡 outOfScope 的**唯一例外** | 每个任务单独授权破例 | 逐任务破例等于每次都要人介入，规则形同虚设；写成通则后 `AGENTS.md §8` 的 DoD 才可能被自动满足 |
| 2026-09-18 | T-005 的占位题写成「一眼可辨的占位文本」，不套用现有那 1 份语料编半真题 | 用 `00_保洁系统_项目说明.md` 编 20 道半真题 | `PLAN.md` 把 T-005 定位为「结构占位」、T-008 才出正式题；半真题会让人误以为是正式题，且与 T-008 重复劳动。占位文本保证 T-007 只验证链路，不产生可被误读的分数 |
| 2026-09-18 | T-006 由 Agent 抓取公开官方文档补齐（用户显式授权） | ① 等用户自己下载 ② 只保留自有文档 | 原卡面定为「人工卡」，但真正的阻塞点只是「谁去下载」。官方 OSS 文档可公开获取、且正是项目自身技术栈（Vue/FastAPI/MyBatis-Plus），不产生真实性风险；第三方文档被 `.gitignore` 挡住、不进公开仓库，版权可控 |
| 2026-09-18 | 语料只选**中文**章节，且不拖整站 | ① 中英双语都抓 ② 抓整站 | `DOC_SOURCES.md §3.1` 要求只抓需要的章节以免稀释检索精度；评测题是中文，混入英文语料会让命中率无法解释。三个主题都有官方中文来源 |
| 2026-09-18 | `00_保洁系统_项目说明.md` 内容与文件名不符，但**不改名** | 改名为 `00_保洁系统_简历配图说明.md` | `SPEC.md §7` 以该文件名登记它，而 SPEC 是最高优先级契约、不得修改；改名会让契约与仓库不一致。已在 `DOC_SOURCES.md §5.2` 登记为已知限制 |
| 2026-09-18 | mock 报告的 `accuracy/hit_rate/refusal_accuracy` **全 0 只证明链路可跑，不可作品质结论** | ① 当作「答得准/不准」的结论 ② 不记录、等 T-008 再说 | 独立验证已定位根因：占位题的 `gold_sources` 指向不存在的 `占位文档-待T-008替换.md`、`gold` 是占位文本、MockGenerator 仅当检索为空才拒答 —— 三个 0 都是**占位题库的结构性结果**。同一份代码在自造的 5 题（`gold_sources` 真实存在）上给出 `hit_rate=0.8`，证明指标函数本身正常。不写清楚，后续会话会把 0 误读成缺陷 |
| 2026-09-18 | `inScope` 为空的验收卡若必须改被跟踪文件，需在 BOARD **显式归属**「队长文书」还是「越权改动」 | ① 一律禁止任何改动 ② 不特别说明 | T-007 的 `inScope` 为空，但 `AGENTS.md §8` 的 DoD 要求更新 BOARD、且 T-007 暴露的缺陷要登记进 PLAN，二者都必须改被跟踪文件。显式归属让后续复核一眼能区分，不必反推（T007-F3 的 requiredFix） |
| 2026-09-18 | T-008 在「题目定稿后不得再修改」（`eval/README.md §3.5`）**之前**就修了题 | ① 带错不改、只把 findings 记进 BOARD ② 停下来等用户裁决 | §3.5 的理由是「改题等于换评测集、历史得分不可比」。本套题**从未产生过正式成绩**（只有占位期的 mock 报告），修改不破坏任何可比性；而带着「gold 里有一句被语料证伪的话」进入 T-009，等于拿错的标准答案去测系统。**从现在起该条对这套题生效** |
| 2026-09-18 | 任何角色**禁止**在真仓库执行 `git clean`（尤其 `-fdX`）；清 `__pycache__` 用 `Remove-Item -Recurse -Filter __pycache__` | ① 只在验收卡里口头提醒 ② 允许 `git clean -X`（"只清忽略文件"听着更安全） | "只清忽略文件"恰恰最危险：本项目**刻意**把语料与索引放进 `.gitignore`（第三方版权文档不入公开仓库），`-X` 一次就全删。本次已实际发生（11 份语料 + 索引 + 报告被删），靠 `DOC_SOURCES.md` 登记逐份重下才恢复 |
| 2026-09-18 | T-009 的 API key **不写入 `.env`**，改由运行时环境变量注入 | ① 写进 `rag-doc-qa\.env`（已 gitignore，方便复用） ② 让用户重新申请 key | 用户明确不愿把凭据落到第二个文件。代价：**跨 pwsh 调用不保留环境变量**，所以"读 key → 入库 → 评测"必须在同一进程里顺序做完（本次由一个一次性脚本读 `~/.dsh/.credentials.yaml` 后注入，用完即删） |
| 2026-09-18 | 真实数字按「单次运行」口径记录，并把两次运行的结果都留下 | ① 只记好看的那次 ② 取两次平均 | `PLAN.md` T-009 红线：不许只挑好指标、不许四舍五入到好看。两次的差异（accuracy 0.45 vs 0.50）是**真实的方法学事实**——模型输出措辞影响 `correct`/`partial` 边界；藏起来等于给未来埋雷。`eval/report.json` 保留第一次的原始结果，逐题明细另存 `eval/report_detail.json`（已 gitignore） |
| 2026-09-18 | README 写入「实测结果 + 已知限制」两节 | ① 保持 README 纯使用说明，数字只放 BOARD ② 只写好指标 | 这是要投出去的仓库：面试官打开先看 README，若那里只有命令、没有数字，"有真实评测"这件事就传不出去；同时把「数字有波动 / `multi_hop` 是短板 / `/chat` 延迟口径有缺陷」**一起**写进去，避免数字被脱离语境引用 |
| 2026-09-18 | T-014（`/chat` 延迟口径）**只登记、不在 T-010 里顺手改** | ① 反正只有几行，顺手改掉 ② 不登记，当作小节不记 | T-010 的 `inScope` 只有 `README.md` 与 `docs/**`，改 `app/main.py` 属越界（`AGENTS.md §7.7`）；且"顺手改"会让同一次提交混入两种性质的改动，破坏"一个任务一个提交"的可追溯性 |
| 2026-09-18 | T-014 的**冷启动计入**接口延迟：不预热、也不从计时里剔除 | ① 启动时预热（首次也报稳态值） ② 把冷启动从计时中剔除 | 字段叫 `latency_ms`，就该是「这次请求真正花了多久」：首次请求确实付了建索引 / 加载 BM25 的钱（mock 3891ms、真实 10516ms），把它抹掉就是名不符实。代价是首次偏大、第 2 次起为稳态；客户端可用「第一次 vs 后续」读出击穿状态。若将来要求首次也报稳态值，那是"启动预热"另一张卡的事 |
| 2026-09-18 | T-014 顺带更新了 `README.md` 的「已知限制」一行（**超出 T-014 的 inScope**，显式记录） | ① 严格只改 `app/main.py`，让 README 留着"T-014 待修" ② 另开一张文档卡去改 | 那一行是 T-010 写的、已被本次修复**证伪**的陈述；留着它就会自相矛盾（`AGENTS.md §8` 的 DoD 要求状态与实际一致，README 是面试官第一眼看的文件）。改动仅一行、不涉及任何代码，故按"同一任务同一提交"合并进 T-014，并在此显式归属以便复核区分 |
| 2026-09-18 | T-015 的验收标准第 1 条**被修正**（跨进程黄金基线 diff → 同进程 A/B） | ① 保留原标准、让这张卡"永久 failed" ② 直接把差异解释成"不重要"就放行 | 原标准**天生不可达**：实测**未改动的原版代码**同样对不上黄金基线（10 / 6 处差异），差异全在 chroma 的 `vector` 层、`bm25` 层始终稳定。同进程 A/B 让两条路径共享同一个 chroma 状态，差异只可能来自缓存本身 —— **在相关维度上更严格**，而非放宽。修正理由已写进 `PLAN.md` 该卡下的「验收标准的修正」小节 |
| 2026-09-18 | 派生发现：**向量检索层不可跨进程复现**，只登记限制、不在 T-015 里修 | ① 顺手改 `src/store.py` 的 HNSW 参数 ② 不记录 | 它超出 T-015 的 `inScope`（只准改 `src/retrieve.py`）；且它影响的是**评测可复现性**这一更大的话题，值得单独成卡而不是塞进性能卡。已写进 `PLAN.md §5.12`：`hit_rate` 可以报 80%，但被追问"逐次召回是否一致"时要答"不是" |
| 2026-09-18 | T-016 以**负结果**收尾：**不跑 V3**、**不改 `TOP_K_FINAL` 默认值** | ① 继续试父子分块 / 按来源限流，凑一个"有提升"的结果 ② 看到 `accuracy 0.50→0.55` 就顺手把默认值改成 8 | ① V3 的动机是"召回不够"，而诊断显示**召回本来就够**（融合池 5/5 全覆盖），继续试只是烧钱加噪声。② `0.50→0.55` 只是 **1 题**的差别，落在已记录的波动区间内（§5.5）；`hit_rate 0.80→0.85` 是 `hit_at_k` **对 k 单调**导致的机械上升，不是质量证据。**没有证据就不改默认值** —— 何况改 `SPEC.md §5` 的默认值需要另开卡 |
| 2026-09-18 | **E-01 的结论被推翻并公开更正**（新增 E-02）；今后对照实验的**主指标改用逐题连续 keypoint 覆盖率** | ① 既然结论已被推翻，就把 E-01 那一节删掉/重写 ② 承认桶指标不够但先不改流程 | ① **原文必须保留**：删掉就看不出"同一个实验在两种粒度下得出相反结论"，而那正是本项目最有价值的方法学教训。② 同一批数据在**文件级**（`hit_at_k`：任一命中 + 对 k 单调）、**桶级**（阈值 0.8）、**连续值**三种粒度下给出三个互相矛盾的答案 —— **粒度决定你能看见什么**。不改流程，下次还会用粗指标否定一个真实的改进 |
| 2026-09-18 | T-018 是**纯分析、0 元**，没有重跑任何评测 | ① 为了让"更正"看起来更硬，重跑一次 V1/V2 ② 直接改成新结论不解释 | `eval/report*.json` 两份原始逐题记录都在本地，重读它们是**充分且必需**的；重跑只会引入 LLM 波动、反而让"5 升 0 降"这个干净信号变浑。**能靠已有数据回答的问题，不要花钱再跑一遍** |
| 2026-09-18 | T-016 的**主指标改用**「`gold_sources` 是否**全部**进 top-k」，而不是 `hit_rate` | ① 沿用现成的 `hit_rate` ② 只看 accuracy | 现有 `hit_at_k` 是「**任一** gold_source 命中即算命中」，导致多跳题报的 `hit 5/5` 只说明**找到了两篇里的至少一篇** —— 正是这个盲区让"多跳题检索没问题"这个判断此前无法被验证。换成集合级指标后立刻定位到：融合池 5/5 全覆盖、top-4 只有 3/5，而扩到 top-8 后多跳题仍无变化 |

| 2026-09-21 | T-019 对 `SPEC.md §5` 的汇总字段清单做**向后兼容扩展**（新增 `avg_keypoint_coverage`），而**不改 `SPEC.md`** | ① 直接改 `SPEC.md` 把新字段写进契约 ② 不新增字段，继续靠临时脚本回读逐题明细 | ① `PLAN.md §0.2` 明令**不得修改 `SPEC.md`**（它是最高优先级契约），越界即 reject。② E-02 证明桶指标会把「0.450 → 0.550、5 升 0 降」压成"一分未动"，**仪器不够细**就必须先修仪器，否则 T-020 做完照样测不出效果。新增字段**只追加、不改名、不删、不改语义**，既有消费方不受影响，属可接受的兼容扩展；差异已按「实现扩展 + 登记」处理，写进 SPEC 需用户批准解除 §0.2 限制 |
| 2026-09-21 | T-020 的 `SECTION_MODE` **默认值定为 `off`**，`diverse` / `expand` 两条路径**实现、测试、但默认不生效** | ① 选 `expand`（覆盖率数值最高 0.5441）② 三条路径全部不保留，回滚代码 | ① `expand` 的 +0.010 落在噪声内（升 2 / 降 1），代价却是 **14/20 题有资料被 3000 字符预算丢掉**、`q04` 从 1.00 掉到 0.00 —— **用"平均值略高"换"十几道题上下文被掏空"明显不划算**，且违背 `SPEC.md §5` 的 `max_chars` 预算。② 回滚会丢掉一个**可复现的实验装置**：E-03 的结论（内容总量 > 排列方式）正是靠它三条路径对照才得出的，将来换语料 / 换 `top_k` 时要能一键重跑。**按证据不改默认值** —— 这正是 T-016 立下的规矩，不能因为这次是自己写的代码就放宽 |
| 2026-09-21 | 把 `SECTION_MODE` 的三模式对照记为**负结果**并写进 `EXPERIMENTS.md` E-03，而不是压下来不提 | ① 只写"已实现三模式" ② 挑 `expand` 好看的那个数写进 README | 假设（"检索到错误小节 → 按小节整理能救"）**是我自己提的**，实测被证伪；不写就等于让下一个会话重犯同一个假设。负结果同时**排除了一整类方案**，把矛头收敛到"排序"（rerank）——**知道此路不通，和知道有路可走一样值钱**。另：`diverse` 反而更差（0.5343→0.5147）这个反直觉数据是"内容总量比排列方式重要"的直接证据，比任何正面结果更有解释力 |
| 2026-09-21 | 开 **T-021**（启用并真实验证 rerank），且**把官方契约核对（Step 0）排在任何真实调用之前** | ① 直接 `RERANK_ENABLED=1` 跑 A/B ② 只做连通性测试、不做降级验证 | ① 核对官方文档（2026-09-04 更新）发现：字段名**与我们实现一致**，但**官方已公告 `gte-rerank` 下线、推荐 `qwen3-rerank`**（另一套扁平请求/响应形状），且文档端点已改为**业务空间域名**。若跳过核对直接跑 A/B，最可能的结果是**每次调用抛异常 → 被降级静默吞掉 → 报告显示"rerank 无提升"**，而真实原因是**契约变了** —— 那会得出一个**归因错误**的结论，比没有结论更糟。② **降级路径必须实测**：rerank 是可选增强，它挂了绝不能让问答跟着挂；而"异常被 `except` 吞掉"这条路径**恰好会让失败看起来像"没效果"**，所以它本身是这张卡的头号风险，不是附赠项 |
| 2026-09-21 | T-021 明确写死 **`RERANK_ENABLED` 默认值保持 `0`**，即使 A/B 出正面结果也不在本卡改 | ① 若有效就顺手把默认值改成 1 ② 卡里不写这条，等结果出来再说 | ① 改默认值等于**改变所有用户的默认行为**，且 `SPEC.md` 把 rerank 列为**二期可选**；这类改动应当由**证据 + 用户批准**驱动，而不是由"我刚好跑出了好数字"驱动。② 先写死红线，才能防止**结果导向地放宽标准** —— T-016 已经立过这条规矩（`accuracy 0.50→0.55` 时拒绝改默认值），这次是同一类诱惑、更早地把它堵住 |
| 2026-09-21 | T-021 的 A/B 用**同进程**做（同一个 `Retriever` 实例 + `use_rerank` 参数切换），而不是跑两个 `python -m eval.run_eval` 进程 | ① 跑两个进程（更贴近"真实开箱即用"，更省事） ② 跑同一进程但用两个 `Retriever` 实例 | ① `§5.12` 已证 **chroma 向量层不可跨进程复现**：两个进程的 A/B 会把「rerank 的效果」和「召回漂移」**混在一起**，升了也说不清是谁的功劳、降了也不知道该怪谁。② 同进程内两个实例仍会各自发起向量查询，不如"同一实例 + 参数切换"干净 —— 后者**共享同一份 BM25 缓存 / chunk_map / chroma 句柄**，差异只可能来自 rerank。代价如实记录在 E-04 §7：这个基线**不能直接和历史 `report.json` 比** |
| 2026-09-21 | T-021 拿到**正面结果**后仍然**不改默认值**，并把"默认值仍为 0"作为验收项之一 | ① 有效就顺手改默认 ② 把结论写成"建议启用"然后就不管了 | ① 开卡时已把这条写成红线（上一条），**兑现红线才是重点** —— 如果结果一好看就改，那前面写死红线只是表演。② 收益（+0.088 覆盖率）与成本（**+2.05 s/题，延迟 1.8 倍**）是**不同量纲的取舍**，该由使用方按场景决定：离线批量评测要质量、在线问答要延迟。③ 把建议写进 `§5.15` 与 README，让"要不要开"变成一个**有数据可依的决策**，而不是由我替所有人拍板 |
| 2026-09-21 | 派生教训（work_t1 自己发现并纠正）：**变异反证必须先证明"真的改到了"** | ① 改坏了源码、发现测试没红 → 宣布"用例无效" ② 只看退出码 | 它第一次用 PowerShell 做多行替换来篡改 `src/retrieve.py`（该文件是 **CRLF**，而替换串是 LF），**锚点根本没命中、文件未被改动**，于是退出码 0 被误读成"用例无效"的结论。它自己用 `contains-original-guard` / `git diff -- src/retrieve.py` 发现并改成字节级替换（写入前 assert 锚点命中）。**这正是"要求可执行的证据、而不是要求结论"的价值**：反证本身也需要证据，否则会得到一个方向相反的假象 |
| 2026-09-21 | 交付前自查抓到：**rerank 的收益全部来自单跳题，多跳题零效果** → 立即把"证据链收口"的措辞全部改为"只收到一半" | ① 只报总体 +0.088（看起来是成功） ② 把这条留到下一个实验再说 | ① 本卡（以及 E-01~E-03）的**动机是多跳**。只报总体数字等于**用一个成功的外壳掩盖没达成的目标** —— 而 `q16`(+0.25) 与 `q14`(−0.25) 精确抵消这件事，恰恰说明 rerank 在多跳上只是**在两道题之间搬运**，不是在改进。② 这条不是"细节"，它决定了**下一步该测什么**：既然扩大窗口对多跳有效、而重排对多跳无效，就该测"**先扩大窗口再多给几条**"，让 rerank 只排序、不替多跳题做取舍。**错误的口径会把下一步引到错的方向** |
| 2026-09-21 | 把这轮"总体 vs 分题型"记为**「粒度决定你能看见什么」的第三次重演** | ① 当成一次性发现、不上升为方法 | 第一次是文件级/桶级/连续值（E-02），第二次是"截断"的两种口径（E-03），这次是**总体 vs 分题型**：同一批数据，总体看成功、分题型看失败。**三次都是同一个错误模式** —— 用一个方便的聚合口径回答一个它没能力回答的问题。记成方法（"任何对照实验都要按题型/分桶拆开报一遍"）才能下次不再犯 |
| 2026-09-21 | T-022 **先花 0 元做机制诊断、再决定要不要花 0.9 元**，结果省下了这 0.9 元 | ① 直接跑 LLM 侧三格评测（0.9 元，更省事） ② 只跑一两个格子省钱但结论不成立 | ① 这个问题问的是**机制**（"窗口里有没有那篇依据"），而机制**不需要 LLM 就能看**：只要检索窗口。**先花 0 元把机制问清楚，是对"要不要花钱"最直接的判断依据** —— 结果 H1 的预测方向直接失败（`(k=8,on)` 3/5 < `(k=8,off)` 4/5），钱就不必花。② 三格必须**同进程**（`§5.12`），只跑一两格无法归因。这是 T-016/T-021 的 Step 0 套路**第三次**奏效 |
| 2026-09-21 | T-022 的决策判据**与卡面字面阈值不一致**，我把差异写进 `EXPERIMENTS.md` **E-05 §4** 而不是悄悄按自己的理解走 | ① 严格按字面（"仍只有 1 篇"）判 → 不触发，继续花钱跑 Step 2 ② 按自己的理解判，不留痕 | 卡面字面阈值（"仍只有 1 篇依据"）是一个**比 H1 更粗的代理**；实测 3/5 不满足字面条件，但 **H1 的预测方向（`(k=8,on) ≥ (k=8,off)`）已经失败**，而且 `q15` 给出"席位足够仍被挤掉"的直接反例。我按 H1 的预测方向判定 —— **方向更严**（提前判失败，而非延后）。① 会白花 0.9 元；② **不留痕就是"事后改标准"**，那是 `AGENTS.md §7` 明确禁止的。**判据可以调整，但必须写下来让复核者能不同意** |
| 2026-09-21 | 独立验证的 7 条 finding**全部处理**；第二轮又抓到 1 处**归因错误**（V8）与 3 处口径不一致（V9~V11），也全部处理 | ① 只改那条真错（口径分母），其余记下 ② 全部无视，因为核心结论已被独立复现 | ① 真错是 T022-V1（"全部依据进窗口 14/20"复现不出来：分子来自 17 道有依据题、分母却写 20）——**它出现在结论旁边，读者会先看它**，必须改；但没有只改它：V3（比较键未声明）、V4（漏记 40 次 rerank 计费调用）、V5（§2.12 另一条判据没留痕）是**同类病**（口径不写清 = 隐藏变量），一起改才不留下一批同类雷。② 核心结论被复现**不是**放过描述错误的理由。**第二轮 V8 最值得记：那不是数字错，是"归因错"** —— 我把"文件级 13/20 vs chunk 级 20/20"的差异归因成"有些题的 4 个槽来自同一份文档"，实际构成是 **3 题换序 + 7 题文件内换序**。**数字错能被复算打脸，归因错不能，只能靠独立复核** —— 两轮验证合计改了 **8 处** |
| 2026-09-21 | **流程教训（T022-V7）**：T-022 的"开卡（含预注册判据）"与"结项（结论）"落在**同一次未提交改动**里，git 层面无法证明判据先于结果 | ① 就这样，靠文内自述"我判据写在前面" ② 为了留证据去手工拆分已有的 diff | ① "判据先于结果"是这套治理的核心价值（防止事后挑标准）；**没有版本证据的预注册，等于没有预注册**。② 手工拆分已写好的 diff 风险高于收益（且本轮已用 E-05 §4 显式留痕替代）。**采取的做法：把这条记为流程限制，并从下一张卡起改为"开卡一个提交、结项一个提交"** —— 让 git 自己提供时序证据 |
| 2026-09-21 | **兑现上一条**：T-023 的判据 5 条**单独提交**（`5c33943`），实验与结论在**其后**的提交里 | ① 还是合成一次提交 ② 只把判据贴进结论文档 | ① 上一条已经承诺，**承诺了就要在下一张卡立刻兑现**，否则那条决策日志只是自我表扬。② 判据写在结论旁边，读者无法判断它是先写的还是后补的 —— **判据必须在"还不知道结果"的状态下被固化下来**（`git show 5c33943` 里没有任何实验结果，这一点本身就是证据）。这是本项目**第一次**让"预注册"具备可核验性 |
| 2026-09-21 | T-023 第三轮验证的 3 条 finding：**1 条成立（面试稿漏改）· 1 条判为不成立（用数据反驳）· 1 条成立（摘要限定语）** | ① 验证说什么就改什么 ② 全部无视，因为核心数字已被复现两轮 | ① **验证也会错**：它怀疑"② 的标签（节没进窗口）与算法（块不在窗口）不一致"，我把三个谓词（块在池 / 节在窗 / 块在窗）逐 keypoint 打出来，**② 的四个成员 `P2` 全是 `False`**、被它点名的两题本来就归 ③ ⇒ **判据与标签自洽，该意见不成立**；证据表已写进 `E-06 §4` 供任何人复算。② 但**不能因此放过真问题**：那条 high 是**面试稿（仓库外文件）漏改**，与我刚修的四处属同一批改动 —— **最容易漏的永远是"不在仓库里、没有 diff 提示"的那个文件**。故：**对验证意见逐条自己复核，既不照单全收，也不因为"数字对"就放过陈述错** |
| 2026-09-21 | T-025 发现**混合召回比仅向量差 0.05~0.09**（本项目第一次否掉自己的核心设计），**但明确不据此改默认值** | ① 既然仅向量最好，顺手把混合去掉 ② 把这次结果说成"混合略差、差距在噪声内" | ① **单次运行、n=20**：`accuracy` 差 0.10 只相当于 2 题；而"混合更差"的方向有覆盖率与逐题 **降 4 升 2** 支撑，**要改 `SPEC.md` 的设计必须先多轮复跑 + 用户批准**（T-016/T-021 已两次立此规矩）。② 独立验证复现出 **−0.054~−0.093** 的区间，说成"噪声内"就是**用措辞抹掉一个真实发现**。**做法：如实报反方向结果 + 明确写"不改默认值、需更多轮次"，两件事都做** |
| 2026-09-21 | T-026 落地第二语料域时**改了一处 outOfScope 文件 `.gitignore`**，并**显式登记**而不是当作"顺手" | ① 不改，让 `data/raw_se/` 进公开仓库 ② 改了但不在 BOARD 记 ③ 先开一张新卡再回来 | ① 第二域语料含**学号、同学与导师姓名**（隐私）+ 课程/案例材料（第三方版权），**进公开仓库不可接受**；且原规则 `data/raw/*` 确实不覆盖 `data/raw_se/`。② 不登记就等于**以"必须"为名扩大自己的权限**，复核者无法区分"必要越界"与"顺手改" —— T-004/T-014 已两次立此先例：**越界可以，但必须留痕**。③ 为一条 ignore 规则开卡不划算，且它属同一任务的必要组成（DoD 要求 `git status` 不出现 `data/**`）。**做法：改 + 在 `PLAN §5.20` 与决策日志登记原因** |
| 2026-09-21 | 第二域语料**转换完成后又剔除 1 份**（`07_软件工程课题研究报告.md`）—— 文件名叫"软件工程1班"但内容是**思政课**报告 | ① 按预注册清单照收（卡面确实列了它） ② 只删文件不说明 | ① 卡面清单是**按文件名与字数**列的，而"软件工程 1 班"是**班级名**不是主题名 —— 照收会把"实事求是思想路线"的思政报告混进"软件工程实践"语料，**污染主题一致性**（T-012 的语料卫生教训：不该进的进去了会稀释检索）。② 只删不说，下一个会话会以为清单 20 份都收了、对不上账。**做法：剔除 + 在 `DOC_SOURCES_SE.md §三` 登记原因 + `PLAN §5.20` 记一笔**。**教训：语料清单要按"内容"复核，不能只信文件名** |
| 2026-09-21 | T-027 的产物独立验证查出 **`se12` 零余量**（要点 `工具层` 不在自身 `gold` 里 ⇒ 覆盖率恰好 0.80），**而我在同一轮已给 `se03` 修过同类问题** | ① 不管它（0.80 仍算 correct） ② 只记在遗留问题里 | ① **0.80 就是阈值本身**：模型少答一个要点就掉到 partial，等于这题**没有容错**；而且**同一个规则不能只对一道题执行** —— 我给 `se03` 修了却漏了 `se12`，那就是**选择性执行**。② 回到原文核实后，该要点**在语料里有据**（PDF 原句"…都可分为核心层、辅助层、工具层"），只是漏写进 `gold` ⇒ **补进 gold** 而不是删要点（删掉会丢掉这个"文档特有"的区分力）。修后 0.80 → **1.00**，全 17 道可答题覆盖率均 1.000。**这条是逐题复算才抓到的，自查没发现** |
| 2026-09-21 | 独立验证指出 **T-027 第 1 次运行"没有落盘产物"**（`report_se.json` 被第 2 次覆盖），我**如实标注证据边界**而不是照常引用 | ① 照旧把两个数并列引用 ② 干脆不提第 1 次 | ① 第 1 次那一列**目前只有终端输出可佐证、无法独立复算** —— 混在一起引用会让读者以为两个数同等可信。② 不提会**隐瞒运行间波动**（0.90 vs 0.95 本身就是"n=20 下 1 题之差"的证据）。**做法：保留两列 + 明确写"以第 2 次为准"（它有逐题明细且已被逐条复算）**，并把"给 `run_eval` 加 `--detail-out`"列为下一步候选 —— **根因是"跑完只留一个汇总文件"** |
| 2026-09-21 | T-027 的题库**先于评测单独提交**（`4a6733f`），再跑评测 | ① 题库与结果一次提交 ② 直接跑、不提交题库 | ① 一样的理由：**没有版本证据的"先写题后跑分"，等于事后挑题**。题库提交那次**没有任何分数**，评测结果的 commit 才能指向一个确定的题库版本 —— 这是 T-022 流程教训（V7）的**第二次兑现**。② 不提交就改题库，评测数字对不上任何可追溯的题集，复核者无法判断"题是不是照着答案改的" |
| 2026-09-21 | T-027 实花 **0.6 元**（估 0.3），**超支 0.3 元**，如实登记而不是略过 | ① 只报汇总、不做分题型（省 0.3 元） ② 补跑但不说超支 | ① 卡面验收**明确要求分题型**（`single_hop`/`multi_hop`/`unanswerable`），而**分题型正是本卡最有价值的产出之一**（它给出了"多跳最弱跨域复现"）—— 省这 0.3 元会丢掉这个发现。② **报预算就要报实际**，否则"预估"这个词就失去意义。**根因**：`eval/run_eval.py` 只写汇总、没有 `--detail-out`，所以只能补跑；**已把"给 run_eval 加明细输出"登记为下一步候选** |
| 2026-09-21 | T-025 的结论直接**改写了简历文案**：从"混合召回提升准确率"改成"实测混合反而更低、两路各有擅场、问题在融合方式" | ① 保留原简历句式（反正没人会去验） ② 直接删掉混合检索这件事不写 | ① 准备清单**坑 8** 就是为这件事写的：*"只做向量检索就喊'混合检索'…这是面试一追就崩的那类表述"*；现在**数据方向相反**，硬写等于自己埋雷。② 删掉也不对 —— **两路召回 + RRF 是真实实现**，而且"我测出它更差、并定位到是融合方式的问题"是**比一句提升更强**的材料。**做法：保留实现事实 + 改成有数据的表述** |
| 2026-09-21 | T-024 用 0 元证明"加大窗口对模型零影响"，**并把 E-06 §3.5 的结论也推翻**（数据成立、结论不成立） | ① 照 E-06 §3.5 的建议花 0.6 元跑 `k=12/15` ② 只报"预算不够"，不查"模型到底看到了什么" | ① E-06 §3.5 只看了**窗口**这一级，而模型只认 **prompt** 这一级：`k=8/12/15` 的 prompt **逐字相同**（都被 `max_chars=3000` 截在 7 条资料）⇒ 那次实验会得到"两次完全相同的输入"，结论只反映 LLM 抖动。② 只说"预算不够"会让人以为"稍微抬一点预算就行"；把 **池 → 窗口 → prompt** 三级都量出来才看清：**多跳「要点证据都进 prompt」在四个 `k` 上恒为 1/5** —— 瓶颈明确落在 prompt 这一级，而不是窗口。**教训：每加一级口径，都要重新问一次"这一级的东西真的传下去了吗"** |
| 2026-09-21 | 把"`max_chars`"登记为**唯一剩下的突破口**，但**不擅自改** —— 明确标注"须先取得用户批准"（`SPEC.md` 属冻结契约） | ① 直接改了跑对照（反正只是个数） ② 连登记都不做，等用户想起来 | ① `PLAN.md §0.2` 明令不得擅改 `SPEC.md`；而 `max_chars` 是写进契约的默认值，**改它等于改所有使用方的默认行为** —— 这类改动必须由证据 + 用户批准驱动（T-016/T-021 已两次立此规矩）。② 不登记，用户就不知道"剩下的路只有这一条"，会继续在窗口/重排上打转。**做法：把证据摆出来 + 明确写"需要批准"+ 给出配对调的依据（`k=4` 有余量、`k=8` 起丢资料），让决策变便宜** |
| 2026-09-21 | T-023 的结论**改变了下一步的优先级**：从"继续在排序层找"改为"**`top_k` 12/15 先验** + 召回层 3/20 + 生成层 11/20" | ① 接着只调 rerank ② 宣布"找到多跳原因了" ③ 把窗口这条线判死 | ① rerank 已被 E-04/E-05 证对多跳净负，继续调是**在已被排除的方向上重复投入**。② 真相是**四个病因的分布**（召回 3 · 排序 4 · 块选择 2 · 生成 11），**不是一个原因**。③ **我一度把窗口判死（"到头了"），随后自己用 `k` 扫描推翻了** —— 需要的小节常排在第 9~15 名，而 E-02/E-05 只比较了 4 与 8，**正好卡在拐点之前**。**教训：只扫两个点就宣布"某条线到头了"，等于用两个样本画一条曲线** |
| 2026-09-21 | 收尾核对时发现交接记录里**累计花费算错**（写 4.4，实为 **3.5**，T-025 的 0.9 元被加了两次），**改数并留更正痕迹** | ① 直接把 4.4 改成 3.5，不留痕 ② 不管它（差 0.9 元没人在意） | ① 只改数不留痕，复核者无法区分"这个数以前是错的"还是"一直是对的"，也就无从判断**其余历史账目是否可信**；而本项目已立过规矩：**推翻结论要留原文**（`E-01` 与推翻它的 `E-02` 并存）。② 花费是**我报给用户的数字**，虚高 0.9 元 ≈ 报高 **26%**；且这个错误模式（**同一笔被重复累加**）说明"手工逐段累加"本身不可靠 ⇒ 值得记一笔。**做法：改数 + 在同一行写明原值与错因 + 列出逐段核算链**；**真正的解法是让脚本累计**（已列为下一步候选，与"给 `run_eval` 加 `--detail-out`"同一类：**别让人手去记机器能算的东西**） |
| 2026-09-21 | 开 **T-028** 时把「改默认值」写成卡面**交付物**（而不是像前六张那样只交结论），**同时明确负结果也算合格交付** | ① 再开一张纯诊断卡（稳、不会失败） ② 开一张"必须涨分"的卡（有交付压力） | ① T-022 / T-023 / T-024 **连续三张都停在诊断**：项目的诊断能力已被反复证明，缺的是**一次真实变更** —— 再开诊断卡，等于把"善于复盘"变成"逃避改动"。② 但**不能反过来预设正结果**：我先做的是**开卡前的 0 元核实**，而它**立刻动摇了默认前提** —— `max_chars` 只对"**已在窗口内、却被截断**"的 keypoint 有效，而 `q13` 与 `q16`「运行时编译」的缺失证据**在四个 `k` 下都根本不在窗口里** ⇒ 一张"必须涨分"的卡会逼我在 Step 1 之后**要么撒谎、要么硬推**。**做法：交付物写成"改默认值"或"可复算的负结果"二选一，并把 Step 1 的终止判据（该类 keypoint ≤1 个即停）先写死** |
| 2026-09-21 | **收尾核账时发现 `PLAN.md §6` 变更记录里漏了 T-026/T-027 两行**（它们改了计划、新增了 M14，却没登记），**补记并标明是补记** | ① 只补 T-028 那一行、不管缺口 ② 悄悄补上、不说是补的 | ① `§6` 是**计划变更的唯一台账**，缺一行就意味着"某次计划变更没留原因" —— 而 T-026/T-027 恰恰是**换语料域**这种大变更，最需要原因可查。② 不标明补记，读者会以为它当时就登记过，**也会顺带以为"这份台账是全的"** —— 而它其实漏过一次。**做法：补两行 + 行内明写"此行是补记"**；并与本次同时发现的**累计花费重复累加**合并成一条教训：**台账类文档必须定期对账，不能只靠"写的时候仔细"** |
| 2026-09-21 | T-028 Step 1 的实测**推翻了我开卡前的判断**（我预期它就地终止，实测是放行），**把错因、而不只是结论写进 `E-10 §3`** | ① 悄悄按 Step 1 的结果往下走，不提自己猜错 ② 只写"实测 a=6"，不写"我原以为 ≤1" | ① 本项目已两次把"自己错了"当成最有价值的产出（`E-01`/`E-02` 并存、T-023 的自推翻），这次更该写 —— 因为**这次不是判断失误，是推理无效**：我用 E-07 的"四个 `k` 恒为 `0/1`"推出"**证据不在窗口里**"，而"不在 prompt 里"**同时兼容"没进窗口"与"进了窗口被截掉"两种成因** ⇒ **那份数据根本推不出我的结论**。② 只报结果会掩盖一个可复用的教训：**粒度过粗时不同成因会塌缩成同一个观测，于是数据会"伪装成"支持一个错误结论** —— 这正是本项目反复踩的"粒度决定你能看见什么"（**第七次**），而**这次栽在"拿旧口径去下一个新结论"上**。**做法：结论与错因一起写，并明确标注"开卡前的预期被推翻"** |
| 2026-09-21 | T-028 Step 2 **H5 成立**（覆盖率 **+0.3333**、`accuracy` **0.50 → 0.80**），但我把"**装置可信度**"排在这条结果之前先验证：**格子 A 是否精确复现 E-08 的混合基线** | ① 直接报"B 比 A 好 +0.30" ② 顺手把 A 的数字当成"新基线"引用 | ① 同进程 A/B 只能排除**跨进程漂移**，**排除不了"装置本身变了"** —— 若 A 与历史基线对不上，B 的涨幅里就混着"换了装置"的成分，**+0.30 这个数就不能归因给参数**。② 必须先回答"这次跑的是不是同一个东西"，再谈差异。**做法：实测 A 与 E-08 的"混合"列三项全同（覆盖率 0.5343 / `accuracy` 0.50 / `hit_rate` 0.80）且逐题对上，把这条复现写进 `E-10 §5` 并标为"本实验最硬的一条证据"** |
| 2026-09-21 | Step 2 的 **token 实测**显示本项目历史"每题 0.015 元"的估算**偏高约 3 倍**，如实登记而不是沿用旧口径 | ① 继续按旧单价报花费（反正没人核账单） ② 只报本卡花费、不提口径问题 | ① 花费是**我报给用户的数字**；单价偏高 3 倍意味着**历史累计金额可能整体虚高** —— 这与今天刚修的"重复累加"是**同一类问题的两种形态**（一个是把同一笔加两次，一个是单价取错）。② 只报本卡，会**让两个互相矛盾的口径同时留在文档里**，下个会话必然二选一乱用。**做法：本卡只报 token 实测 + 区间估算（约 0.1~0.35 元），并明确写"历史估算口径偏高、不宜当精确值引用"** |
| 2026-09-21 | T-028 Step 3（第二域）**通过**（覆盖率未下降），但我在同一节里**主动写"不伤也没用"**，并额外做了一项**卡面没要求的诚实性检查** | ① 只报"Step 3 通过、改动安全" ② 不查 `X=6000` 在第二域够不够 | ① "通过"这个词有歧义：判据本是"不得下降"，而实测**桶分布一动不动**（0.95→0.95）、`+0.0403` 全来自"部分对的题答得更完整" ⇒ **说成"有效"是夸大；只说"通过"读者仍会以为第二域也涨了**。② `X=6000` 是**按第一域**的未截断长度选的，而第二域块长更短 —— **不查就会把"该格子零截断"从第一域外推到第二域**（实测第二域 **18/20 仍截断**、未截断最大 6619）。**做法：把"不伤也没用"写进 `E-10 §7`，把外推风险写进 `§5.22` 与 `§2.17`** |
| 2026-09-21 | T-028 的**人工逐题判分**把自动口径的 `+0.30` 修正为 **`+0.20`** —— 9 道变化题里查出 **2 道是度量伪影** | ① 直接报 0.80（自动口径，最省事也最好看） ② 发现伪影后干脆作废、宣布"改动无效" | ① **0.80 是这套口径下的真实读数，但它不是"答得更对"的比例**：`q07` 的 B 格答案**明说"资料未说明（如 `defineEmits`）"**，那句里的 `defineEmits` 被算成命中；`q11` 的 A/B **语义等价**，A 只因没写出字面串"编译宏命令"而失分。**不做判分，我就会把 0.80 报出去。**② 作废也不对 —— 去掉 2 道伪影后**仍有 +0.20**，且 `q03`（A 整题拒答 → B 答出）、`q05`（**B 更短**却更对）这类改善**不可能由长度解释**。**做法：报 +0.20 并同时给出"7 真 / 2 假"的判分明细**；根因登记为 **`PLAN §5.23`** —— **纯字面覆盖在上下文变长时系统性高估**，且**偏差方向与本项目所有"加上下文"的改动一致** ⇒ 以后凡靠覆盖率下结论，都先回答"会不会只是词变多了" |
| 2026-09-21 | T-028 Step 4 **执行前**查出**卡面写漏了一个参数**，我**没有照着卡面改**，而是先补正再动手 | ① 照着卡面只改 `max_chars`（卡面就写了这一个） ② 直接一起改但不说明 | ① **只改 `max_chars` 会是一个完全无效的改动**：`k=4` 时本来 **0/20** 题被截断（Step 1 实测），预算放到多大输入都**逐字不变** —— 那会把"改了默认值"这个动作做完，**却不产生任何效果**，而且**看起来像交付了**。这正是本卡开卡时想避免的那类"只有动作没有结果"。② 胜出配置是**一对** `(15, 6000)`，`SPEC.md:103` 的 `TOP_K_FINAL` 与 `:260` 的 `max_chars` **都在冻结契约里**，所以这次 `inScope` 必须扩张到 `SPEC.md:103`/`src/config.py:54`/`.env.example:36`/`tests/test_offline.py:334`。**做法：先查契约面 → 发现漏项 → 显式登记 `inScope` 扩展（T-004/T-014/T-026 先例：越界可以，但必须留痕）→ 两个参数成对改** |
| 2026-09-21 | 改默认值时**同步改了 `tests/test_offline.py:334` 的断言**（`top_k_final == 4` → `15`），并**在卡面与日志里写清这不是放宽校验** | ① 不改测试（那它会红，逼我想别的办法"绕过"） ② 改了但不说 | ① 这条断言**编码的是旧契约默认值**：用户已批准契约变更，**断言必须跟着契约走** —— 否则就只能去删/skip 测试，那才是红线（AGENTS §7.1）。② 但"改了一个测默认值的断言"**看起来就像为了让构建变绿而动测试**，所以必须主动说明**区别在哪**：**如果默认值再被改回 4，这条断言必须再次失败**（它就是为这个而存在）。**做法：改 + 加注释说明"值一变这里就必须失败" + 在 `§2.17` Step 4 表格里逐行列明** |

## 4. 交接记录

<!-- 会话结束 / 上下文将满时写，保证下一个会话无缝接续 -->

### 交接 · 第二十四段（2026-09-21，最近一次）

- **已完成**：**T-028 `max_chars` × `top_k_final` 联合调参（M15）** —— 全部卡片闭环（**28 / 28**），**零待办**
- ⭐ **这是本项目第一次真正改动主指标，也是第一次改动冻结契约的默认值**（前七张卡都只交结论）
- **默认值变更（`SPEC.md`，用户明确批准）**：`TOP_K_FINAL` **4 → 15**（`:103`）、`max_chars` **3000 → 6000**（`:260`）；
  同步 `src/config.py:54`、`src/generate.py:51`、`.env.example:36`、`tests/test_offline.py:334`（断言 4→15）
  - ⚠️ **卡面缺陷（已登记）**：卡面 Step 4 **只写了改 `max_chars`**，而胜出配置是**一对** ——
    **只改 `max_chars` 会是零效果改动**（`k=4` 时 0/20 题被截断，输入逐字不变）。
    执行前查契约面才发现 ⇒ 同时改两个，并把 4 处改动**显式登记为 `inScope` 扩展**（越界可以，但必须留痕）
- **四步结果**：
  1. **Step 1（0 元）**：给"在不在 prompt 里"这个**二值口径**加第三态后，多跳 20 个 keypoint 里
     **6 个属于"进了窗口却被截掉"**（且只在 `k=15`）⇒ 判据放行（≤1 就终止）
  2. **Step 2（第一域 A/B）**：覆盖率 **0.5343 → 0.8676**、`accuracy` **0.50 → 0.80**、`wrong_rate` 0.25→0.05；
     代价 token **×3.26**、延迟 **+188.5 ms**。⭐ **格子 A 精确复现 E-08 的混合基线**（三项 + 逐题全同）⇒ 装置可信
  3. **Step 3（第二域，判据"不得下降"）**：覆盖率 **0.9345 → 0.9748 通过**，但**桶分布一动不动** ⇒ **不伤，也没用**
  4. **Step 4**：按出厂默认值重跑 ⇒ `accuracy` **0.75**、覆盖率 **0.8480**、hit 0.85、延迟 1593.95 ms；
     单跳 **10/1/1**、多跳 **2/3/0**、不可答 **3/0/0** ⇒ **多跳仍是最弱的一类**（该规律未消失）
- ⚠️ **对外口径必须用修正值**：人工逐题判分（0 元）查出 9 道变化题里 **2 道是度量伪影**
  ⇒ **`accuracy` 0.50 → 0.70（+0.20）**、覆盖率 **0.5343 → 0.7794（+0.2451）**，**不是自动口径的 +0.30 / +0.3333**
- ⚠️ **我在本次犯过一次判断错误并留痕**：开卡前我写"`q13`/`q16` 的缺失证据根本不在窗口里"，据此预期 Step 1 会以负结果终止；
  **实测是 k 依赖的**（`k=4/8` 是 b 类，`k=12/15` 是 a 类）。错因：**用二值口径的数据去推一个它无法区分的结论**
- **新增已知限制**：`§5.23`（`keypoints_match` 纯字面覆盖会把"提到该词"算成"答出要点"，**上下文变长时系统性高估**）·
  `§5.24`（默认值进入 `(15, 6000)` 时代，README 旧数字全部失效）
- **未开卡（用户决定先记录）**：`keypoints_match` 的口径修复（否定句 / 同义匹配）—— 它是全部历史数字的共同底层，改它会让历史分数不可比
- **进行中**：无
- **下一步候选（非阻塞）**：
  1. **`eval/run_eval.py` 加 `--detail-out`** —— T-027/T-028 都得靠仓库外脚本落明细，属 `eval/**` 改动（已第三次登记）
  2. `§5.23` 判分口径修复卡（**须先评估历史可比性**）
  3. `max_chars` 的**跨域定值**：`6000` 是按第一域选的，第二域 18/20 仍截断（未截断最大 6619）⇒ 是否取 `7000` 需单独测
  4. 第一域遗留：融合方式（自适应权重 / 按题型路由）· 治召回 3/20 · 治生成 11/20 · 3203 BEL 字符
- **关键上下文**：
  - **累计真实 API 花费约 4.0 元**（T-028 约 0.5 元：Step 2 ≈0.1~0.35 + Step 3 ≈0.1~0.2 + 重跑 ≈0.15）
    ⚠️ 按 token 实测折算的历史口径**偏高约 3 倍**，累计金额是**估算、未核对账单**
  - **`main` = `origin/main` = `9cdc69d`**，工作区干净；`compileall` 0 + 离线单测 **52 / OK**
  - **两套语料域都在**（`data/raw` 400 块 / `data/raw_se` 830 块）；第二域语料**不入公开仓库**（含隐私）

### 交接 · 第二十三段（2026-09-21）

- **已完成**：**T-026 + T-027 第二语料域「软件工程实践」** —— 全部卡片闭环（**27 / 27**），**零待办**
- **起因**：用户否决了"用自有项目文档（保洁系统）"的原方案，让我在 `作业` 文件夹另找语料，并选定**新增第二语料域**（而非替换）以保住第一域的全部资产
- **语料（T-026）**：`data/raw_se/` **19 份 / 中文 169,083 字**（11 份本人作品 + 8 份课程/案例文档），入库 **`files=19 chunks=830`**
  - 转换用**本机 Word COM 16.0**（`.doc/.docx`）+ PDF 直接复制（走 `pypdf`）；**没用"读字节+正则"**那个在普查里已见乱码的做法
  - **转换后又剔除 1 份**：`07_软件工程课题研究报告.md` —— 文件名里的"软件工程1班"是**班级名**，内容是**思政课**报告（**先转换、按内容核对、后剔除**）
  - **第一域未被碰**：`data/raw/` 13 文件、`chunks.jsonl` 仍 **400 条 / 12 来源**、最后写入仍是 **09-18**
  - ⚠️ **改了一处 outOfScope 的 `.gitignore`**：原规则 `data/raw/*` **不覆盖 `data/raw_se/`** ⇒ 语料（含**学号、同学与导师姓名**）会进公开仓库。已加忽略规则并**显式登记**（T-004/T-014 的先例）
  - 台账：`docs/DOC_SOURCES_SE.md`（19 份逐条：原始路径 / 转换方式 / 中文字数 / sha256）
- **题库与基线（T-027）**：20 题（12/5/3，id `se01`…），**先于评测单独提交**（`4a6733f` —— 那次提交里没有任何模型输出或分数）
  - **独立复核判定 needs_revision，9 条 finding**，其中 1 条 **high**：`se06` 所引文件**自相矛盾**（可靠性章 RTO≤30 分钟 vs 容灾章 RTO≤4 小时），gold 单选会让答对另一节的模型**假失败** ⇒ 修订后两种答法**都判 correct**
  - 另两条必修：`se03` 要点 `"105万"` 在 gold 里无字面（覆盖率恒 0.80、零余量）⇒ 改为 `"1,050,000"`；
    `se19` 的不可答性被"代码部分=某某负责"**部分信息污染** ⇒ 去掉可答引子
  - **修订时还纠正了复核的两处错误建议**：`4学时` 其实只在 18 号文（"两篇都有"是 `normalize_answer` 去空白造成的**假阳性**）；
    复核建议的 `ERP系统选型/BOM/MPS` 在 04 号文里也有（**补了等于没补**）⇒ 改用真正单源的要点，**单用 04 的覆盖率从 1.00 降到 0.571**
- **基线结果（两次运行）**：

  | 指标 | 第 1 次 | 第 2 次 |
  |---|---|---|
  | `accuracy` | **0.90** | **0.95** |
  | `partial_rate` / `wrong_rate` | 0.10 / **0.00** | 0.05 / **0.00** |
  | `refusal_accuracy` | 1.00 | 1.00 |
  | `hit_rate` | 0.85 | 0.85 |
  | `avg_keypoint_coverage` | 0.8913 | **0.9109** |
  | `avg_latency_ms` | 1578.7 | 1644.1 |

  **分题型（第 2 次）**：单跳 **12/0/0**（cov 0.9667）· **多跳 4/1/0（cov 0.7771）** · 不可答 3/0/0；唯一失分 `se15`（cov 0.286）
- ⭐ **最有价值的发现是跨域对照**：**两个域都是"多跳最弱"**（第一域多跳 cov 0.40~0.55；第二域 0.7771，均低于同域单跳）⇒
  **多跳的难度更像"题型固有"，不是第一域（框架文档）语料特有的问题** —— 这是 E-01~E-08 一直缺的那块对照。
  且第二域多跳**检索 5/5 命中**仍有 1 题部分对 ⇒ 又一次"检索到了 ≠ 答得好"
- ⚠️ **两域数字绝对不可比**：语料风格（第二域是本人作业，含成串明确指标与表格，答案**可直接摘抄**）+ 题库构建方式（每题都过了"能贴出连续原文片段"这一关）都不同
  ⇒ **0.90 vs 0.50 主要说明"语料与题目的可答性"，不是"系统变好了"**；但它本身可讲：**同一套代码换语料，分数从 0.50 到 0.95**
- **成本**：T-026 ≈0.1 元 + T-027 **0.6 元** ⇒ **超支 0.3 元**。原因：`eval/run_eval.py` 只写汇总、没有 `--detail-out`，而卡面要求**分题型** ⇒ 用**仓库外**脚本复刻同一链路补跑一次（**未改仓库代码**）
- **进行中**：无
- **下一步候选（非阻塞）**：
  1. **给 `eval/run_eval.py` 加明细输出**（`--detail-out`）—— 让"分题型"成为默认能力，不必每次补跑（属 `eval/**` 代码改动，需开卡）
  2. **跨域对照实验**：同一套方法（三路检索 / rerank / 小节级）在第二域上重跑一遍，看结论是否跨域成立
  3. 第一域遗留：`max_chars` 变更卡 · 融合方式（自适应权重 / 按题型路由）· 治召回 3/20 · 治生成 11/20
  4. **语料卫生**：3203 个 BEL 控制字符（Word 表格行首）未处理
- **关键上下文**：
  - 累计真实 API 花费约 **3.5 元**（T-026 0.1 + T-027 0.6）
    - ⚠️ **更正**：此处原写 **4.4 元**，错因是**把 T-025 的 0.9 元加了两次**（误算成 `2.8 + 0.9 + 0.1 + 0.6`）。
      上一段（第二十二段）的 2.8 元**已含** T-025 的 0.9 元 ⇒ 正确累计 **2.8 + 0.1 + 0.6 = 3.5 元**。逐段核算见下表：
      `0.5（T-009≈0.3+刷新≈0.2）→ 0.7 → 0.8 → 1.1（T-020 0.3）→ 1.9（T-021 0.7）→ 1.9 → 1.9 → 1.9（T-022/T-023/T-024 各≈0.01）→ 2.8（T-025 0.9）→ 3.5（T-026 0.1 + T-027 0.6）`
  - **本域语料不入公开仓库**（含学号、同学与导师姓名）⇒ **clone 后无法复现本域数字**，这是与第一域在可复现性上的**结构性差异**
  - 题库里有 3 题（`se09`/`se10`/`se12`）可凭通用知识答对 ⇒ 对本域的**检索区分力低**（已知未改）

### 交接 · 第二十二段（2026-09-21）

- **已完成**：**T-025 三路检索对照** —— 全部卡片闭环（**25 / 25**），**零待办**。**H4 被证伪：混合召回反而比仅向量差 0.05~0.09**
- **这是本项目第一次否掉自己的核心设计**（`SPEC.md` 与 README 一直写着"向量 + BM25 混合召回"，现在有了**反方向**的实测数据）
- **判据先于结果有 git 证据**：判据 8 条见 `36543ec`；执行期间无调整；**`src/**` 零改动**（三格都走现成的 `_rankings()` → `_fused_hits()`）

  | 指标 | **仅向量** | 仅 BM25 | **混合（现状）** |
  |---|---|---|---|
  | `avg_keypoint_coverage` | **0.6275** | 0.5294 | **0.5343** |
  | `accuracy` | **0.60** | 0.45 | 0.50 |
  | `hit_rate` | **0.85** | 0.80 | 0.80 |

  逐题（混合 − 仅向量）：**升 2 / 降 4 / 平 14**，主指标差 **低 0.05~0.09**（独立验证复现区间）
- **分题型揭示机制（本卡最有价值的部分）**：

  | 题型 | **仅向量** | 仅 BM25 | 混合 |
  |---|---|---|---|
  | `single_hop`（12） | **0.7222** | 0.5417 | 0.5694 |
  | `multi_hop`（5） | 0.4000 | **0.5000** | 0.4500 |

  ⇒ **"两路互补"是真的，但"固定权重 RRF 等权融合"不是好的组合方式** —— 混合把弱的那一路也平均了进来
- **层级教训（第六次），而且我初版写错了口径**：检索侧**四个口径方向不同** ——
  `hit_at_k`（任一命中）**17/16/16** ✅同向 · **"全部 gold 文件进窗口" 13/14/14 ❌反向** · 证据**小节**级 **11/8/9** ✅同向 · 证据**块**级 **7/6/6** ✅同向。
  **只有"文件级全命中"这一个口径反向**（它对"进来的是不是需要的那一块"不敏感）。
  ⇒ **正确的机制说法是「混合让"需要的证据"更难进窗口」**；我初版写的"文件更全、块质量更差"**方向说反了**（已改，见 E-08 §3/§6）
- **独立验证判定 pass 且真跑了两格复现**：混合格**连跑 3 次逐位相同**（0.5343 / 0.50 / 0.80，逐题降 4 升 2 集合也相同）；
  仅向量格给出 **0.5882 与 0.6275**（差异只来自 `q07` 一题）⇒ 差值实测 **−0.054 ~ −0.093**。**方向稳、数值不稳**
- **不变式检查通过**：仅向量格的窗口顺序 == `_rankings()["vector"][:4]`，**0 违反**（证明那一格真的只用了向量）
- **⚠️ 简历必须改写**（判据第 7 条要求写明）：
  - ❌ 不能写"采用 BM25 + 向量混合召回**提升了准确率**"
  - ✅ 写"**实现两路召回 + RRF 融合；自建 20 题评测后实测混合反而低于仅向量（覆盖率 0.534 vs 0.627）；分题型看单跳向量强、多跳 BM25 强 ⇒ 固定权重融合不是最优组合**"
  - —— **敢报反方向结果，比一句没数据的好话强得多**；这也正好回答了准备清单**坑 8**
- **进行中**：无
- **下一步候选（非阻塞，按证据强度排序）**：
  1. **融合方式**（本卡直接推出）：两路**不同权重** · **按题型路由**（单跳偏向量、多跳偏 BM25）· RRF `k` 调参
  2. **"仅向量 + rerank"**：rerank 的 +0.088 是在**混合基线**上测的，换到仅向量基线是否仍成立**未知**
  3. **`max_chars` 变更卡**（E-07 指出它是唯一剩下的检索侧杠杆）—— **须用户批准改 `SPEC.md`**
  4. 治 ① 召回层（3/20）· 治 ④ 生成层（11/20）
- **关键上下文**：
  - 累计真实 API 花费约 **2.8 元**（T-025 约 0.9 元）
  - **仅向量的 `accuracy 0.60` 是本项目测到过的最好数字** —— 但**单次运行、n=20**，**不构成"把默认改成仅向量"的证据**（改契约要另开卡 + 多轮复跑 + 用户批准）
  - **过去所有"混合检索"基线的 accuracy 都在 0.45~0.50** —— 与本卡的混合格（0.50）一致，说明这次对照的混合格是可信的锚点

### 交接 · 第二十一段（2026-09-21）

- **已完成**：**T-024 `k=12/15` 的 LLM 侧验证** —— 全部卡片闭环（**24 / 24**），**零待办**。**H3 在花钱之前被证伪，省下约 0.6 元**
- **这是"0 元先探路"第三次直接省掉一笔钱**（T-016 负结果收尾、T-022 省 0.9 元、本次省 0.6 元）
- **Step 1（0 元）prompt 预算**：

  | `k` | prompt 中位 | 被 3000 预算丢资料 | prompt 里实际保留（中位） | **多跳「要点证据都进 prompt」** |
  |---|---|---|---|---|
  | **4** | **1623.5** | **0 / 20** | **4 条** | **1 / 5** |
  | 8 | **2800** | **15 / 20** | **7 条** | **1 / 5** |
  | 12 | 2800 | **20 / 20** | **7 条** | **1 / 5** |
  | 15 | 2800 | **20 / 20** | **7 条** | **1 / 5** |

- **结论不是"收益小"，而是"输入根本没变"**：`k=8/12/15` 送给模型的 prompt **逐字相同**（都被截在 7 条资料）。
  **加大窗口只是多取候选，`build_prompt` 随后整条丢掉** ⇒ 那 0.6 元花下去只会得到一次 LLM 抖动
- **预注册决策点生效**：`k=12`/`k=15` 丢资料都是 20/20 > 阈值 4/20 ⇒ **不进入 Step 2**。
  且实际触发信号（prompt 顶在 2800~2999 天花板）**比阈值强得多**，判定不依赖阈值选得准
- **⚠️ 又推翻了一条（这是两张卡内的第二次）**：E-06 §3.5 说"`top_k` 回到第一候选"—— **数据成立、结论不成立**。
  `k≥8` 之后 `top_k` 对模型**零影响** ⇒ **窗口不是可用的杠杆**
- **这解释了此前几处"看起来矛盾"的事**：
  - `k=4→8` 有效（E-02）而 `k=8→12` 无效？因为 **4→8 时 prompt 还在长**（1623.5→2800、4→7 条），**8 之后被截断**
  - rerank 只对单跳有效（E-04）？单跳需要的块常在前几条；多跳需要的"第二篇"常排在第 8 名之后 —— **排到哪儿都没用，模型看不见**
- **唯一剩下的突破口：`max_chars`**（`SPEC.md §5` 冻结值 3000）。多跳 5 题里 **4 题**的关键要点在第 4 条资料之后才出现，而 prompt 在 3000 字符处硬停。
  ⚠️ **改它要动 `SPEC.md`，必须先取得用户批准**；且**应与 `top_k` 同步调**（`k=4` 有余量、`k=8` 起丢资料）
- **代码**：`src/**` **零改动**；单测仍 **52 / OK**；**约 0.01 元**（只有 query embedding，无任何生成调用）
- **进行中**：无
- **下一步候选（非阻塞，按证据强度排序）**：
  1. **`max_chars` 变更卡**（唯一剩下的杠杆）—— **须先取得用户对"修改 `SPEC.md`"的批准**，再设计对照（建议：`max_chars` 与 `top_k` 配对调，如 3000/4 → 6000/8）
  2. **治 ① 召回层（3/20）** —— 与 prompt 预算**无关**的一格：`q13`/`q17` 有要点的证据块连融合池都没进，**这条路不受 `max_chars` 限制**
  3. **治 ④ 生成（11/20）** —— **按小节分组、逐节回答**或**子问题拆解**；**不需要改检索，也不受预算限制**
- **关键上下文**：
  - 累计真实 API 花费仍约 **1.9 元**（T-024 只花约 0.01 元）
  - **"粒度决定你能看见什么"已有五次**：文件级 → 小节级 → 块级 → 窗口级 → **prompt 级**；
    **前四级都在检索侧，而模型只认最后一级** —— 前四级涨、最后一级不动，等于没涨
  - **两张连续的自推翻（E-06 §3.5、E-07）说明一件事**：单个层级的指标涨了，**不等于下一个层级也会涨**；
    每加一级口径，都要重新问一次"这一级的东西真的传下去了吗"

### 交接 · 第二十段（2026-09-21）

- **已完成**：**T-023 小节级覆盖的量化** —— 全部卡片闭环（**23 / 23**），**零待办**。**H2 成立，0 元，且拆出了多跳的四种病因**
- **这是本项目第一次做到「判据先于结果」有 git 证据**：判据 5 条写在 `PLAN.md §2.13` 并**单独提交**（`5c33943`），
  结论是其后另一次提交 —— `git show 5c33943` 即可核验。**执行期间无任何判据调整**
- **H2**：把口径从「文件」降到「小节 / 块」后覆盖率会明显更低。**多跳（5 题，`k=8,off`）**：

  | 来源 | 文件级 | **小节级** | 块级 |
  |---|---|---|---|
  | 融合池 | **5/5** | 4/5 | 3/5 |
  | 窗口 `k=4,off` | 3/5 | 2/5 | 1/5 |
  | **窗口 `k=8,off`** | **4/5** | **2/5** | **1/5** |

  ⇒ **文件级 4/5、小节级只有 2/5 = 窗口里有那份文件、但没有那一节**（小节级未过 4 题：`q13`/`q14`/`q16`/`q17`；其中"整节没进窗口"3 题：`q13`/`q14`/`q17`，`q16` 属③）
- ⚠️ **我在结项后自行推翻了一条判断（验证线索触发，0 元补测）**：原写"扩大窗口这条线到头了"（只看了 `k=4→8`），**错**：

  | 多跳 | k=4 | k=8 | **k=12** | k=15 |
  |---|---|---|---|---|
  | 文件级 | 3/5 | 4/5 | **5/5** | 5/5 |
  | **小节级** | 2/5 | 2/5 | **3/5** | **4/5** |
  | 块级 | 1/5 | 1/5 | **3/5** | 3/5 |

  ⇒ **需要的小节常排在第 9~15 名**，E-02/E-05 只比较 4 与 8，**正好卡在拐点之前**。当时据此把 `top_k` 放回第一候选
  🔴 **该判定已被 T-024/E-07 否掉**：窗口里的东西**到不了模型**（`k=8/12/15` 送给模型的 prompt **逐字相同**，都被 `max_chars=3000` 截在 7 条资料）
- **最有价值的副产品（多跳 20 个 keypoint 按"丢在哪一级"归类）**：

  | 病因 | 个数 | 该怎么治 |
  |---|---|---|
  | ① 池子里就没有 | **3 / 20** | **召回/融合层** —— 提高召回 |
  | ② 进了池子但节没进窗口 | **4 / 20** | 排序/截断 |
  | ③ 小节在、块不在 | **2 / 20** | 窗口内块选择 |
  | ④ **全都在场** | **11 / 20** | **生成/综合**（按小节分组逐节回答 / 子问题拆解） |

  ⇒ **过去四张卡（E-02~E-05）一直在 ② 这一格打转，而它只占 4/20**；
  **① 召回层第一次被量化成 3/20**，此前被"文件级 5/5"这个口径完全掩盖
- **E-01 的第一版结论被部分平反**：`q15` 四个要点的小节与块**全在窗口里且它答对了（1.000）**；
  `q16` 3/4 在场却只拿 0.500 ⇒ **"东西都在眼前之后仍答不好"确实存在**。
  当年 E-01 说"瓶颈在生成侧"被 E-02 用指标粒度推翻 —— 现在看：**它错在把这当成唯一原因，而不是错在指出了它**（④ 占 11/20）
- **口径警告（必须一起说）**：证据靠**词面子串匹配**定位（可定位率 53/53，100%），
  但通用 keypoint 会**放宽**小节级判定（`q16` 的 `.vue` 命中 **52 个小节**）——
  该偏差**对 H2 不利**，所以 H2 成立不依赖它；但**不能**据此说"小节级容易满足"
- **代码**：`src/**` **零改动**；单测仍 **52 / OK**；**0 元 LLM 费用**（无任何生成调用）
- **进行中**：无
- **下一步候选（非阻塞，按证据强度排序）**：
  1. **治 ① 召回（3/20）** —— 四张卡以来第一次指向召回层；`q13`/`q17` 各有要点的证据块**连融合池都没进**
  2. **治 ④ 生成（11/20）** —— **按小节分组、逐节回答**，或**子问题拆解**（多跳问句拆成两个单跳）；**这条路不需要改检索**
  3. **治 ③**（窗口内块选择）—— 注意与 E-03 的 `expand` 不同：那次是整节进 prompt 撞预算，这次是窗口内选块
  4. **仍然不建议**：调 rerank（E-04/E-05 已证对多跳净负）。~~`top_k` 12/15 移回第一候选~~ 🔴**该候选已被 T-024/E-07 否掉**（见上表：窗口内容到不了模型）；**真正的杠杆是 `max_chars`（属 `SPEC.md`，需用户批准）**
- **关键上下文**：
  - 累计真实 API 花费仍约 **1.9 元**（T-023 只花 query embedding，约 0.01 元）
  - **多跳的病因现在是"分布"而不是"一个瓶颈"**：引用时必须说四个数，不能说"找到了原因"
  - 本卡的**判据文本 + 提交 hash** 构成可核验的时序证据，这是后续所有对照实验该照做的模板

### 交接 · 第十九段（2026-09-21）

- **已完成**：**T-022 `top_k` × rerank 联合实验** —— 全部卡片闭环（**22 / 22**），**零待办**。**H1 被证伪，且 Step 2 没花钱**
- **原来的假设（H1）**：E-02 说"扩大窗口对多跳有效"、E-04 说"重排对多跳无效" ⇒
  猜 rerank 是**因为只有 4 个席位**才挤掉多跳依据，扩到 8 席就不用排除了
- **0 元 LLM 费用机制诊断的结果（只花 query embedding + rerank 调用，无任何生成调用）**：多跳「两篇依据都进窗口」

  | 配置 | 多跳两篇都进(/5) | 有依据 17 题全部进窗口(/17) | 平均命中(/20 题) |
  |---|---|---|---|
  | `k=4, off` | 3/5 | 14/17 | 1.000 |
  | `k=8, off` | **4/5** | **16/17** | **1.100** |
  | `k=4, on` | 2/5 | 14/17 | 1.000 |
  | **`k=8, on`** | **3/5** | 15/17 | 1.050 |

  > 分母必须写清：3 道不可答题 `gold_sources` 为空，计入即"空集恒真"，故该行只统计有依据的 17 题；
  > 统一按 20 题分母则为 17/19/17/18。**独立验证就是在这里抓到一处口径错误**（E-05 §7 已更正）

  → H1 预测 `(k=8,on)` **≥** `(k=8,off)`，实测**更低** ⇒ **假设死**
- **最干净的反例 `q15`**：`k=8` 且**不开** rerank 时三篇依据全进窗口，**一开 rerank 就掉一篇**
  ⇒ **席位足够也照挤：问题不在"位置不够"，而在 rerank 的目标函数（单 query 相关性）与"跨文档互补覆盖"本来就不一致**
- **`q14` 是唯一符合 H1 的一题**（扩窗口确实救回来了）⇒ **H1 不是全错，而是"只在个别题上成立"**
- **`q13` 四种配置全缺同一篇** ⇒ 与窗口/重排**都无关**，问题在融合池排名
- **rerank 真在跑（三条证据，照用 E-04 的方法）**：`_rerank` 调用 **40 次 / 0 异常**；
  - **"rerank 真的改变了结果"的正确证据是 `chunk_id` 级 = 20/20（k=4 与 k=8）**；
    文件级同一件事有三个数：有序 `source` 列表 **13/20 与 19/20**、去重集合 **10/20 与 12/20**
    （k=4 有 3 题 `q01`/`q12`/`q16` 仅换序；另 7 题 rerank 只在同一文件内部换序，文件级看不出）
    → **引用文件级数字必须写明是"列表"还是"集合"**（这是"粒度决定你能看见什么"第四次现身）
- **决策点**：**不进入 Step 2（省下约 0.9 元）**。
  ⚠️ 如实留痕：卡面原写的字面阈值是"`(k=8,on)` 仍只有 1 篇依据"，实测 3/5 **不是字面触发条件**；
  我按 **H1 的预测方向**判定（方向更严：提前判定失败而不是延后），并写进 `EXPERIMENTS.md` **E-05 §4** —— **判据的调整必须留痕，否则就是"事后改标准"**
- **顺带解释 E-04**：rerank **从不通过"增加依据覆盖"生效** ——
  `k=4` 开它前后平均命中依据数都是 **1.000**，可 `accuracy` 却升了；`k=8` 开它反而让覆盖**变差**（1.100 → 1.050）
  ⇒ **它做的是"把已有的块排到更前面"，不是"把缺的块找回来"** ⇒ **单跳受益、多跳受害**，与 E-04 的分题型结论完全自洽
- **代码**：`src/**` **零改动**（本轮是诊断，不是改代码）；单测仍 **52 / OK**
- **进行中**：无
- **下一步候选（非阻塞，按性价比排序）**：
  1. **0 元的小节级覆盖测量** —— `k=8,off` 下多跳题已经 **4/5 文件级齐全**，答案却仍差 ⇒
     "文件在窗口里"≠"需要的那一小节在窗口里"（E-02）。**把 keypoints 对应到具体 heading 再看一次，不需要花钱**
  2. **按 `source` 限席位的排序**（保证跨文档覆盖）—— 正交方向，直击"rerank 挤依据"的机制
  3. **是否在线启用 rerank** —— "延迟换质量"的取舍决策（默认值 + 超时/降级可观测性）
  4. ②Agent / Function Calling（JD 缺口）· ③SSE 前端（**须先改 SPEC**）
- **关键上下文**：
  - 累计真实 API 花费约 **1.9 元**（T-022 只花了约 0.01 元 —— **0 元诊断直接把 0.9 元省掉了**）
  - **多跳的瓶颈至今未找到**，但排除项已经很硬：文件级召回 · **窗口大小**（`k=4→8` 不足；**后经 T-023 用 `k=12/15` 重新救回，见第十九/二十段**）· 材料排列 · 全局重排 · **扩窗口+重排的联合**

### 交接 · 第十八段（2026-09-21）

- **已完成**：**T-021 启用并真实验证 rerank** —— 全部卡片闭环（**21 / 21**），**零待办**。**这是本项目第一个正面结果，但它没有解决多跳**
- ⚠️ **证据链只收到一半（最重要的一条，别读成"收口了"）**：
  内容在池子里（E-01）→ 扩大窗口**对多跳**有效（E-02）→ 排列方式无效（E-03）→ **排序对单跳有效、对多跳无效（E-04）**
  - `single_hop` 覆盖率 **0.5694 → 0.6944（+0.125）**、桶内 5/4/3 → **6/5/1**
  - `multi_hop` 覆盖率 **0.5000 → 0.5000（±0.000）**、桶内 **1/3/1 → 1/3/1 完全没变**（`q16` +0.25 被 `q14` −0.25 精确抵消）
  - ⇒ **`+0.088` 的收益全部来自单跳题**；而本卡（以及 E-01~E-03）的动机都是**多跳** ⇒ **多跳的瓶颈仍未找到**
  - 已逐一排除的：文件级召回 · **窗口大小**（`k=4→8` 不足；**`k=12/15` 又重新有效**，见下）· 材料排列 · 全局重排
- **Step 1 连通性（3 条路径实测，几厘钱）**：

  | 路径 | HTTP | 结论 |
  |---|---|---|
  | 老全局端点 + `gte-rerank-v2` + 嵌套 body（**现配置**） | **200** | ✅ 响应形状与实现一致 ⇒ **A 路径，`src/retrieve.py` 零改动** |
  | 老全局端点 + `qwen3-rerank` + 同一套嵌套 body | **200** | 官方推荐的替代模型**可纯靠 `RERANK_MODEL` 切换**（**实测行为、非文档承诺**） |
  | `compatible-mode/v1/reranks` + 扁平 body | **404** | ❌ **不能**按文档把 `_rerank` 改成扁平形状 |

- **Step 2 同进程 A/B（约 0.6 元；唯一自变量 `use_rerank`）**：

  | 指标 | `rerank=0` | `rerank=1` |
  |---|---|---|
  | **`avg_keypoint_coverage`** | 0.5490 | **0.6373（+0.088）** |
  | `accuracy` / `wrong_rate` | 0.45 / 0.20 | 0.50 / **0.10** |
  | `hit_rate` | 0.80 | 0.85 |
  | `avg_latency_ms` | 1136.2 | **3187.9（+2.05 s）** |

  逐题 **升 4 / 降 1**（`q03`/`q11`/`q12`/`q16` 升；**`q14` 0.500 → 0.250 降**）
- **三条"确实生效"的证据**（缺一条都不能说 rerank 起作用了）：`_rerank` 被调 **20 次 / 0 异常** · **20/20 题 top-k 顺序被改变** · 输出**全部来自融合池（0 违反）**
- **Step 3 降级实测（通过）**：错 key → `401 Client Error` → 打出 `[warn] rerank 降级` → **不抛异常** → 结果与融合结果**逐字一致**
- **代码**：`src/**` **零改动**（A 路径）；`tests/test_offline.py` **+121 行 / −0**（3 条新用例）→ **49 → 52 测试全绿**
  - 新用例：`test_mock_mode_never_calls_rerank`（mock 绝不发请求）、`test_rerank_failure_degrades_to_fused_result`（失败必须降级）、`test_rerank_order_is_actually_used`（接线正确，别把输出丢了）
  - work_t1 还做了**变异反证**：分别把三处守卫改坏 → 对应用例**确实变红**（并自己发现并纠正了一个"替换没生效导致的假绿"，见决策日志）
- **进行中**：无
- **下一步候选（非阻塞）**：
  1. **`top_k` 与 rerank 联合调参**（`RERANK_ENABLED=1` + `top_k_final=8`）—— **当前证据指示的最优先项**：
     E-02 说"窗口更大对多跳有效"，E-04 说"重排对多跳无效"，合起来意味着 rerank 现在是在**替多跳题做取舍**（把第二跳挤掉）；
     先扩大窗口，再让它只做排序。这是**最便宜、最直接**的下一个实验（约 0.3 元）
  2. **保证跨文档覆盖的排序**（按 `source` 限席位，E-01 就提过的"多路召回"）—— 与 rerank 的"单 query 相关性"正交，正对它挤掉第二跳的倾向
  3. **是否在线启用 rerank** —— 需要一个"延迟换质量"的取舍决策（默认值 + 超时/降级可观测性）
  4. `TOP_K_FINAL` 4→8（有 5 升 0 降证据，改 `SPEC.md` 默认值需另开卡 + 用户批准）
  5. ②Agent / Function Calling（JD 缺口）· ③SSE 前端（**须先改 SPEC**）
- **关键上下文**：
  - 累计真实 API 花费约 **1.9 元**（本轮 T-021 约 0.7 元：3 次探测 + 40 次生成 + 1 次降级）
  - **`eval/report_t021_rerank_{off,on}.json` 与 `report_detail_*` 都留了本地**（已 gitignore），E-04 的数字全部可从它们复核
  - **rerank 的静默降级是最大的运维陷阱**：`[warn] rerank 降级` 只打到 stdout，没有计数器/指标。
    任何基于 rerank 的对比，**必须先确认没发生降级**（E-04 用三条证据兜住）
  - **收尾时抓到的这个"总体 vs 分题型"差异，是本轮最有价值的一条**：总体数字看起来是成功（+0.088），
    按题型拆开才发现**目标（多跳）完全没动**。它没有改变任何代码，但改变了**结论该怎么讲**

### 交接 · 第十七段（2026-09-21）

- **已完成**：**T-021 的 Step 0（官方契约核对，0 元）** —— 卡片已开（M9），**实现尚未动**
- **为什么先做 Step 0**：直接开开关跑 A/B，最可能的结果是**调用抛异常 → 被降级静默吞掉 → 看起来"rerank 没效果"**，而真实原因是契约变了
- **核对结果（官方文档更新时间 2026-09-04）**：

  | # | 结论 |
  |---|---|
  | 1 | ✅ 字段名与我们实现**一致**：`{model, input:{query,documents}, parameters:{top_n}}` → `output.results[].{index, relevance_score}` |
  | 2 | ⚠️ 官方已公告 **`gte-rerank` 下线、推荐 `qwen3-rerank`**；后者是**另一套形状**：请求扁平、**响应 `results` 在顶层（无 `output`）**、端点 `/compatible-api/v1/reranks` |
  | 3 | ⚠️ 文档端点已改为**业务空间域名** `{WorkspaceId}.cn-beijing.maas.aliyuncs.com/...`；我们配的全局域名 `dashscope.aliyuncs.com/...` 是否仍兼容**未验证** |

- **T-021 的五步**：契约核对（✅ 完成）→ 最小连通性真实调用 → 按结果二选一（沿用 gte / 补 qwen3-rerank 分支）→ **A/B**（`RERANK_ENABLED` 0 vs 1）→ **降级路径实测**（错 key 下不得抛异常、须降级、须出现 `[warn] rerank 降级`）
- **红线**：`RERANK_ENABLED` 默认值**保持 `0`**；本卡只产出证据与实现，改默认值须另开卡 + 用户批准
- **成本**：Step 1/3 几分钱，Step 2 约 0.6 元，**上限 ≤ 0.8 元；Step 2 执行前须用户明确确认**
- **进行中**：无（Step 0 已完成并落盘，未动任何代码）
- **下一步候选**：① 用户点头后执行 T-021 Step 1/2/3 · ② `TOP_K_FINAL` 4→8（有 5 升 0 降证据，但改 `SPEC.md` 默认值需另开卡）· ③ Agent / Function Calling（JD 缺口）· ④ SSE 前端（**须先改 SPEC**）

### 交接 · 第十六段（2026-09-21）

- **已完成**：**T-020 小节级整理三模式对照** —— 全项目 **20 / 20 卡片闭环，零待办**。**结论：假设被证伪，两个模式都没有净收益**
- **假设从哪来**：E-02 定位到「检索到同一文件的**错误小节**」→ 于是设计三模式（唯一自变量 `SECTION_MODE`）
- **实测（每个模式一次真实评测，合计约 0.3 元）**：

  | 指标 | `off`（基线） | `diverse` | `expand` |
  |---|---|---|---|
  | **`avg_keypoint_coverage`** | **0.5343** | 0.5147 ↓ | 0.5441（噪声内） |
  | `accuracy` | 0.50 | 0.45 ↓ | 0.50 |
  | prompt 中位 / 最大 | 1623.5 / 1936 | 1465 / 1936 | **2388 / 4742** |
  | **有资料被 3000 字符预算丢掉的题数** | **0** | 0 | **14 / 20** |
  | 其中"最终 prompt 仍超 3000"的题 | 0 | 0 | **4**（`q04`/`q07`/`q11`/`q14`） |

- **为什么 `diverse` 反而更差**：按小节去重**减少了进入上下文的块数**（材料 4→2~4 条），
  换来的"小节多样性"**补不回丢掉的内容** → **内容总量比排列方式重要**
- **`expand` 为什么不能当默认**：覆盖率 +0.010 落在噪声内（升 2 / 降 1），代价却是 **14/20 题丢资料**；
  而且 `build_prompt` 是"第一条塞不下就 `break`"，第 2 条小节过长时模型只剩很小的第 1 条（`q05` **346 字符**、`q09` **200 字符**），`q04` 从 1.00 直掉 0.00
- **三个实验合起来是一条一致的结论**：`top_k` 4→8（**内容更多**）→ 5 升 0 降 ✅；`diverse`（**内容更少**）→ 更差；`expand`（**更长但被截断**）→ 持平偏险
  → **真正起作用的是「上下文里有没有那段内容」**
- **下一步的矛头（证据指向）**：需要的内容**明明在融合池里**（E-01 证：文件级 5/5），却没进 top-k 窗口 ——
  这是**排序**问题，正是 **rerank** 该解决的。而 `src/retrieve.py` 的 rerank **已实现、`RERANK_ENABLED=0` 从未启用、也从未用真实 API 验证过**（`SPEC.md` 把它列为二期可选）
- **进行中**：无
- **下一步候选（非阻塞）**：
  1. **启用并真实验证 rerank**（`gte-rerank-v2`）—— 证据指向这里，但**要先核官方字段名**（当前实现从未联网验证）
  2. 把 `TOP_K_FINAL` 默认 4→8 —— 已有 5 升 0 降 的证据，但改 `SPEC.md` 默认值需另开卡 + 用户批准
  3. ②Agent / Function Calling（JD 缺口）· ③SSE 前端（**须先改 SPEC**）
- **关键上下文**：
  - 累计真实 API 花费约 **1.1 元**
  - **`eval/report*.json` 是本项目最值钱的资产** —— E-02 完全靠重读它们得出（0 元），E-03 的三个原始报告也都留了本地
  - `SECTION_MODE` 默认 `off`：**代码在仓库里、测试有、但默认不生效**（这是按证据做的决定，不是没做完）
- **已同步**：工作区里的 `RAG项目_面试问答.md`（不在仓库内）的同类错误表述已于 2026-09-21 一并对齐（Q5 归因改写 + 全部行号引用重核）
- **本次一并修掉的"仪器问题"**：早先写进 E-03 / PLAN / BOARD 的「4 题被截断」用的是**窄口径**（最终 prompt 仍 > 3000）。
  2026-09-21 用一次**只跑检索、不调 LLM** 的本地复算（0 元）核对后，主口径改为**宽口径**：**14 / 20 题有资料被丢**。
  两个数都真，但窄口径把风险说小了一个数量级 —— **这正是 E-02 那条教训（粒度决定你能看见什么）在本实验里重演**，
  所以 E-03 现在把两个口径**并排写出**，并注明「报截断必须说清判据」

### 交接 · 第十五段（2026-09-18）

- **已完成**：**T-018 修正 E-01 的错误结论** —— 全项目 **18 / 18 卡片闭环，零待办**。**0 元**（纯分析，未重跑任何评测）
- **怎么发现的**：判分把 `keypoints_match` 的连续值（0~1）粗分成「对(≥0.8)/部分对/错」，于是 5 道多跳题里没有整题翻转就显示"一分未动"。
  换成**连续覆盖率**重读**同一批已有数据**：

  | 指标 | V1 (top-4) | V2 (top-8) |
  |---|---|---|
  | `single_hop` 平均覆盖率 | 0.569 | **0.667** |
  | `multi_hop` 平均覆盖率 | 0.450 | **0.550** |
  | 逐题升降 | — | **上升 5 题 / 下降 0 题 / 持平 12 题** |

  **5 升 0 降 → p ≈ 0.031（单侧符号检验）**，是本项目目前最强的统计证据。
- **⚠️ 修正后的瓶颈判断（三层都试过）**：

  | 曾怀疑 | 证据 | 判决 |
  |---|---|---|
  | 文件级召回 | 融合池 5/5 覆盖全部 gold 文件 | ❌ 不是 |
  | 生成侧跨文档综合 | 模型答"资料里没有"——而内容**确实不在它看到的小节里** | ❌ **不是（E-01 说错了）** |
  | **小节级检索精度** | 上升的 5 题全是"多给几块后对的小节进来了"；`q03/q12/q14/q16/q17` 缺的内容**全在语料里**，只是给的是**错的节** | ✅ **是这里** |

- **最有价值的副产品（面试可直接讲）**：同一批数据、三种指标粒度、三个互相矛盾的结论 ——
  **`hit_at_k`（任一命中 + 对 k 单调）说"召回没问题"，桶指标说"检索侧无效"，连续覆盖率说"有效，只是没跨阈值"**。
  我因此确立：**今后所有对照实验的主指标用逐题连续覆盖率**，桶指标仅作汇报
- **还有 4 题连 `top_k=8` 都没救**（`q02`/`q12`/`q13`/`q17`）→ 光加 k 不够，需要**把命中块所在的小节一起给模型**（父子分块的最小形态）
- **进行中**：无。**下一步（已排好）**：
  1. **T-019**：把**连续覆盖率**接进评测报告（`eval/metrics.py` / `eval/run_eval.py` / 测试）—— **先修仪器，再改系统**，否则下一次改动照样测不出来
  2. **T-020**：实现**小节级上下文**（命中块带上其所在小节），用新的覆盖率指标对照
  3. 之后跑一次真实评测（约 0.1 元）看覆盖率能推到多少
- **关键上下文（⚠️ 后续会话必读）**：
  - **`keypoints_match` 是字面子串匹配** —— 改写措辞即判错（例：`q02` 模型说"原始值/对象"，keypoints 是"原始类型/对象类型"）。**所有准确率数字都必须带这条限制引用**
  - `hit_rate` **不能跨 k 比较**（单调性）
  - 向量检索层**不可跨进程复现**（§5.12）
  - **`eval/report*.json` 是本项目最值钱的资产**：E-02 完全靠重读它们得出，没花一分钱。**别删本地产物**
- **待同步**：工作区里的 `RAG项目_面试问答.md`（不在仓库内）有同类错误表述，需另行更新

### 交接 · 第十四段（2026-09-18）

- **已完成**：**T-017 修正治理脚本路径** —— 全项目 **17 / 17 卡片闭环，零待办**
- **问题**：`PLAN.md §0.2` 的 `$K` 指向 `_kit_inspect\agent-project-kit\push-task.mjs`，但工作区整理后它被移到**工作区根目录**（`agent-project-kit\push-task.mjs`）。T-016 提交时实际撞到 `MODULE_NOT_FOUND`，临时改用新路径才推成功 —— **这个坑不修，下一个会话照样会踩**
- **修复**：更新 `$K`，并在 §0.2 补一条「若报 Cannot find module → 先 `Get-ChildItem -Recurse -Filter push-task.mjs` 定位再改」。
  这条提示比路径本身更重要：`$K` 是**仓库外的本机路径**，会随工作区整理反复失效
- **确认无需改 `AGENTS.md`**：`grep push-task` 在该文件**零命中** —— 它从不引用这个脚本（此前看到的"怎么落地这条"那段是 `_kit_inspect` 里的**套件模板**，不是本仓库的治理书）
- **⚠️ 工作区已发生较大变化（供后续会话注意）**：文件时间已到 **2026-09-21**，新增/变动包括 `简历_AI应用开发岗/`、`求职面试个人信息保护_*`（报告 / PPT / 6 分钟发言稿）、`local-llm-tier/`、`_ab-test2/`、`foo-rename/`、`t2-local/`、`slides/`、`assets/`、`shots/`。
  其中 **`agent-project-kit/` 的位置变化直接影响本仓库的提交闭环**（即本条修复）。若后续再整理工作区，请同步更新 `§0.2` 的 `$K`
- **进行中**：无
- **下一步候选（非阻塞，由用户决定）**：②Agent/Function Calling（JD 缺口）· ③SSE 前端（**须先改 `SPEC.md`**，因为"前端页面"在非目标清单里）· ④自有项目文档（用户已说明该项目结束、小组解散）
- **关键上下文**：累计真实 API 花费约 **0.8 元**；项目 17/17、门禁全绿、`main` 干净

### 交接 · 第十三段（2026-09-18）

- **已完成**：**T-016 multi-hop 检索侧对照实验** —— **全项目 16 / 16 卡片闭环，零待办**。**结论是负结果**，而这个负结果比"提升了 X%"更有价值
- **实验设计（新建 `docs/EXPERIMENTS.md`，E-01）**：题库 / 语料 / 模型全部冻结，**唯一自变量是 `TOP_K_FINAL`**（环境变量，不动一行代码）
- **Step 0 诊断（先量再改）**：

  | 题 | 判分 | 融合池覆盖 | top-4 覆盖 |
  |---|---|---|---|
  | q13 | wrong | 2/2 | **1/2** |
  | q14 | partial | 2/2 | 2/2 |
  | q15 | correct | 3/3 | **2/3** |
  | q16 | partial | 2/2 | 2/2 |
  | q17 | partial | 2/2 | 2/2 |

  → **融合池 5/5 全覆盖；top-4 只有 3/5**
- **Step 1 对照（V1 top-4 → V2 top-8）**：

  | 指标 | V1 | V2 | 差异 |
  |---|---|---|---|
  | accuracy | 0.50 | 0.55 | +0.05 = **1 题** |
  | **multi_hop 对/部分/错** | **1/3/1** | **1/3/1** | **0（一分未动）** |
  | single_hop | 6/2/4 | 7/3/2 | +1 题 |
  | hit_rate | 0.80 | 0.85 | 对 k 单调，**不可比** |
  | avg_latency_ms | 1316.3 | 1300.8 | 基本不变 |

- **结论（面试可直接讲）**：
  1. **多跳题的瓶颈不在检索召回** —— 候选从 4 扩到 8，多跳题**一分未动**，而两篇依据本来就 5/5 全在池子里
  2. **瓶颈在生成侧的跨文档综合** —— `q14`/`q16`/`q17` 两篇依据**都在 top-4 里**，却只拿到"部分对"
  3. 所以「先修 multi-hop」的正确顺序是：**先改生成侧（prompt / 上下文组织），而不是扩召回**；父子分块若要做，应作为**生成侧改进**设计
  4. **V2 的数字不足以支持改默认值**（1 题量级 + 单调性伪信号）
- **副产品发现（已写进 `PLAN.md §5.7`）**：`hit_at_k` **对 k 单调**，所以 `hit_rate` **不能跨 k 比较质量**；跨 k 比较必须用「全部依据是否都进 top-k」这类集合级指标
- **进行中**：无。**下一步候选（非阻塞，由用户决定）**：②Agent/Function Calling（JD 缺口，工作量较大）· ③SSE 前端（**须先改 `SPEC.md`**，因为"前端页面"在非目标清单里）· ④自有项目文档（用户已说明该项目结束、小组解散，只能自己凭记忆写）
- **关键上下文**：累计真实 API 花费约 **0.8 元**；`eval/report_v2_topk8.json` 与 `eval/report_detail_v2_topk8.json` 是 V2 的原始产物（本地，已 gitignore）；临时脚本已清

### 交接 · 第十二段（2026-09-18）

- **已完成**：**T-015 缓存检索索引**（M5 性能优化）—— **全项目 15 / 15 卡片闭环，零待办**
  - **根因**：`search()` → `_rankings()` → `KeywordIndex.from_chunks_file(...)`，**每检索一次就把整份 `chunks.jsonl` 重读并重建 BM25 索引**；`_chunk_map()` 又读一遍全量
  - **修法**：把 `KeywordIndex` 与 chunk 映射缓存在 `Retriever` 实例上，并新增 `clear_cache()`；失效点现成（`app/main.py:123` 在 `/ingest` 后已调 `get_retriever.cache_clear()`）
  - **效果（实测）**：单次检索 **401.6 ms → 10.7 ms（-97.3%）**；真实评测端到端 **1654.6 ms → 1316.3 ms（-20.4%）**
- **⚠️ 这张卡的核心争议：我最初的验收标准是错的，而且是被证伪的**
  - 我抓了"改动前"的黄金基线，要求改动后**逐字 diff 为空**。Worker 报 failed 且**拒绝为了让标准变绿而放松它** —— 这个反应是对的
  - 我自己复核后确认：**未改动的原版代码同样对不上**（10 / 6 处差异），差异**只出现在 `vector` 层**（`bm25` 从不出现 → `chunks.jsonl` 没变）→ 原标准**天生不可达**
  - 改为**同进程 A/B**（缓存路径 vs 强制每次重建路径，共享同一 chroma 状态）：**差异 0 处** ← 这才是本卡要保证的"结果中性"，且比原标准更严格
- **⚠️ 派生发现（重要，已记入 `PLAN.md §5.12`）**：**向量检索层不可跨进程复现** —— same code + same index，不同进程对同一 query 的候选集与排序会漂移。
  影响：`hit_rate` 这类指标带这一层不确定性。实测三次真实评测 `hit_rate` 都稳定在 **0.80**，但那是"指标层稳定"，**不等于**逐次召回排名一致。被追问时要如实答。
- **⚠️ 数字变化里哪部分是优化带来的、哪部分不是（必须分清）**
  - **是**优化带来的：`avg_latency_ms 1654.6 → 1316.3`（同进程 A/B 已证明结果中性，延迟下降纯粹来自不再重建索引）
  - **不是**优化带来的：`accuracy 0.45 → 0.50`、`partial_rate 0.30 → 0.25` —— **缓存不可能改变结果**（差异 0 处已证），这是 LLM 的运行间波动，恰好落在 §5.5 记录的 0.45–0.50 区间内。**不许说成"优化提升了准确率"**
  - 旧数字已备份为 `eval/report_before_t015.json`（已 gitignore）
- **进行中**：无。**下一步候选（均需你决定，非阻塞）**：T-016（向量层可复现性）/ T-011 后续（自有文档）/ 那 10 个历史分支的清理
- **关键上下文**：累计真实 API 花费约 **0.7 元**（T-009 首测 + 刷新 + T-015 刷新）；所有临时脚本已清，服务进程已收

### 交接 · 第十一段（2026-09-18）

- **已完成**：**T-014 `/chat` 延迟口径修正** —— **全项目 14 / 14 卡片闭环，零待办**
  - **根因**：`app/main.py` 的 `chat()` 里 `started` 计时本身**是**覆盖检索的，但下一行**优先采用 `answer.latency_ms`**（只含生成段），把检索那一段丢掉了
  - **修法**（diff 仅 1 个文件、约 12 行）：`started = time.perf_counter()` 挪到 lazy 构建之前，并**弃用 `answer.latency_ms`**，直接返回请求作用域耗时
  - **修复前后（captain 独立起服务、双模式复测）**：

    | 模式 | 修复前 接口 / 客户端（比值） | 修复后 第 2 次 接口 / 客户端（比值） |
    |---|---|---|
    | `MOCK=1` | 0 / 460 ms（0.00） | **452 / 466.8 ms（0.97）** |
    | `MOCK=0` | 1251 / 8217 ms（0.15） | **1700 / 1705.4 ms（1.00）** |

  - 顺带确认没改坏：`/health` 仍返回 `{"status":"ok","mock":…,"chunks":400}`；空 `question` 仍 **400**
- **冷启动取舍（已记入决策日志）**：**计入**，不预热、不剔除 —— 首次请求确实付了建索引 / 加载 BM25 的成本（mock 3891 ms、真实 10516 ms），抹掉它等于名不符实；第 2 次起即为稳态
- **⚠️ 本卡无法补离线单测**：`AGENTS.md §6` 禁止 `tests/test_offline.py` import `fastapi`，而 `app/main.py` 顶层就 import 它 —— 本卡只能靠「真起服务打接口」验证（已做）。用例数仍为 **25**
- **进行中**：无。**没有待办卡了**
- **关键上下文**：
  - 累计真实 API 花费约 **0.5 元**（T-009 首测 ≈0.3 + 按最终语料刷新 ≈0.2 + 本轮验证 2 次 ≈0.01）
  - 起服务验证后**务必收掉进程**（本轮每次都确认：8000 / 8001 端口空闲、无残留 python 进程）
  - 重建凭据读取器时：解析 YAML 用 `[ \t]` 而非 `\s`
  - 任何角色**禁止**在真仓库执行 `git clean`
- **待确认（都不阻塞交付）**：
  - `TASK_BRIEF.md §8` 的「自有项目文档谁来补」仍未定（当前 12 份语料全是框架官方文档）
  - 「环境变量优先于 `.env`」缺落盘测试（`PLAN.md §5.11`，需另开卡）
  - 10 个已合并的历史分支**未清理**（用户选择"先不动"）

### 交接 · 第十段（2026-09-18）

- **已完成**：**T-010 文档与交接收口** —— M1 / M2 的卡片全部闭环，项目进入「可以投出去」的状态
  - **README 里 9 条命令本轮逐条实跑**（这正是 T-010 的通过标准）：`pip install` / `unittest` / `compileall` / `src.ingest`（真实模式）/ `--no-reset` / `--raw-dir` / `eval --mode mock` / **`uvicorn` 起服务（mock 与真实两种模式）**
  - **首次验证了 HTTP 层**（此前从未真正起过服务）：`GET /health` → 200 `{"status":"ok","mock":true,"chunks":400}`；`POST /chat` → 200（4 条 hits + 引用 + 真实回答）；空 `question` → **400**（符合 `SPEC.md`）
  - ⚠️ 测 `--no-reset` 时用隔离的 `CHROMA_DIR` / `COLLECTION` / `CHUNKS_FILE`，**没有污染已建好的真实索引**（复测真 `chunks.jsonl` 仍 400 行）
- **T-009 的数字已按最终语料刷新**（T-012 之后：12 份 / 400 块）：
  `{n:20, accuracy:0.45, partial_rate:0.30, wrong_rate:0.25, refusal_accuracy:1.00, hit_rate:0.80, avg_latency_ms:1654.6}`
  - **对照实验（重要）**：去掉 README 噪声前后 `accuracy` / `hit_rate` **完全不变**（0.45 / 0.80），只有一题从"部分对"落到"错"，幅度落在波动范围内 → **这点噪声的影响在 20 题分辨率下测不出来**。T-012 的价值是索引干净与语义正确，**不是分数提升** —— 记录这个"测不出来"正是为了避免把噪声当成绩效。
  - 旧数字已备份为 `eval/report_t009_before_t012.json`（已 gitignore）
- **⚠️ 新发现一个真缺陷，已开卡 T-014（唯一待办）**：`/chat` 的 `latency_ms` 只覆盖生成段 —— mock 接口报 **0** / 客户端 **460 ms**；真实接口报 **1251** / 客户端 **8217 ms**。
  **评测报告的 `avg_latency_ms` 不受影响**（由 `eval/run_eval.py` 独立计时，覆盖检索 + 生成）。
- **进行中**：无。**下一步：T-014**（几行改动，改完项目就彻底干净）
- **关键上下文**：
  - 累计真实 API 花费约 **0.5 元**（T-009 首测 ≈0.3 + 刷新 ≈0.2）
  - ⚠️ 跑单测前确认 shell 里**没有**导出 `MOCK`（T-013 虽已修，保持习惯更稳）
  - ⚠️ 任何角色**禁止**在真仓库执行 `git clean`（见决策日志 —— 语料与索引都刻意放在 `.gitignore` 里）
  - 重建凭据读取器时：解析 YAML 用 `[ \t]` 而非 `\s`（`\s` 会吃掉换行，把下一行整行当成上一行的值）
- **待确认**：
  - `TASK_BRIEF.md §8` 的「自有项目文档谁来补」仍未定（当前 12 份语料全是框架官方文档）
  - 「环境变量优先于 `.env`」缺落盘测试（`PLAN.md §5.11`，需另开卡）

### 交接 · 第九段（2026-09-18）

- **已完成**：**T-009 真实模式 20 题评测** —— **M2 拿到首个真实数字**
  - `& $P -m src.ingest`（`MOCK=0`）→ exit 0，`files=13 chunks=405 elapsed_ms=23127 mocked=False`（真实调用了 Embedding 接口）
  - `& $P -m eval.run_eval --mode full --questions eval\questions.jsonl --out eval\report.json` → exit 0，32.9s
  - **`eval/report.json` = `{n:20, accuracy:0.45, partial_rate:0.35, wrong_rate:0.20, refusal_accuracy:1.00, hit_rate:0.80, avg_latency_ms:1644.8}`**
- **第二个哨兵（逐题明细，另存 `eval/report_detail.json`）**：分类结果 —— `single_hop` **0.50**（6 对 / 3 部分 / 3 错，检索命中 11/12）；`multi_hop` **0.20**（1 对 / 3 部分 / 1 错，检索命中 5/5）；`unanswerable` **1.00**（3 题全部正确拒答）
- **⚠️ 数字有运行间波动**：同输入连跑两次 `accuracy 0.45 → 0.50`、`partial_rate 0.35 → 0.30`；`wrong_rate / refusal_accuracy / hit_rate` 稳定在 0.20 / 1.00 / 0.80。**引用时必须注明"单次运行"或写成区间**（详见 `PLAN.md §5.5`）
- **⚠️ 一个必须先知道的行为**：`q12` 是**可答题**（检索 `hit=True`）却被模型答成「根据现有资料无法回答」—— 生成侧过度保守，不是检索缺陷
- **进行中**：无。**下一步 T-010（文档与交接收口）**；另有 **T-012**（README 被入库）与 **T-013**（单测未隔离环境）两张待办缺陷卡
- **关键上下文（下一个会话必看）**：
  - **key 不进 `.env`**：本次由一次性脚本读 `~/.dsh/.credentials.yaml`（`DEEPSEEK_API_KEY` / `DASHSCOPE_API_KEY`）注入**当前进程**。**跨 pwsh 调用不保留**，必须"读 key → 入库 → 评测"在同一进程里顺序做完；临时脚本已删，下次需重建
  - ⚠️ 重建那个读取器时**别用 `\s*` 包住冒号**：`\s` 会吃掉换行，把 `refs:` 的下一行整行当成它的值 —— 本次就因此漏掉了 `DEEPSEEK_API_KEY`，改用 `[ \t]` 才对
  - 实测 **`openai==3.15.0` 与代码假设兼容**（`client.embeddings.create` / `client.chat.completions.create` 均正常）→ `TASK_BRIEF.md §8` 里那条"真实调用兼容性待确认"可以关掉
  - 费用：真实入库 + 两次 20 题评测，合计 **约 0.3 元以内**
  - `eval/report.json`（第一次原始结果）与 `eval/report_detail.json`（第二次 + 逐题明细）都在本地，被 `.gitignore` 的 `eval/report*.json` 覆盖，**不入公开仓库**
- **待确认**：`TASK_BRIEF.md §8` 里「自有项目文档谁来补」仍未定（现有 12 份语料全是框架官方文档）

### 交接 · 第八段（2026-09-18）

- **已完成**：**T-008 按真实语料出 20 题正式题库**（`eval/questions.jsonl`）。配比 12/5/3、`gold_sources` 全部指向 `data/raw/` 真实文件、无 `\uXXXX`、`multi_hop` 每题 ≥2 篇依据
- **独立复核两轮，共关掉 4 条 findings**：
  - 第一轮（20 题逐题核依据、每一条都贴出原文句）：`F1`（medium）q13 的 `gold` 末句**被语料证伪**（它说计算属性页面都在用 ref()，而 `04` 的基础示例用的是 `reactive()`）；`F2`（low）q15 漏了 `08_FastAPI_查询参数.md`；`F3`（low）q17 的第二依据只是凑数
  - 第二轮（只看这 3 处修复）：F1/F2/F3 **全部真关闭**，但暴露 `D1`（medium）—— 我为关闭 F3 把 q17 改成了 **q16 的近重复题**
  - 已把 q17 换成与 q16 正交的另一跳（跨组件复用有状态逻辑 ＋ 组件间传数据），D1 关闭
- **⚠️ 一次事故（已恢复，但必须留痕）**：复核角色收尾时执行 `git clean -fdX`，而 `-X` 只清"忽略文件"——于是把被 `.gitignore` 忽略的 **11 份第三方语料 ＋ `data/chunks.jsonl` ＋ `data/chroma/` ＋ `eval/report_mock.json` 全删了**
  - **恢复**：按 `DOC_SOURCES.md §1.2` 登记的 URL ＋ 字节数 ＋ sha256 前 12 位逐份重下，**11/11 校验一致**；`MOCK=1` 重跑 `src.ingest` 得 `files=13 chunks=405`（与事故前**完全一致**，反证恢复是逐字节正确的）；`report_mock.json` 已重生成
  - **新规矩见决策日志**：任何角色都不许在真仓库跑 `git clean`
- **副产品：T-008 让 mock 报告终于有了信号**。占位题时 `hit_rate=0.0`；换真题后 mock 得到 `hit_rate=0.8000`、`partial_rate=0.1500`、`wrong_rate=0.8500`、`avg_latency_ms=478.60` —— **`hit_rate=0.8` 说明检索真的命中了正确文档**（T007-F1 的"占位 0"问题因此消解）。注意 `accuracy=0.0` 仍**不可作品质结论**：MockGenerator 只是回显命中片段、不会组织答案
- **新开缺陷卡 T-013**：`tests/test_offline.py:299` **未隔离环境变量** —— shell 里 `$env:MOCK='1'` 时单测 `FAILED (failures=1)`、不设时 `OK`。`SPEC.md §5` 规定环境变量优先于 `.env`，所以**实现是对的**；而 `README.md` 第 1 步恰好教用户导出 `MOCK=1`，照做再跑测试就会看到假失败
- **进行中**：无。**下一步 T-009（真实模式 20 题评测）** —— 需用户确认费用并在 `.env` 填 `LLM_API_KEY` / `EMBED_API_KEY`
- **关键上下文**：
  - ⚠️ 跑 `AGENTS.md §6` 第二条命令（离线单测）前，确认 shell 里**没有**导出 `MOCK`，否则会命中 T-013 的假失败
  - 事故恢复后语料 sha256 与 `DOC_SOURCES.md §1.2` 登记一致；如需再次核对，直接照登记表比
- **待确认**：见 `docs/TASK_BRIEF.md §8`（自有文档谁来补 / 真实评测费用 / `openai==3.15.0` 真实调用兼容性）

### 交接 · 第七段（2026-09-18）

- **已完成**：**T-007 mock 全链路验收（核心卡）** → **M1 闭环**。`MOCK=1`、零 API key、零费用下跑通「入库 → 检索 → 生成 → 评测」：
  - `& $P -m src.ingest` → exit 0，`files=13 chunks=405 elapsed_ms=2539 mocked=True`
  - `data/chunks.jsonl` **405 行**（= ingest 报告的 `chunks`）、`data/chroma/` 已生成
  - `& $P -m eval.run_eval --mode mock --questions eval\questions.jsonl --out eval\report_mock.json` → exit 0，`mode=mock mock=True top_k=4 n=20`
  - 报告 = `{n:20, accuracy:0.0, partial_rate:0.0, wrong_rate:1.0, refusal_accuracy:0.0, hit_rate:0.0, avg_latency_ms:477.85}`
  - 反作弊探针：同一命令换成 3 题题库后 **`n` 从 20 → 3** → 证明真的在读 `--questions` 指向的文件
- **独立验证（另一角色，只判定不改）**：裁决 **pass**。它没有沿用我的探针，而是自造 **5 题 + 1 行坏 JSON** 重跑：`n=5`、坏行被正确跳过、指标由 `hit_rate=0.0` 变为 **`0.8`**、`accuracy=0.4` → **证明指标是真算出来的、不是硬编码**；另做硬编码扫描（`477|405|0.0000|report_mock` 全无命中）与 mock 真实性反证（无 `.env`；`MOCK=0` 时必然抛 `EMBED_API_KEY 未配置`）
- **⚠️ 报告的 0.0000 不可作品质结论（T007-F1 · high）**：20 题的 `gold_sources` 全指向磁盘上不存在的占位文件 → `hit_at_k` 恒 False → `hit_rate=0`；`gold` 是占位文本 → `accuracy=0`；MockGenerator 只在检索为空时拒答、而检索永远有结果 → `refusal_accuracy=0`。**T-008 换成真题后必须重跑；在此之前任何引用该报告的地方都要标注「占位数据、不可作品质结论」**
- **新开缺陷卡 T-012**：`data/raw/README.md` 被当语料切了 **5 块**入库（`files=13` 而非 12），会让「怎么放语料」这类元信息参与检索、稀释真实命中。已在 `PLAN.md` 登记最小复现与验收命令；**本卡未改一行代码**（T-007 `inScope` 为空）
- **进行中**：无。**下一步 T-008（按真实语料出 20 题正式题）** → T-009（真实评测，产生费用，需用户填 key）
- **关键上下文**：
  - T-007 期间 `docs/PLAN.md` 有改动（+83/−5）：是**队长文书**（把 T-007 标完成、登记 T-012），**不是越权改代码**；`src/**`、`eval/**` 一字未动（T007-F3 要求显式归属）
  - 产物 `data/chunks.jsonl`(405) / `data/chroma/` / `eval/report_mock.json` **全部保留**，是 T-008 / T-009 的输入
- **待确认**：见 `docs/TASK_BRIEF.md §8`（自有文档谁来补 / 真实评测费用 / `openai==3.15.0` 真实调用兼容性）

### 交接 · 第六段（2026-09-18）

- **已完成**：**T-006 补齐语料**。`data/raw/` 现共 **12 份**（1 份自有 + 11 份第三方官方中文文档），超过卡面要求的 10 份；`docs/DOC_SOURCES.md` 已按「已入库 / 待补」两态重写，逐条登记 文件名 → 来源 URL → 字节数 → sha256 前缀
- **独立验证（另一角色只判定、不改代码）**：11 份语料**全部重新联网下载**，与本地做**原始字节 sha256 比对 11/11 完全一致**；无重复文件、无空壳（最小 3224 B）、UTF-8 无 BOM；第三方文档全被 `.gitignore` 挡住（`git status` 不出现任何 `data/raw/NN_*.md`）→ 裁决 **pass**
- **进行中**：无。**下一步 T-007（mock 全链路验收，核心卡）** —— T-006 阻塞已解除
- **关键上下文**：
  - 语料构成：Vue 3 中文 5 份 + FastAPI 中文 5 份 + MyBatis-Plus 1 份 + 自有 1 份；抓取日期 2026-09-18
  - ⚠️ **T-008 出题必须回避代码示例类题面**：FastAPI 4 份语料里有 **29 处**未展开的 `{* ../../docs_src/... *}` include，代码示例全部缺失（`DOC_SOURCES.md §5.1`）。只出概念 / 名称 / 参数语义类题目
  - ⚠️ **自有文档（需求 / 库表 / 接口 / 部署 / 状态机）仍然缺失**；`00_保洁系统_项目说明.md` 实际内容是简历配图说明（`DOC_SOURCES.md §5.2`）。业务类题目目前没有语料依据
  - T-007 通过标准里的「`files` ≥ 10」**现已自然满足**，无需放宽标准
- **待确认**：见 `docs/TASK_BRIEF.md §8`（自有文档谁来补 / 真实评测费用 / `openai==3.15.0` 真实调用兼容性）

### 交接 · 第五段（2026-09-18）

- **已完成**：**T-005 建 20 题题库（结构占位）**。`eval/questions.jsonl` 共 20 行，配比 12/5/3，7 个字段齐全、id 唯一、`must_refuse` 与 `type` 严格一致、无 `\uXXXX` 转义（中文直写）
- **超出卡面的一处收紧**：卡面只要求「格式与配比」，但验收时发现 `eval/README.md §3.3` 要求 `multi_hop` 至少引用 2 篇文档，占位稿只有 1 篇 → 已改为 A/B 两个占位文档名（`single_hop` 保持 1 篇、`unanswerable` 保持空数组）
- **进行中**：无。**下一步 T-006（补语料）** —— 这是**人工卡**，已按 `PLAN.md` T-006「卡住时」的要求记入**阻塞区**
- **关键上下文**：
  - **M1 现在只差语料这一环**：T-004（依赖）✅ + T-005（题库）✅ + T-006（语料）⏳ → T-007（核心卡）
  - T-007 的通过标准含「`files` ≥ 10」；若用户不补语料，必须**显式授权**放宽，Agent **不许自己改验收标准**
  - 占位题的 `gold_sources` 指向 `占位文档-待T-008替换.md` 等**刻意不存在**的文件：T-007 只验证链路与指标键，不验证准确率；正式题由 T-008 产出
- **待确认**：见 `docs/TASK_BRIEF.md §8`（语料来源 / 真实评测费用 / `openai==3.15.0` 真实调用兼容性）

### 交接 · 第四段（2026-09-18）

- **已完成**：**T-011 治理链修复**。三件事都落地 ——
  1. `docs/TASK_BRIEF.md` 从 11 节全空的模板补齐为 **8 节真实任务书**（按 `SPEC.md` + `PLAN.md` 反推；7 条功能验收标准全部可执行）
  2. `PLAN.md §0.2` 新增 `$K`（治理脚本绝对路径），`§3` 步骤 6 改用它 —— 修掉「照抄 PLAN 的命令跑不通」这个坑
  3. `PLAN.md §0.2` 明确 `docs/BOARD.md` 是 outOfScope 的**唯一例外**，解掉 `AGENTS.md §8` 的 DoD 与 T-004 `outOfScope` 的冲突
- **进行中**：无。**下一步 T-005（建 20 题题库）** —— T-004 已装好依赖，T-005 可立刻开工，做完就能跑 T-007 核心卡
- **关键上下文**：
  - 治理链现在完整：`AGENTS.md`（规矩）→ `SPEC.md`（需求契约，最高优先级）→ `TASK_BRIEF.md`（做什么/给谁做/不做什么）→ `PLAN.md`（怎么做）→ `BOARD.md`（进度与决策）
  - **提交闭环靠外部脚本**（不在仓库内）：`& "C:\Program Files\nodejs\node.exe" $K "$R" -m "..."`，`$K` 定义见 `PLAN.md §0.2`
  - **分支策略已实测生效**：`push-task.mjs` 会**拒绝在 `main` 上提交**；默认「提交 → 推分支 → 快进合并 main → 推 main」，非快进会中止并切回原分支，从不改写历史
  - T-004 的提交 `8da9163` 已按此闭环走完（分支 `chore/T-004-冻结依赖版本` 已合并推送）
- **已知问题（范围外，按 §9.3 只记录不改）**：`data/raw/README.md` 被当作语料入库（见第三段）
- **待确认**：见 `docs/TASK_BRIEF.md §8` 的 3 条（语料由谁提供 / 真实评测费用 / `openai==3.15.0` 真实调用兼容性）

### 交接 · 第三段（2026-09-18）

- **已完成**：**T-004 装依赖并冻结版本**。9 个第三方库全部可导入（`chromadb`/`openai`/`fastapi`/`jieba`/`rank_bm25`/`pypdf`/`requests`/`uvicorn`/`dotenv`），`requirements.txt` 已 pin 直接依赖到实测版本；`compileall`（exit 0）与 23 项离线单测（OK）仍全绿
- **额外证据（超出 T-004 验收，但 import 通过 ≠ 能跑，故必做）**：`MOCK=1` 下真跑了一次最小烟雾 ——
  - `& $P -m src.ingest` → exit 0，`files=2 chunks=12 elapsed_ms=1698 mocked=True`；`data/chunks.jsonl` 12 行、`data/chroma/` 已生成
  - `Retriever.search('管理端数据看板有哪些指标')` → vector 10 + bm25 10 → fused 12 → final 4，命中 `00_保洁系统_项目说明.md`
  - 结论：**chromadb 落盘读写 + jieba/rank_bm25 关键词召回 + RRF 融合已在完全离线、零 API key 下跑通**（"从没真跑过"这句已不成立）
- **进行中**：无。**下一步 T-005（建 20 题题库）** —— 它是 T-007 的另一半前置
- **关键上下文**：
  - 装到的版本：`openai==3.15.0`（**3.x 大版本**，代码是按 1.x 风格写的；已静态核对 `client.embeddings.create` 与 `client.chat.completions.create` 仍存在，但**真实调用未验证**）、`chromadb==1.5.9`、`fastapi==0.141.1`
  - `PLAN.md §3` 步骤 6 写的 `node push-task.mjs "$R" -m "..."` 是**相对路径、跑不通** —— 脚本实际在 `_kit_inspect\agent-project-kit\push-task.mjs`，必须用绝对路径调用。待 T-011 一并修
- **已知问题（范围外，按 §9.3 只记录不改）**：
  - `data/raw/README.md` 被当作语料入库了（烟雾 `files=2` 里含它）—— 它是语料格式说明书，会污染检索。建议 T-006 / T-007 阶段处理（在 `read_raw_files` 里排除 README，或把它移出 `raw/`）
- **待确认**：同「第二段」三条（语料需用户提供 / 题库内容需基于语料才有意义 / 真实评测会产生 API 费用）

### 交接 · 第二段（2026-09-18）

- **已完成**：`docs/PLAN.md` 已按 `SPEC.md` 重写为 **T-004 ~ T-010 共 7 张真实任务卡**（含逐条验收命令与通过标准）；`BOARD.md` 进度表同步
- **进行中**：M1 尚未开工。**下一步是 T-004（装依赖）**——它是 T-007 的硬前置
- **关键上下文**：<!-- 只有代码里看不出来的才写 -->
  - 沙箱内 `PATH` 为空字符串，**所有命令必须写绝对路径**
  - Python 解释器：`C:\Users\a2695\AppData\Local\Programs\Python\Python312\python.exe`（3.12.0）
  - ⚠️ 机器上另有 **Anaconda Python 3.7**（`C:\Users\a2695\anaconda3\python.exe`）—— `SPEC.md §2.7` 明令**禁止使用**
  - 本地验收两条命令：`compileall` + `unittest discover`（当前 **23 项全绿**）
  - 远端 `origin` = `git@github.com:ano-kodokushi/rag-doc-qa.git`，SSH 免密，推送约 5 秒
  - 本仓库第一次推送是**强推覆盖** GitHub 的自动初始化提交（原内容只有一个 30 字节的 `# rag-doc-qa`）
  - **代码已实现，不是空壳**：`src/` 9 模块 + `eval/` + `app/` 均无 TODO/占位。
    真正的缺口是①依赖未装 ②`eval/questions.jsonl` 不存在 ③从没真跑过
  - **`MOCK=1` 仍需要 `chromadb`**（`SPEC.md §5`）—— 所以"不花钱跑通"也得先装依赖
- **待确认**：
  - `data/raw/` 的 10 份语料尚未下载（`docs/DOC_SOURCES.md` 列了来源）—— **需用户提供**
  - `eval/questions.jsonl` 的题目内容需基于语料才有意义（T-005 只做结构占位，T-008 出正式题）
  - **真实评测会产生 API 费用**（Embedding + 生成），跑 T-009 前需用户确认并填 `.env`

## 5. 累计统计

| 指标 | 数值 |
|---|---|
| 任务总数 | 27（T-001~T-003 治理脚手架；T-004~T-010 RAG 实现计划；T-011/T-017 治理链修复；T-012~T-014 缺陷修复；T-015 性能优化；T-016/T-018 对照实验与结论修正；T-019 指标改造；T-020 小节级整理对照；T-021 rerank 真实验证；T-022 `top_k`×rerank 联合；T-023 小节级覆盖量化；T-024 加大窗口的 LLM 侧验证；T-025 三路检索对照；T-026/T-027 第二语料域） |
| 已完成 | 27 |
| 待审 | 0 |
| 待办 | 0 |
| 阻塞中 | 0 |
| 返工次数 | 0 <!-- 反映任务书写得好不好：返工多说明验收标准不清楚 --> |
